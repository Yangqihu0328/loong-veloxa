---
description: 复杂度级别系统 - 决定任务使用哪种工作流路径
globs: []
alwaysApply: false
---

# 复杂度级别系统

根据任务复杂度自动选择合适的工作流和文档深度。

## 级别定义

### Level 1：快速修复
- **特征：** 单文件修改、定向修复、明确的 bug
- **工作流：** `/van` → `/build` → `/reflect` → `/archive`
- **文档：** 最小化，聚焦于修复本身
- **预估时间：** < 15 分钟

**判断标准：**
- 修改 1-2 个文件
- 不需要新组件
- 不需要设计决策
- 修复路径明确

**Level 1 完整路径：**
`/van`（初始化）→ `/build`（微设计 + TDD 实现）→ `/reflect`（简要回顾）→ `/archive`（简要归档）

其中 `/build` 内置微设计：用 1-2 句话与用户确认修复方向（替代完整头脑风暴），然后进入 TDD 循环。

### Level 2：简单增强
- **特征：** 多文件修改、需求清晰、有现有模式可循
- **工作流：** `/van` → `/plan` → `/build` → `/reflect` → `/archive`
- **文档：** 基础计划、实现步骤
- **预估时间：** 15-60 分钟

**判断标准：**
- 修改 2-5 个文件
- 遵循已有模式
- 不需要架构决策
- 可能需要简单规划

### Level 3：中等功能
- **特征：** 新组件、需要设计决策、多个相关变更
- **工作流：** `/van` → `/plan` → `/creative` → `/build` → `/reflect` → `/archive`
- **文档：** 全面计划、创意设计文档、详细回顾
- **预估时间：** 1-4 小时

**判断标准：**
- 创建新组件或模块
- 需要 UI/UX 或架构设计决策
- 涉及多个子系统的协调
- 需要探索多种实现方案

### Level 4：复杂系统
- **特征：** 多个子系统、架构决策、分阶段实现
- **工作流：** `/van` → `/plan` → `/creative` → `/build` → `/reflect` → `/archive`（`/build` 阶段内部可分多轮迭代，每轮完成一个子系统后做阶段性验证）
- **文档：** 分阶段实现计划、架构图、综合归档
- **预估时间：** > 4 小时

**判断标准：**
- 涉及多个独立子系统
- 需要重大架构决策
- 可能需要分多个迭代
- 高风险，需要谨慎规划

**迭代机制：** Level 4 的 `/build` 阶段按子系统分轮实施。每轮迭代包含：实现子系统 → 验证子系统 → 集成测试 → 更新 `progress.md`。所有子系统完成后再统一进入 `/reflect`。

## 多轮次 Build 中间态（跨 Level 2-4，来源 TASK-20260419-13）

> 反复出现（TASK-20260419-03 Round 1 合并暂停 ~30 min / TASK-20260419-04 多 Phase 停歇期状态漂移）：长 plan（Phase ≥ 5）拆多天执行时，单个 `构建中` 状态覆盖跨度过粗，本轮完成但未进入 `/reflect` 的时段里 `activeContext.md` 无中间锚点，重新召回需重读 plan 全文 + 差异 diff，~10-20 min 恢复成本。

### 触发条件

任一命中即适用：
- Level 4 迭代机制的每轮子系统结束
- Level 2-3 plan 包含 **≥ 5 Phase** 且单次 session 只完成部分 Phase
- 用户明确要求中途暂停（如"先提交到这里明天继续"）

### 子状态协议

- **`activeContext.md` 当前阶段**：沿用 `构建中`（**不新增独立状态**）
- **子状态标签**：在 `构建中` 下方显式追加一行，形如：
  ```markdown
  ## 当前阶段
  构建中·轮次 N 完成（Phase P1-P3 of P5 ✅；下次进入 P4）
  ```
  其中 `N` 对应 Level 4 子系统迭代号 或 Level 2-3 的 Phase 分段组号
- **`progress.md`**：在任务里程碑列表里对已完成 Phase 打 ✅，**保留**未完成 Phase 为 ⏳
- **`tasks.md`**：状态保持 `🟡 构建中`，**不**改为 `规划完成` / `回顾中`

### 向前兼容

- 未触发多轮次机制（Phase < 5 且一次性完成）→ 完全不受影响，保持原 5 主阶段骨架
- 已有 Level 4 迭代机制段是**特化描述**，本段是**跨 Level 的通用扩展**
- `/build` 命令守卫在每 Phase commit 后可选输出"继续下一 Phase / 暂停等待"，由本段授权的 `构建中·轮次 N 完成` 作为暂停态合法标签

### 进入 `/reflect` 的恢复路径

- 下次 session 启动时识别 `构建中·轮次 N 完成` 子状态 → 跳过 `/van` `/plan` 守卫 → 直接续 `/build` 余下 Phase
- 所有 Phase 完成后 `/build` 末尾清除子状态标签 → 回到正常 `构建中` → 才允许 `/reflect` 进入

### 与 git 的关系

- `wip` 游离 commit 仅允许在**同 Phase 内部**（测试红 / green / refactor 之间）
- Phase 结束必须 squash 或直接 `docs(...): <task> P<N> — <subject>` 常规 commit；**子状态切换不使用游离 wip**
- 参考 `.cursor/rules/skills/git-workflow.mdc`「提交粒度」

## 复杂度决策树

**从高到低检查，取最高匹配级别：**

```
任务描述
  ├── 涉及多个子系统或重大架构？
  │   └── YES → Level 4
  ├── 需要新组件或设计决策？
  │   └── YES → Level 3
  ├── 遵循现有模式，不需要设计决策？
  │   └── YES → Level 2
  └── 修改 1-2 个文件，修复路径明确？
      └── YES → Level 1
```

## 级别可以升级

如果在执行过程中发现任务比预期更复杂：
- 随时可以升级复杂度级别
- 不能降级（避免偷懒跳过步骤）
- 升级后的补救路径：

| 升级场景 | 当前阶段 | 补救步骤 |
|---------|---------|---------|
| Level 1 → Level 2 | 构建中 | 暂停构建 → 返回 `/plan` 补充规划 → 继续 `/build` |
| Level 1/2 → Level 3 | 任何阶段 | 暂停当前 → 返回 `/plan` 重新规划 → `/creative` 设计 → 继续 `/build` |
| Level 2/3 → Level 4 | 任何阶段 | 暂停当前 → 返回 `/plan` 分解子系统 → `/creative` 设计 → 继续 `/build` |

升级时更新 `tasks.md` 中的复杂度级别和补充步骤，在 `progress.md` 中记录升级原因。

## 各级别 Memory Bank 文档要求

| 文件 | Level 1 | Level 2 | Level 3 | Level 4 |
|------|---------|---------|---------|---------|
| tasks.md | 简要记录 | 基础任务列表 | 详细任务分解 | 分阶段任务 |
| activeContext.md | 基础状态 | 基础上下文 | 详细上下文 | 全面上下文 |
| progress.md | 简要进度 | 步骤跟踪 | 详细进度 | 分阶段进度 |
| techContext.md | 按需更新 | 按需更新 | 归档时更新 | 归档时更新 |
| systemPatterns.md | 按需更新 | 按需更新 | 归档时更新 | 归档时更新 |
| productContext.md | 按需更新 | 按需更新 | 归档时更新 | 归档时更新 |
| creative/ | 不需要 | 不需要 | 需要 | 需要 |
| reflection/ | 简要（可内联到 progress） | 基础 | 详细 | 全面 |
| archive/ | 简要 | 基础 | 详细 | 全面 |
