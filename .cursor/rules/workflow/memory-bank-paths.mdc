---
description: Memory Bank 文件路径定义 - 定义所有持久化文件的标准位置
globs: []
alwaysApply: false
---

# Memory Bank 文件路径

## 目录结构

```
memory-bank/
├── tasks.md                          # 任务跟踪唯一真相来源
├── activeContext.md                   # 当前开发焦点
├── progress.md                       # 实现状态跟踪
├── projectbrief.md                   # 项目基础信息
├── techContext.md                     # 技术上下文和技术栈
├── systemPatterns.md                 # 系统模式和架构决策
├── productContext.md                 # 产品上下文和需求
├── creative/                         # 设计决策文档
│   └── creative-[feature-name].md
├── reflection/                       # 回顾文档
│   └── reflection-[task_id].md
└── archive/                          # 归档文档
    └── archive-[task_id].md
```

## 文档规格

```
docs/
├── specs/                            # 设计规格文档（"做什么"：需求层面的设计规格，由头脑风暴产出）
│   └── YYYY-MM-DD-[feature-name]-design.md
└── plans/                            # 实现计划文档（"怎么做"：分步骤的实现计划，由编写计划技能产出）
    └── YYYY-MM-DD-[feature-name].md
```

**`docs/specs/` 与 `memory-bank/creative/` 的职责划分：**
- `docs/specs/` — 需求层面的设计规格（"做什么"），由 `/plan` 的头脑风暴阶段产出，覆盖整体需求和约束
- `memory-bank/creative/` — 实现层面的方案探索（"怎么做"），由 `/creative` 产出，针对被标记的组件深入探索多种实现方案

## 核心文件说明

### tasks.md — 任务跟踪的唯一真相来源

所有任务状态、清单和组件列表的中心存储。任务 ID 格式：`TASK-YYYYMMDD-NN`。

#### 状态机定义

任务状态流转：`初始化` → `规划中` → `设计中` → `构建中` → `回顾中` → `归档中` → `已完成`（任何活跃状态均可转为 `已取消` 或 `已搁置`）

合法的状态转换及对应命令：

| 当前状态 | 允许的命令 | 转换到 |
|---------|-----------|-------|
| （无任务） | `/van` | 初始化 |
| 初始化 | `/plan`, `/build`(仅 Level 1), `/debug` | 规划中 / 构建中 / 调试中 |
| 规划中 | `/creative`(Level 3-4), `/build`(Level 2), `/debug` | 设计中 / 构建中 / 调试中 |
| 设计中 | `/build`, `/debug` | 构建中 / 调试中 |
| 构建中 | `/reflect`, `/debug`, `/plan`(回退重新规划) | 回顾中 / 调试中 / 规划中 |
| 调试中 | 调试完成 | 恢复到调试前状态 |
| 回顾中 | `/archive` | 归档中 |
| 归档中 | （自动完成） | 空闲 |
| 已完成 | `/van` | （新任务）初始化 |
| 任何活跃状态 | `/van`（搁置旧任务） | 已搁置 |
| 任何活跃状态 | `/van`（放弃旧任务） | 已取消 |
| 已搁置 | `/van` | （新任务）初始化 |
| 已取消 | `/van` | （新任务）初始化 |

**`/debug` 状态转换说明：** `/debug` 可在任何活跃状态（初始化～构建中）触发，触发时阶段变为 `调试中` 并记录调试前状态；调试完成后恢复到触发前的状态。

**`/archive` 状态转换说明：** `/archive` 执行时先将阶段设为 `归档中`，归档操作完成后将 `tasks.md` 中任务标记为 `已完成`，最后将 `activeContext.md` 重置为 `空闲`。

`activeContext.md` 中的 `当前阶段` 字段**必须**使用以上状态值之一，或 `空闲` 表示无活跃任务。

### activeContext.md — 当前开发焦点

维护当前开发阶段的焦点信息，包括：
- 当前阶段（必须是状态机中的合法值：空闲/初始化/规划中/设计中/构建中/回顾中/归档中/已完成/已取消/已搁置/调试中）
- 当前正在处理的任务及任务 ID
- 关键决策和约束
- 待解决的问题
- 调试前状态（仅 `/debug` 期间记录，用于调试完成后恢复）

### progress.md — 实现状态跟踪

跟踪实现进度，包括：
- 已完成的步骤
- 当前状态
- 遇到的问题和解决方案

### projectbrief.md — 项目基础信息

项目的基础上下文，包括：
- 项目概述和目标
- 技术栈
- 项目结构
- 团队约定

### techContext.md — 技术上下文

技术栈和技术决策的持久化记录，包括：
- 使用的技术栈和版本
- 开发环境配置
- 技术约束
- 依赖关系
- 在 `/archive` 时追加任务中引入的新依赖和技术变更

### systemPatterns.md — 系统模式和架构决策

项目中使用的设计模式和架构决策记录，包括：
- 架构模式（如 MVC、微服务等）
- 编码规范
- 数据流模式
- 集成模式
- 安全模式
- 关键架构决策日志
- 在 `/archive` 时追加新发现的模式和决策

### productContext.md — 产品上下文

产品层面的需求和上下文，包括：
- 产品概述和目标用户
- 用户故事
- 功能需求和非功能需求
- 业务约束
- 在 `/archive` 时追加产品变更

## 文件更新规则

1. **tasks.md** — 每个命令都必须读取，大多数命令需要更新
2. **activeContext.md** — 命令切换时更新，记录当前焦点
3. **progress.md** — 实现过程中持续更新
4. **projectbrief.md** — 初始化时创建，很少更新
5. **techContext.md** — 初始化时创建，技术栈变更时更新
6. **systemPatterns.md** — 架构决策时更新
7. **productContext.md** — 需求变更时更新
8. **creative/*.md** — `/creative` 命令创建
9. **reflection/*.md** — `/reflect` 命令创建
10. **archive/*.md** — `/archive` 命令创建

## 禁止存储规则

以下内容**严禁**写入 Memory Bank 或任何受版本控制的文件：

- API 密钥、Token、密码、数据库连接字符串
- 私钥、证书文件内容
- 个人身份信息（PII）：身份证号、银行卡号、手机号等
- 生产环境的具体 URL、IP 地址、端口
- 任何标记为机密的商业信息

如果实现过程中涉及以上内容，使用 `[REDACTED]` 占位符代替，并在 `techContext.md` 中记录"此功能涉及敏感配置，具体值存储在 `.env` 中"。

## 文件初始化

当 Memory Bank 目录不存在或缺少核心文件时，`/van` 命令负责创建它们。
