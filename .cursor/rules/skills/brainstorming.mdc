---
description: 头脑风暴技能 - 在任何创造性工作之前使用，通过苏格拉底式对话将想法转化为完整设计
globs: []
alwaysApply: false
---

# 头脑风暴：将想法转化为设计

通过自然的协作对话，帮助将想法转化为完整的设计和规格文档。

> **铁律：** 在你提出设计并获得用户批准之前，不要调用任何实现技能、编写任何代码、搭建任何项目或执行任何实现操作。无论项目看起来多简单，都适用此规则。

## 适用范围

- **Level 2-4 任务：** 必须完整执行本技能的所有步骤
- **Level 1 任务：** 在 `/build` 中进行微设计（1-2 句话确认方案即可），不需要完整的头脑风暴流程

## 反模式："这太简单了，不需要设计"

对于 Level 2 及以上的任务，每个都要经过这个过程。"简单"的项目恰恰是未经审查的假设最容易导致浪费的地方。设计可以很短（几句话即可），但你**必须**展示它并获得批准。

## 核心清单

你**必须**按顺序完成以下步骤：

1. **探索项目上下文** — 检查文件、文档、最近的提交
2. **提出澄清问题** — 一次一个，理解目的/约束/成功标准
3. **提出 2-3 种方案** — 附带权衡分析和你的推荐
4. **展示设计** — 按部分展示，每部分后确认
5. **保存设计文档** — 写入 `docs/specs/` 目录
6. **用户审查规格** — 让用户在继续之前审查规格文档
7. **过渡到实现** — 将设计结论传递给后续的计划编写步骤（当本技能由 `/plan` 命令内联调用时，自然衔接到计划编写阶段）

## 流程

```
探索上下文 → 逐一提问 → 提出方案 → 展示设计 → 用户批准
    ↓                                        ↓
  读取文件/文档                           保存设计文档
  检查最近提交                            → /plan 命令
```

## 流程详解

### 理解想法

- 首先查看当前项目状态（文件、文档、最近提交）
- 在问详细问题之前评估范围：如果请求描述了多个独立子系统，立即标记出来
- 如果项目太大不适合单一规格，帮助用户分解为子项目
- 逐一提问以细化想法
- 尽量使用选择题，开放式也可以
- 每条消息只问一个问题
- 聚焦于：目的、约束、成功标准

### 探索方案

- 提出 2-3 种不同的方案并附权衡分析
- 以推荐选项开头并解释原因
- 以对话方式展示

### 展示设计

- 当你认为理解了要构建什么时，展示设计
- 按复杂度调整每部分的篇幅：简单的几句话，复杂的 200-300 字
- 每个部分后询问是否正确
- 覆盖：架构、组件、数据流、错误处理、测试策略、**安全考量**（认证/授权需求、输入验证策略、敏感数据处理、攻击面分析）
- 准备好在需要时回溯澄清

### 设计原则

- 将系统分解为更小的单元，每个单元有一个清晰的目的
- 通过良好定义的接口通信
- 可以独立理解和测试
- 更小、边界清晰的单元更容易推理和编辑

### 在现有代码库中工作

- 在提出更改之前探索当前结构，遵循现有模式
- 如果现有代码有影响工作的问题，将改进包含在设计中
- 不要提出无关的重构，保持聚焦于当前目标

## 设计完成后

1. 将设计文档保存到 `docs/specs/YYYY-MM-DD-[feature-name]-design.md`
2. 更新 `memory-bank/tasks.md` 记录设计状态
3. 更新 `memory-bank/activeContext.md` 记录当前焦点
4. 更新 `memory-bank/progress.md` 记录头脑风暴完成的里程碑
5. 请用户审查写好的规格文档
6. 用户批准后，过渡到后续的计划编写步骤

## 关键原则

- **一次一个问题** — 不要用多个问题淹没用户
- **选择题优先** — 比开放式更容易回答
- **YAGNI 原则** — 从所有设计中移除不必要的功能
- **探索替代方案** — 总是在确定之前提出 2-3 种方案
- **增量验证** — 展示设计，获得批准后再继续
- **灵活应变** — 在需要时回溯澄清

## 跨决策协同度（来自 TASK-20260430-04 反思 §3.2）

当任务有多个相互依赖的决策点（典型 Level 4 蓝图任务 D1-D8）时，单决策不应孤立提出。每个决策抛出 AskQuestion 时，必须附带：

### 「与已锁定决策的协同度」标注

对每个候选选项，明确标注其与已锁定决策的兼容性：

```markdown
| 候选 | 描述 | 与 D2=B 协同 | 与 D4=B 协同 | 与 V3=A 协同 |
|:-:|---|:-:|:-:|:-:|
| A 全公开 C API | 所有 introspection 走 C API | ⚠️ 性能损失 | ⚠️ 共享容器零拷贝优势失效 | ✅ |
| B 全内部 C++ | 仅内部 C++，无公开扩展 | ✅ | ✅ | ⚠️ 无法对接外部 CDP |
| **C 双层 API ⭐** | 内部 C++ 核心 + 公开 C API 薄封装 | ✅ 一致 | ✅ 一致 | ✅ 一致 |
```

### VAN 推荐默认始终选「与已锁定决策一致」候选

如果某候选与已锁定决策矛盾，VAN 推荐 ⭐ **绝不**指向该候选；强行选该候选会触发架构不一致风险。

### 适用前置

- 任务复杂度 ≥ Level 4 + 决策矩阵 ≥ 5 项（决策依赖图非平凡）
- 决策间存在明显语义耦合（如数据采集协议 ↔ 隔离边界 ↔ API 边界）
- 不适用：单决策 / 决策完全独立的简单任务

### 反模式

- ❌ 决策抛出时只列候选优劣对比，忽略与其他决策的协同度
- ❌ 用户跳过 AskQuestion 隐式批准时，VAN 推荐指向「与已锁定决策矛盾」的候选
- ❌ brainstorming 阶段决策依赖图未前置（导致后续决策时回溯修订早期决策）

## 决策跳过率监控（来自 TASK-20260430-04 反思 §5 P2 #9）

「Checkpoint 推荐默认 + 隐式批准协议」（systemPatterns 段）允许用户跳过 AskQuestion 隐式批准 VAN 推荐。但当决策跳过率过高时，需要警觉：

### 触发监控

当用户在单任务内**连续 ≥ 5 次**跳过 AskQuestion（V/D 决策无差别）时，触发以下监控：

1. brainstorming 进行中时：每跳过 5 个决策，提示用户「您已连续跳过 N 个决策，按 VAN 推荐默认锁定。如需重新审视任一决策，请明示决策编号」
2. reflect 阶段：必须重新审视 D1-D8（或对应决策矩阵）合理性，回答：
   - 是否所有决策都「真的合理」（VAN 推荐基于充分实证）？
   - 还是「task is well-scoped」（推荐合理且无重大反对）？
   - 还是「用户疲劳 / 信任 VAN」（潜在风险点 — 决策错误未被识别）？

### 平衡张力

- **快速决策**：单任务高跳过率 → brainstorming 时间近零，加速主交付
- **决策审慎**：跳过率过高可能导致设计错误未被识别（架构债隐性累积）
- **平衡点**：跳过率 ≥ 80% 但 reflect 阶段无重大问题 = OK；跳过率 ≥ 80% 且 reflect 阶段发现 1+ 决策需返工 = 触发协议改进

### 实证

- TASK-20260430-04：13/13 决策跳过（V1-V5 + D1-D8），reflect 阶段 §2 重审无问题 → 协议有效
- 后续同类蓝图任务（如 Console / JS Debugger 蓝图阶段）应继续记录跳过率 vs reflect 重审差异点

### 适用前置

- VAN/brainstorm 推荐基于 grep 实证 + systemPatterns 既有规则（推荐质量高）
- 任务复杂度 ≥ Level 3 且决策数 ≥ 5（高跳过率才有意义）
- 不适用：决策数 ≤ 3 的简单任务（高跳过率天然合理）
