---
description: 系统化调试 - 遇到任何 Bug、测试失败或异常行为时使用，在提出修复方案之前
globs: []
alwaysApply: false
---

# 系统化调试

## 概述

随机修复浪费时间并制造新 Bug。快速补丁掩盖底层问题。

**核心原则：** 始终在尝试修复之前找到根因。症状修复是失败。

## 铁律

```
没有根因调查，就不能提出修复
```

如果你没有完成第一阶段，你不能提出修复方案。

## 适用场景

用于**任何**技术问题：
- 测试失败
- 生产环境 Bug
- 异常行为
- 性能问题
- 构建失败
- 集成问题

**特别是当：**
- 时间紧迫时（紧急情况让猜测变得诱人）
- "一个快速修复"看起来很明显时
- 你已经尝试了多次修复
- 之前的修复没用

## 四个阶段

必须按顺序完成每个阶段。

### 阶段 1：根因调查

**在尝试任何修复之前：**

1. **仔细阅读错误消息**
   - 不要跳过错误或警告
   - 它们通常包含确切的解决方案
   - 完整阅读堆栈跟踪
   - 记录行号、文件路径、错误代码

2. **一致地重现**
   - 你能可靠地触发它吗？
   - 确切的步骤是什么？
   - 每次都会发生吗？
   - 如果不可重现 → 收集更多数据，不要猜测

3. **检查最近的变更**
   - 什么变更可能导致了这个？
   - git diff，最近的提交
   - 新依赖、配置变更
   - 环境差异

4. **在多组件系统中收集证据**
   - 对每个组件边界添加诊断日志
   - 记录进入和离开组件的数据
   - 运行一次收集证据
   - 分析证据确定**哪里**出了问题

5. **追踪数据流**
   - 坏值从哪里来？
   - 什么调用传入了坏值？
   - 持续向上追踪直到找到源头
   - 在源头修复，不是在症状处

### 阶段 2：模式分析

**在修复之前找到模式：**

1. **找到工作示例** — 在同一代码库中找到类似的可工作代码
2. **与参考对比** — 完整阅读参考实现
3. **识别差异** — 列出所有差异，无论多小
4. **理解依赖** — 需要什么组件、设置、配置、环境？

### 阶段 3：假设与测试

**科学方法：**

1. **形成单一假设** — 明确陈述："我认为 X 是根因，因为 Y"
2. **最小测试** — 做最小的可能变更来测试假设
3. **验证后继续**
   - 成功了？ → 阶段 4
   - 没成功？ → 形成**新**假设
   - **不要**在上面叠加更多修复

### 阶段 4：实现

**修复根因，不是症状：**

1. **创建失败测试用例** — 使用 TDD 技能
2. **实现单一修复** — 一次一个变更
3. **验证修复** — 测试通过？没有其他测试被破坏？
4. **如果修复不起作用**
   - 停下来
   - 尝试了几次？
   - < 3 次：返回阶段 1，用新信息重新分析
   - **≥ 3 次：停下来质疑架构**
5. **如果 3+ 次修复失败：质疑架构**
   - 这个模式是否从根本上合理？
   - 应该重构架构还是继续修复症状？
   - **与用户讨论后再尝试更多修复**

## 并发会话 / 多 agent 冲突诊断（首发工具：git reflog）

**触发场景**：
- Background agent 模式下用户主线在另一会话推进
- 两个 agent / 工具实例在同一仓库工作
- 主 worktree 单一性导致并发写冲突

**典型症状**：
- `git status` 显示 clean 但你刚做了 StrReplace / 文件操作
- `git commit` 报 "nothing to commit, working tree clean"
- 文件内容回退到改动前
- 当前分支与预期不符
- ctest 通过率正常但 commit 缺失改动（编译跑在切分支前的旧 working tree）

**首发诊断命令**（任一症状立即跑）：

```bash
git reflog --date=iso | head -20
```

**判读标准**：
- 看 1 秒级时间戳的多次 `checkout` / `branch` 操作 → 外部会话切分支
- 看 `moving from X to Y` 序列 → 工具自动 reset working tree
- 看你预期 commit 之间出现非自己发起的操作 → 并发干扰确认

**实证案例**（TASK-20260430-03 R2 阶段）：
```
9411584 HEAD@{2026-04-30 23:45:08 +0800}: checkout: moving from main to feature/TASK-20260430-04
9411584 HEAD@{2026-04-30 23:45:08 +0800}: checkout: moving from feature/TASK-20260430-03 to main
1467207 HEAD@{2026-04-30 23:44:19 +0800}: checkout: moving from feature/TASK-20260430-04 to feature/TASK-20260430-03
9411584 HEAD@{2026-04-30 23:41:30 +0800}: checkout: moving from main to feature/TASK-20260430-04
9411584 HEAD@{2026-04-30 23:41:23 +0800}: checkout: moving from feature/TASK-20260430-03 to main
1467207 HEAD@{2026-04-30 23:41:07 +0800}: commit: fix(html): R2.2 F-033 ...
```
↑ 23:41:23/30 同秒双切（main → 04） + 23:45:08 同秒再次双切 = 强冲突信号。

**应对协议**（确认冲突后立刻执行）：

1. **物理隔离 worktree**：
   ```bash
   git worktree add .worktree-<task_short>/ feature/TASK-<id>-<slug>
   cd .worktree-<task_short>
   ```
2. **每 commit 前断言守门**（详见 git-workflow skill「Commit 前防御断言」段）：
   ```bash
   [ "$(git symbolic-ref --short HEAD)" = "feature/TASK-<id>-<slug>" ] || exit 1
   ```
3. **CMake / build 配置同步**：worktree 默认配置可能与主 worktree 不同（如 `VX_BUILD_BENCHMARKS=OFF`），重 configure 时显式指定选项匹配基线
4. **完成后清理**：`rm -rf <wt>/build` 后 `git worktree remove --force <wt>/`

**与 git-workflow skill 联动**：本段「reflog 诊断」+ git-workflow「Commit 前防御断言」是孪生协议，分别处理「事后定位」和「事前预防」。

---

## 红色信号 — 停下并遵循流程

如果你发现自己在想：
- "先快速修复，之后再调查"
- "试试改 X 看看行不行"
- "同时加多个变更，跑测试"
- "跳过测试，我手动验证"
- "大概是 X，让我修复那个"
- "我不完全理解但这可能有用"
- "再试一次修复"（已经试了 2+ 次）

**以上所有意味着：停下。返回阶段 1。**

## 常见借口

| 借口 | 现实 |
|------|------|
| "问题很简单，不需要流程" | 简单问题也有根因 |
| "紧急情况没时间" | 系统化调试比反复猜测更快 |
| "先试这个，再调查" | 第一次修复定下基调。从一开始就做对 |
| "部分检查就够了" | 部分检查什么都证明不了 |

## 与 Memory Bank 集成

调试过程中持续更新：
- **activeContext.md** — 记录调试中状态和调试前状态（用于恢复）
- **progress.md** — 记录调试阶段、假设、验证结果、最终根因和修复方案

调试完成后：
- 恢复 `activeContext.md` 到调试前状态，并清除 `调试前状态` 字段
- 在 `progress.md` 中记录调试结论
- 重要 Bug 的根因分析结果应持久化到 `systemPatterns.md`（防止同类问题复发）

## 与其他技能的衔接

- **调试发现设计缺陷** → 回到 `/plan` 或 `/creative` 重新设计
- **3+ 次修复失败，需要重构** → 与用户讨论，可能回到 `/plan` 进行架构调整
- **阶段 4 创建失败测试** → 使用 TDD 技能（`test-driven-development.mdc`）
- **修复完成后清理诊断日志** → 移除阶段 1 添加的所有临时诊断代码

## 快速参考

| 阶段 | 关键活动 | 成功标准 |
|------|---------|---------|
| 1. 根因 | 读错误、重现、检查变更、收集证据 | 理解什么和为什么 |
| 2. 模式 | 找工作示例、对比 | 识别差异 |
| 3. 假设 | 形成理论、最小测试 | 确认或新假设 |
| 4. 实现 | 创建测试、修复、验证 | Bug 解决，测试通过 |
