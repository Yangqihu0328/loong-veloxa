---
description: 子代理驱动开发 - 当执行具有独立任务的实现计划时使用
globs: []
alwaysApply: false
---

# 子代理驱动开发

通过为每个任务分派新鲜的子代理来执行计划，每个任务后进行两阶段审查：先规格合规性审查，再代码质量审查。

**核心原则：** 每任务新鲜子代理 + 两阶段审查 = 高质量、快速迭代

## 适用场景

- 有实现计划
- 任务大部分独立
- 在当前会话中执行

## Build 阶段重评估（**P1**，TASK-20260426-01 升级）

> 反复模式新观察（TASK-20260426-01 R3/R4 实证）：plan 阶段标 D3 子代理任务 3 处（R3.3 LayoutBlock 算法 / R4.3 FT metrics / R4.6 LayoutInline）实际全直执行均成功且更快，提示「creative 锁定 + 单文件主修改 + 上下文连续」场景子代理 overhead > 收益。

**触发**：plan 阶段标 D3 但 build 阶段开始前需要重评估是否真要派子代理。

**降级直执行 3 条同时满足条件**：

1. **Creative 完整锁定算法**（含全部公式 + 坐标约定 + 边界 case + ABI 决策）— creative-X.md 已 ACCEPT 且无未决决策
2. **单文件主修改**（核心算法集中在 1 个 .cc/.h，外围改动 ≤ 2 文件）
3. **plan / 上下文连续**（同一会话内 R0-R(N-1) 已完成，子代理 prompt 重复输入上下文成本 > 直执行）

**保持子代理（D3）3 条任一不满足**：

- ❌ creative 留有未决决策（如「2-pass 还是 3-pass 待 build 实证」）
- ❌ 多文件协同（≥ 3 文件，含跨子库）
- ❌ 跨会话或与其他工作并行（上下文切换成本高）

**判定示例**（来自 TASK-20260426-01）：

| 任务 | plan 标记 | 重评估结果 | 理由 |
|---|---|---|---|
| R3.3 LayoutBlock 算法重写 | D3 子代理 A | **降级直执行** | creative-margin-collapsing.md D1 5 决策全 ACCEPT + 单文件 layout_engine.cc + R0-R2 上下文连续 |
| R4.3 FreeType FT metrics | D3 子代理 B | **降级直执行** | creative-line-box-model.md D2.C ABI 锁定 + 单文件 freetype_shaper.cc + ABI 兼容渐进式 |
| R4.6 LayoutInline 算法重写 | D3 子代理 C | **降级直执行** | creative D2.B strict 2-pass 锁定 + 单文件 + R3 上下文连续 |

**反模式**：build 阶段不重评估，盲目按 plan D3 派子代理 → 子代理需要重复 ingest creative 文档（~5-10 min） + 两阶段审查（~10-15 min）+ 失败重派（30%）；同等任务直执行可 ~30-60 min 完成。

**操作步骤**（写入 build.md §6.5「轮次开始重评估」）：

```markdown
- [ ] 读取 plan §6 子代理决策表
- [ ] 对每个标 D3 的任务，对照「降级直执行 3 条件」检查
- [ ] 全部满足 → 降级直执行，progress.md 记「实证微调：D3 → 直执行，降级理由 X/Y/Z」
- [ ] 任一不满足 → 保持 D3，按原计划派遣
```

## 流程

```
读取计划 → 提取所有任务 → 创建任务列表
    ↓
[对每个任务]
    分派实现子代理
        ↓
    子代理提问？ → 回答后重新分派
        ↓ (无问题)
    子代理实现、测试、提交、自审
        ↓
    分派规格审查子代理
        ↓
    规格合规？
        ↓ 否 → 修复后重新审查
        ↓ 是
    分派质量审查子代理
        ↓
    质量通过？
        ↓ 否 → 修复后重新审查
        ↓ 是
    标记任务完成
        ↓
[更多任务?] → 下一个任务
        ↓ (全部完成)
    最终全局代码审查
        ↓
    完成开发分支
```

## 模型选择

使用能处理每个角色的最低能力模型来节省成本和提高速度。

- **机械实现任务**（独立函数、清晰规格、1-2 个文件）→ 快速模型
- **集成和判断任务**（多文件协调、模式匹配、调试）→ 标准模型
- **架构、设计和审查任务** → 最强模型

## 处理实现者状态

| 状态 | 处理方式 |
|------|---------|
| **DONE** | 进入规格合规审查 |
| **DONE_WITH_CONCERNS** | 阅读关切，评估后进入审查 |
| **NEEDS_CONTEXT** | 提供缺失上下文，重新分派 |
| **BLOCKED** | 评估阻碍：上下文？能力？任务太大？计划错误？ |

## 子代理输出验证

子代理生成的代码必须在主线程统一验证，特别是跨语言组件：

### 编译验证要求

| 输出类型 | 验证方式 | 说明 |
|---------|---------|------|
| C++ 代码 | `make` 编译 + `ctest` 运行 | 主线程直接验证 |
| Python SDK | `python -m py_compile` + `pytest`（如有） | 检查语法正确性 |
| Node.js/TypeScript | `npx tsc --noEmit`（TS）或 `node -c`（JS） | 检查语法和类型 |
| Go 代码 | `go build` + `go test` | 检查编译和测试 |
| 配置/文档 | 格式校验（YAML lint、Markdown lint） | 基础格式检查 |

### 流程

1. 子代理完成实现后，主线程**必须**对所有输出执行编译验证
2. 如果当前环境缺少某语言工具链（如无 Go 编译器），在审查记录中标注"**未验证：缺少 X 工具链**"，并在 `progress.md` 中记录为遗留项
3. 跨语言组件（如 SDK + C++ 服务端联动）应在集成测试中验证端到端行为

## 关键规则

**永远不要：**
- 在 main/master 分支上开始实现（除非用户明确同意）
- 跳过审查（规格合规或代码质量）
- 在未修复的问题下继续
- 并行分派多个实现子代理（冲突）
- 让子代理自己读取计划文件（应由你直接提供完整计划文本给子代理）
- 忽略子代理问题（回答后再让他们继续）
- 在规格合规通过之前开始代码质量审查（顺序错误）
- 跳过子代理输出的编译验证（即使子代理声称已测试通过）
- 让子代理修改全局共享资源（见下方「禁止修改清单」），这些由主线程统一管理

**如果审查发现问题：**
- 实现者（同一子代理）修复
- 审查者重新审查
- 重复直到通过
- 不要跳过重新审查

## 子代理 Prompt 模板

分派子代理时，prompt 应包含以下结构化上下文，避免因上下文不足导致返工：

### 必需上下文

1. **项目基础** — 语言/框架/构建系统（如"C++17, CMake, vcpkg, 命名空间 aegisgate"）
2. **编码约定** — 头文件守护(`#pragma once`)、智能指针优先、错误处理模式
3. **文件路径** — 涉及的所有文件的完整路径（含已有文件的关键接口签名）
4. **测试要求** — 测试框架（GTest/GMock）、测试文件命名（`test_*.cpp`）、CMake 注册方式
5. **构建约束** — 编译器标志、链接目标、可选编译开关

### 禁止修改清单

子代理**不得**修改以下全局共享资源（由主线程统一管理）：

- 根 `CMakeLists.txt`（子代理只操作子目录的 CMakeLists）
- `vcpkg.json` / `vcpkg-configuration.json`
- `memory-bank/` 下任何文件
- `.cursor/` 下任何文件
- `.github/` 下任何文件
- `.gitignore`、`.clang-format`、`.clang-tidy`

### Prompt 骨架示例

```
你要实现 [功能名]。

项目：C++17, CMake, GTest, 命名空间 aegisgate
目标文件：[精确路径列表]
接口：[关键接口签名，从代码中复制]
测试：创建 tests/test_[name].cpp，用 GTest TEST_F
构建：在 tests/CMakeLists.txt 中注册，链接 aegisgate_core + GTest::gtest_main
禁止：不修改根 CMakeLists.txt、不修改 vcpkg.json

[具体任务描述]
```

### LayoutBox 坐标语义（任务涉及 LayoutBox 时**必填**）

> 来源 TASK-04：子代理用 absolute 计算 hover 区域，但 LayoutBox 存的是 padding-box 相对父原点。

涉及 `LayoutBox`、坐标转换、命中测试、绘制定位时，prompt 必须包含：

- **`LayoutBox::origin`**：相对**父盒**的 padding-box 原点（不是绝对屏幕坐标）
- **要得到屏幕坐标**：自下而上累加链上每个父节点的 `origin`，或调用 `LayoutEngine::GetAbsoluteRect(node)`
- **`size`**：content-box 还是 border-box？查 `veloxa/core/layout/layout_box.h` 当前实现注释
- **margin/padding/border 是否已并入**：明示

提供「坐标转换辅助函数」清单到 prompt（避免子代理自己重新发明）。

### 并行子代理前置条件（计划要并行派发多个子代理时**必填**）

> 来源 TASK-08：3 个并行子代理同时改 EventDispatcher::AddEventListener，最后无法合并。

并行派发**前**必须验证（写入计划文档）：

1. **文件级隔离**：每个子代理写入的目标文件**互不重叠**？逐文件列出归属。
2. **共享头依赖**：所有子代理都依赖同一 .h 接口签名时，**必须**先有一个串行的「接口冻结」前置 Phase 把 .h 改完并提交，再并行派发实现。
3. **数据流方向**：若 A 的输出是 B 的输入，**禁止**并行 → 串行执行。
4. **测试目标隔离**：每个子代理新增的测试文件互不重叠？

只有 1+2+3+4 **全部满足**才能并行派发。否则降为串行。

### 跨模块数据格式（涉及共享数据结构时**必填**）

> 来源 TASK-02：缺少格式 prompt 触发 24 小时返工（像素格式假设矛盾）。

当子代理任务读写跨模块共享数据时，prompt 必须包含：

- **格式精确定义**：bit 布局、字节序、单位、范围
  - 例：`RGBA32 = R[0:7] | G[8:15] | B[16:23] | A[24:31]`
  - 例：`CSS color = u32 0xRRGGBBAA`（与上面**不同**！黑色 CSS=0x000000FF，gfx=0xFF000000）
- **格式引用位置**：上游/下游模块在哪里使用？文件 + 函数
- **已知不一致**：如有跨格式风险（CSS color vs gfx::Color），明示桥接函数（如 `CssColorToGfx`）

反例（不要给子代理这种描述）：
> "把 CSS background-color 应用到 PaintCommand"

正例：
> "把 ComputedStyle::background_color (u32 RRGGBBAA) 通过 render_utils.h::CssColorToGfx 转换为 gfx::Color，再写入 PaintCommand::FillRect{color, ...}"

## 与 Memory Bank 集成

- 每个任务完成后更新 `memory-bank/progress.md`
- 更新 `memory-bank/tasks.md` 中的任务状态
- 记录重要决策到 `memory-bank/activeContext.md`
