---
description: 代码审查 - 在任务之间进行质量检查时使用
globs: []
alwaysApply: false
---

# 代码审查

## 概述

代码审查是质量保证的关键环节。**在完成每个计划任务的所有 TDD 循环后、提交代码之前**进行审查。

**触发时机：** 一个计划任务中的所有 TDD 循环完成 → 代码审查 → 通过后提交

## 审查前清单

在请求代码审查之前，确保：

- [ ] 所有新代码都有测试
- [ ] 所有测试通过
- [ ] 没有 linter 错误
- [ ] 代码格式正确
- [ ] 没有注释掉的代码
- [ ] 没有调试日志遗留
- [ ] 不必要的文件没有被包含

## 审查维度

### 1. 规格合规性（优先级最高）

- 代码是否实现了规格中的所有需求？
- 是否有遗漏的需求？
- 是否有超出规格的额外功能？（YAGNI）

### 2. 代码质量

| 维度 | 检查项 |
|------|--------|
| **正确性** | 逻辑正确？边界情况处理？错误处理？ |
| **可读性** | 命名清晰？结构合理？复杂度可控？ |
| **可维护性** | DRY？单一职责？接口清晰？ |
| **性能** | 明显的性能问题？不必要的复杂度？ |
| **安全性** | 输入验证？敏感数据处理？注入风险？ |

### 3. 测试质量

- 测试覆盖了关键路径？
- 测试覆盖了边界情况？
- 测试覆盖了错误情况？
- 测试名称清晰描述行为？
- 测试使用真实代码而非过度 mock？

## 问题严重级别

| 级别 | 描述 | 处理方式 |
|------|------|---------|
| **Critical** | 功能错误、数据丢失风险、安全漏洞 | **阻塞进展**，必须修复 |
| **Important** | 潜在 Bug、缺少错误处理、测试不足 | 应该修复后再继续 |
| **Suggestion** | 命名改进、代码风格、小优化 | 可以选择性采纳 |

## 两阶段审查（用于子代理开发）

### 阶段 1：规格合规性审查

检查实现是否匹配计划中的任务规格：
- 所有需求都实现了？
- 没有多余的实现？
- 文件路径和结构匹配？
- 测试覆盖了规格中的场景？

如果不通过 → 修复后重新审查

### 阶段 2：代码质量审查

在规格合规通过后：
- 代码质量是否达标？
- 是否遵循了项目模式？
- 是否有技术债务？

如果不通过 → 修复后重新审查

## 审查后操作

- 所有 Critical 和 Important 问题已修复
- 重新运行测试确认修复没有引入回归
- 更新 `memory-bank/progress.md` 记录审查结果
- 如果审查发现架构性问题 → 更新 `memory-bank/systemPatterns.md`
- 如果审查发现安全问题 → 记录到 `memory-bank/techContext.md` 的技术约束
- 继续下一个任务或完成开发

## 与其他技能的衔接

- **审查发现规格不清晰** → 针对不清晰部分与用户做补充讨论并更新 `docs/specs/` 中的设计文档，不需要重新执行完整头脑风暴流程
- **审查发现实现偏离规格** → 修复实现或更新规格（与用户确认）
- **审查通过** → 提交代码（`git-workflow.mdc`），继续下一个任务
