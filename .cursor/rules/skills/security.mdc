---
description: 安全技能 - 涉及外部输入、认证授权、数据存储、API 接口、部署配置时使用
globs: []
alwaysApply: false
---

# 安全

## 概述

安全不是附加功能，是基础设施。每个接受外部输入、存储数据、暴露接口的系统都需要安全设计。

**核心原则：** 安全必须内建于设计和实现中，而非事后补救。

## 适用场景

**必须触发：**
- 任何接受用户输入的功能（表单、API、文件上传）
- 认证和授权系统
- 数据存储和传输（数据库、缓存、消息队列）
- 外部 API 集成
- 部署和基础设施配置
- 涉及敏感数据（PII、支付信息、凭证）的功能

**按需触发：**
- 依赖更新或新增
- 性能优化（可能引入安全降级）
- 重构（可能破坏安全边界）

## 一、威胁建模（在 `/plan` 阶段使用）

在设计阶段，对涉及安全的功能执行轻量威胁建模：

### 步骤

1. **识别资产** — 系统中有什么值得保护的？（用户数据、凭证、业务逻辑）
2. **绘制信任边界** — 哪里是受信任区域和不受信任区域的分界线？
3. **列举威胁** — 使用 STRIDE 模型：

| 威胁 | 含义 | 示例 |
|------|------|------|
| **S**poofing | 身份伪造 | 伪造 JWT、会话劫持 |
| **T**ampering | 数据篡改 | 修改请求参数、SQL 注入 |
| **R**epudiation | 抵赖 | 无审计日志、无操作记录 |
| **I**nformation Disclosure | 信息泄露 | 错误消息暴露堆栈、日志含敏感数据 |
| **D**enial of Service | 拒绝服务 | 无速率限制、资源耗尽 |
| **E**levation of Privilege | 权限提升 | 水平越权、垂直越权 |

4. **评估风险** — 对每个威胁评估影响和可能性
5. **制定缓解措施** — 针对高风险威胁制定具体的技术方案

### 产出

在设计文档或计划中增加安全章节：

```markdown
## 安全分析

**信任边界：** [描述]
**关键威胁：**
1. [威胁] — 缓解措施：[方案]
2. [威胁] — 缓解措施：[方案]

**安全需求：**
- [ ] [具体安全需求 1]
- [ ] [具体安全需求 2]
```

## 二、OWASP Top 10 (2021) 检查清单（在代码审查中使用）

审查代码时，逐项检查：

| # | 漏洞类型 | 检查项 |
|---|---------|--------|
| A01 | **访问控制失效** | RBAC/ABAC？资源级权限校验？CORS 配置？最小权限？ |
| A02 | **加密机制失效** | 传输加密（TLS 1.2+）？存储加密？密码哈希（bcrypt/argon2）？日志脱敏？ |
| A03 | **注入** | 参数化查询？ORM？输入验证？XSS 输出编码？CSP 头？ |
| A04 | **不安全设计** | 威胁建模？安全设计模式？业务逻辑滥用防护？ |
| A05 | **安全配置错误** | 默认凭证？调试模式？不必要的端口/服务？安全响应头？ |
| A06 | **自带缺陷和过时的组件** | 依赖审计？版本更新？已知 CVE 检查？ |
| A07 | **身份识别和认证失效** | 会话管理？MFA？暴力破解防护？密码策略？ |
| A08 | **软件和数据完整性失效** | 依赖来源验证？CI/CD 管道安全？不安全的反序列化？ |
| A09 | **安全日志和监控失效** | 安全事件日志？异常检测？告警机制？审计跟踪？ |
| A10 | **服务端请求伪造 (SSRF)** | URL 白名单？内网访问限制？DNS 重绑定防护？ |

## 三、安全编码规范（在 `/build` 阶段使用）

### 输入验证

- **所有外部输入都是不可信的** — 包括 URL 参数、请求体、Headers、Cookie、文件上传
- 在信任边界处验证：类型、长度、范围、格式
- 使用白名单而非黑名单
- 拒绝无效输入而非尝试清理

### 输出编码

- HTML 上下文 → HTML 实体编码
- JavaScript 上下文 → JavaScript 编码
- URL 上下文 → URL 编码
- SQL 上下文 → 参数化查询（永远不要拼接 SQL）

### 认证与会话

- 密码存储：bcrypt/argon2，永远不用 MD5/SHA
- 会话 Token：加密随机生成，足够长度（128+ bit）
- 会话管理：HttpOnly、Secure、SameSite Cookie 属性
- 登录失败：速率限制，不泄露用户是否存在

### 授权

- 每个请求都验证权限，不依赖前端隐藏
- 资源级权限检查（不只是角色检查）
- 最小权限原则

### 错误处理

- 生产环境不暴露堆栈跟踪、SQL 语句、内部路径
- 统一错误响应格式，区分面向用户和面向开发者的错误信息
- 记录详细错误到安全日志，返回通用错误给客户端

### 密钥管理

- **严禁**硬编码密钥、Token、密码到源码中
- 使用环境变量或密钥管理服务
- `.env` 文件不提交到版本控制
- 不同环境（dev/staging/prod）使用不同密钥
- 定期轮换密钥

## 四、依赖安全审计（在 `/build` 完成后使用）

### 审计命令

| 技术栈 | 审计命令 | 说明 |
|--------|---------|------|
| Node.js | `npm audit` | 检查 npm 依赖漏洞 |
| Python | `pip audit` | 检查 Python 依赖漏洞 |
| Go | `govulncheck ./...` | 检查 Go 依赖漏洞 |
| Rust | `cargo audit` | 检查 Rust 依赖漏洞 |
| Java | `mvn dependency-check:check` | OWASP Dependency Check |

### 处理规则

| 漏洞级别 | 处理方式 |
|---------|---------|
| **CRITICAL** | **阻塞发布**，立即修复或更换依赖 |
| **HIGH** | 应在当前任务中修复 |
| **MEDIUM** | 记录到 `techContext.md`，在后续任务中处理 |
| **LOW** | 记录，按优先级排期 |

### 依赖管理最佳实践

- 使用锁文件（`package-lock.json`、`poetry.lock` 等）
- 定期更新依赖（每月至少一次审计）
- 新增依赖前检查：维护状态、下载量、已知漏洞、许可证

## 五、部署安全（在部署相关任务中使用）

### 部署检查清单

- [ ] HTTPS/TLS 配置正确（TLS 1.2+）
- [ ] 安全响应头配置（CSP、HSTS、X-Frame-Options、X-Content-Type-Options）
- [ ] CORS 策略限制为必要的域
- [ ] 调试模式关闭
- [ ] 默认凭证已更改
- [ ] 不必要的端口和服务已关闭
- [ ] 日志不包含敏感数据
- [ ] 环境变量通过安全方式注入（不通过命令行参数）
- [ ] 数据库连接使用最小权限账户
- [ ] 文件上传大小和类型限制

### 环境隔离

- 开发、测试、生产环境使用不同的密钥和配置
- 生产数据不用于开发环境（如需要，使用脱敏数据）
- 生产环境访问权限最小化

## 六、安全测试（融入 TDD 流程）

在编写功能测试的同时，为安全相关功能编写安全测试：

### 安全测试类型

| 类型 | 测试内容 | 示例 |
|------|---------|------|
| **输入验证** | 恶意输入被正确拒绝 | SQL 注入字符串、XSS 载荷、超长输入 |
| **认证** | 未认证请求被拒绝 | 无 Token 访问受保护接口 → 401 |
| **授权** | 越权访问被拒绝 | 用户 A 访问用户 B 的资源 → 403 |
| **速率限制** | 暴力攻击被阻止 | 连续 N 次失败后被限制 |
| **错误处理** | 错误响应不泄露信息 | 500 错误不包含堆栈跟踪 |

### TDD 中的安全测试

安全测试遵循同样的 RED-GREEN-REFACTOR 循环：
1. **RED** — 写安全测试（如：测试 SQL 注入输入被拒绝）
2. **验证 RED** — 确认测试因缺少安全措施而失败
3. **GREEN** — 实现安全措施（如：添加参数化查询）
4. **验证 GREEN** — 确认安全测试通过且功能测试不受影响

## 与 Memory Bank 集成

- **威胁建模产出** → 记录到 `docs/specs/` 的设计文档中
- **安全决策** → 记录到 `memory-bank/systemPatterns.md` 的"安全模式"章节
- **依赖审计结果** → 记录到 `memory-bank/techContext.md`
- **安全相关 Bug** → 在 `memory-bank/progress.md` 中标注 `[安全]` 前缀

## 与其他技能的衔接

- **头脑风暴/设计阶段** → 触发威胁建模（本技能第一章）
- **编写计划** → 为安全需求生成安全任务和安全测试
- **TDD** → 安全测试与功能测试并行编写
- **代码审查** → 使用 OWASP Top 10 检查清单（本技能第二章）
- **完成验证** → 包含依赖安全审计（本技能第四章）

## 红色信号 — 停下

- "这个功能不需要安全考虑"（所有接受输入的功能都需要）
- "先实现功能，之后加安全"（安全必须内建，不是补丁）
- "内部系统不需要认证"（零信任原则）
- "日志里打印一下密钥方便调试"（严禁）
- "用 MD5 哈希密码就够了"（不够，使用 bcrypt/argon2）
