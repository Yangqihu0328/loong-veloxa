---
description: 完成验证 - 在声明工作完成、修复或通过之前使用，需要运行验证命令并确认输出
globs: []
alwaysApply: false
---

# 完成验证

## 概述

在没有验证的情况下声称工作完成是不诚实，而非效率。

**核心原则：** 证据先于断言，始终如此。

## 铁律

```
没有新鲜的验证证据，就不能声称完成
```

如果你在这条消息中没有运行验证命令，你不能声称它通过了。

## 验证门控函数

```
在声称任何状态或表达满意之前：

1. 识别：什么命令能证明这个断言？
2. 运行：执行完整命令（新鲜的、完整的）
3. 阅读：完整输出，检查退出码，计算失败数
4. 验证：输出是否确认了断言？
   - 如果否：陈述实际状态并附证据
   - 如果是：陈述断言并附证据
5. 然后才能：做出声明

跳过任何步骤 = 不是在验证
```

## 常见失败

| 声明 | 需要 | 不充分 |
|------|-----|--------|
| 测试通过 | 测试命令输出：0 失败 | 之前的运行、"应该通过" |
| Linter 干净 | Linter 输出：0 错误 | 部分检查、推断 |
| 构建成功 | 构建命令：exit 0 | Linter 通过、日志看起来不错 |
| Bug 已修复 | 测试原始症状：通过 | 代码改了，假设已修复 |
| 需求满足 | 逐行清单检查 | 测试通过 |

## 红色信号 — 停下

- 使用"应该"、"大概"、"似乎"
- 在验证前表达满意（"好了！"、"完美！"、"搞定！"）
- 准备在没有验证的情况下提交/推送/PR
- 依赖部分验证
- 想"就这一次"

## 合理化借口防护

| 借口 | 现实 |
|------|------|
| "现在应该行了" | 运行验证命令 |
| "我很有信心" | 信心 ≠ 证据 |
| "就这一次" | 没有例外 |
| "Linter 通过了" | Linter ≠ 编译器 |
| "我累了" | 疲惫 ≠ 借口 |

## 关键模式

**测试：**
```
✅ [运行测试命令] [看到：34/34 通过] "所有测试通过"
❌ "现在应该通过了" / "看起来正确"
```

**构建：**
```
✅ [运行构建] [看到：exit 0] "构建通过"
❌ "Linter 通过了"（Linter 不检查编译）
```

**需求：**
```
✅ 重读计划 → 创建清单 → 逐项验证 → 报告差距或完成
❌ "测试通过了，阶段完成"
```

## 与 Memory Bank 集成

验证完成后：
- 更新 `memory-bank/progress.md` — 记录验证结果（通过/失败、具体证据）
- 验证失败时 → 记录失败原因，切换到调试技能或回到 TDD 修复

## 与其他技能的职责划分

三层验证各有侧重：
- **TDD 验证**：单元级别——每个函数/方法的行为正确性
- **代码审查**：规格合规性——是否实现了需求 + 代码质量
- **完成验证（本技能）**：端到端——完整测试套件、构建、Linter、需求清单逐项确认

## 底线

**验证没有捷径。**

运行命令。阅读输出。**然后**才声明结果。

这是不可协商的。
