---
description: Git 工作流 - 分支管理、开发隔离和完成开发分支的标准流程
globs: []
alwaysApply: false
---

# Git 工作流

## 分支策略

### 开始开发前

**永远不要直接在 main/master 分支上开发**（除非用户明确同意或是 Level 1 的微小修复）。

1. 确认当前分支状态：`git status`
2. 确保在最新状态：`git pull`
3. 创建功能分支：`git checkout -b feature/[task-id]-[short-description]`

### 分支命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| 功能 | `feature/TASK-YYYYMMDD-NN-description` | `feature/TASK-20260314-01-user-auth` |
| 修复 | `fix/TASK-YYYYMMDD-NN-description` | `fix/TASK-20260314-02-login-bug` |
| 重构 | `refactor/TASK-YYYYMMDD-NN-description` | `refactor/TASK-20260314-03-cleanup` |

### 提交规范

使用语义化提交消息：

```
<type>(<scope>): <description>

类型：feat, fix, refactor, test, docs, chore
范围：可选，指明影响区域
描述：简洁的变更说明
```

示例：
- `feat(auth): add JWT token validation`
- `fix(api): handle empty response body`
- `test(auth): add login failure test cases`
- `refactor(db): extract connection pool logic`

### commit body Source 溯源 + 实测数据格式（quad-evidence ~39 commits / 已成默认协议）

> **TASK-20260503-03 反思 §6 #4 实证 + 累计 ~39 commits 实测**：commit body 含「Source 溯源」+「实测数据」格式已成 Veloxa 默认协议 / 跨任务可追溯性 + 度量数据汇总的关键基础设施。

#### 强制要求

所有 Level 2+ 任务的 commit body **必填**以下两段：

##### Source 溯源段（4 类）

每个 commit body 末尾**必须**含 `Source:` 段，引用 4 类来源中的至少 1 类：

```markdown
Source:
- spec：docs/specs/2026-MM-DD-feature.md §X.Y
- plan：docs/plans/2026-MM-DD-feature.md §B.N
- archive：memory-bank/archive/archive-TASK-XXXXXXXX-XX.md §X
- reflection：memory-bank/reflection/reflection-TASK-XXXXXXXX-XX.md §X #N
```

**优先级：** spec > plan > reflection > archive（spec 最稳定 / archive 最具体）

##### 实测数据段

涉及度量类任务（性能 / 时长 / 行数 / commit 数 / 决策数）的 commit body **必填**实测数据：

| 度量类型 | 格式范本 |
|---|---|
| **plan ×0.6 时间** | `plan ×0.6 实测系数：~N min vs plan ×0.6 M min = X.YZ× <子档名>` |
| **commit hash** | `commit hash: a1b2c3d`（reflect/archive 阶段引用前序 commit）|
| **行数** | `+M 行 / 既有 N 行 → N+M 行` |
| **决策数 / 协同度** | `跨决策协同度 100% 第 N 次连续命中 / 累计 X/Y` |
| **测试数** | `ctest 期望：DEVTOOL=ON N + DEVTOOL=OFF M` |

#### commit body 完整结构（基础任务 / 非蓝图）

```markdown
<type>(<scope>): <description>

[1-2 段任务概述]

[实施清单 / 子项汇总]

验证：
- ✅ <验证点 1>
- ✅ <验证点 2>

Source:
- <溯源类型>: <文件路径> §<段号>
- ...

实测数据（如适用）：
- plan ×0.6 实测系数: ...
- commit hash: ...

[反复模式 / 跨决策协同度元数据]
```

#### 反模式

- ❌ commit body 仅含 `<description>` 无 Source 溯源（破坏跨任务可追溯）
- ❌ Source 溯源仅写 `TASK-XXXXXXXX-XX` 不指明 `§X.Y`（无法定位上下文）
- ❌ 度量类任务无实测数据段（reflect 阶段无法汇总系数）
- ❌ Source 引用已 archive 的旧任务但未注明 archive 路径（导致引用 broken link）

#### quad-evidence 实证

| # | 任务范围 | 累计 commits | 实证 |
|:-:|---|:-:|---|
| 1 | TASK-20260503-01 ~ 03（DevTool 系列）| ~12 | 首次实证 Source 溯源全覆盖 |
| 2 | TASK-20260503-04 ~ 05（DOM bindings 蓝图）| ~10 | dual-evidence + 实测数据全覆盖 |
| 3 | TASK-20260504-01（MVP-scope V2=a）| ~6 | triple-evidence + V2=a 协同 |
| 4 | TASK-20260505-01 ~ 04（MVP-B + GLES 蓝图 + 工作流元）| ~11+ | **quad-evidence ~39 commits / 已成默认协议** |

#### 交叉引用

- 本规则「蓝图任务 commit body 范本」段（蓝图任务专属 8 段范本 / Source + 实测数据是其中 2 段）
- `.cursor/rules/skills/writing-plans.mdc`「plan/spec docs 落盘即 commit P0 协议」段（plan 阶段 commit body 协议同源）
- `memory-bank/systemPatterns.md`「跨决策协同度 100% doudec-evidence」段（决策矩阵元数据来源）

## 蓝图任务 commit body 范本（V2=a 蓝图任务 / V2=完整蓝图任务**必填**）

> **TASK-20260505-03 commit `1555cf4` 8 段范本首次完整实施 → TASK-20260505-04 plan commit `02dd40c` quad-evidence 候选升级**：蓝图任务 commit body 含 8 段固化范本（任务定位 / 决策矩阵 / 主交付清单 / 后续实施 / 协议元数据 / plan ×0.6 / Source / 下一步），与 P0 协议「plan/spec docs 落盘即 commit」绑定。

### 触发条件

以下任一蓝图任务**必填**本范本：

- Level 4 V2=a 蓝图任务（`/van → /plan → /reflect → /archive` / 跳过 `/build`）
- Level 4 V2=完整蓝图任务（含 build 阶段）的 plan commit
- 工作流元任务的 plan commit（豁免 spec / 沿用范本结构）
- 任何含 ≥ 5 D 决策的 plan commit

### 8 段范本

```markdown
<type>(<scope>): <description> [TASK-XXXXXXXX-XX]

任务定位：
- TASK-XXXXXXXX-XX <任务名>（Level X / V2=a 蓝图 / 工作流变体标注）
- 沿用 <既有范式 / first-evidence 任务> 范式
- 主交付：spec ~N 行 + plan ~M 行 + creative ×K + Memory Bank 三件套同步
- [V2=a 标注：跳过 /build / 主交付 = 文档]

V/B/D 决策矩阵（N 决策 / M 次 AskQuestion all_recommended 锁定）：
- D1=A <决策项 1>: <理由概要>
- D2=B <决策项 2>: <理由概要>
- ...

主交付清单（N 文件 / 协议规则）：
- docs/specs/YYYY-MM-DD-feature-design.md（新建 ~N 行 / Y 段）
- docs/plans/YYYY-MM-DD-feature.md（新建 ~M 行 / Z 段）
- memory-bank/creative/creative-XXX.md ×K（新建 ~K×N 行）
- memory-bank/activeContext.md（更新 +N 行）
- memory-bank/tasks.md（更新 +N 行）
- memory-bank/progress.md（更新 +N 行）

后续实施（Build 阶段 / V2=完整任务填 / V2=a 任务标 N/A）：
- B.1 <子任务 1> [估时]
- B.2 <子任务 2> [估时]
- ...
- 总计 N commits / +M LOC（×1.3-1.5 buffer 后）

协议元数据：
- ✅ plan/spec docs 落盘即 commit P0 协议执行（X 文件单 commit / 0 collateral）
- ✅ 跨决策协同度 100% 第 N 次连续命中（dec → endec → doudec → ...）
- 工作流元任务：豁免独立 spec（沿用 TASK-03-02 范式）/ V2=a 蓝图：跳过 /build

plan ×0.6 实测系数：
- 估时：plan ×0.6 N-M min
- 实测：~K min
- 系数：~X.YZ× <子档名 / 极速区 / 极致极速区 / 标准区>
- 子档归属：<原因>

Source:
- spec / plan / archive / reflection 引用清单
- commit hash 链（如有前序 commit 引用）

下一步：/<reflect|build> — <说明>
```

### 反模式

- ❌ 蓝图任务 commit body 仅写 1-2 段（缺决策矩阵 / 缺 plan ×0.6 实测系数）
- ❌ V2=a 蓝图任务 commit body 不标「跳过 /build」（让 reviewer 误以为还有后续 build）
- ❌ 决策矩阵段仅写决策结果不写「N 决策 / M 次 AskQuestion all_recommended」元数据
- ❌ 协议元数据段缺「plan/spec docs 落盘即 commit」执行确认（破坏 P0 协议自查）

### 与既有规则协同

- 与 `.cursor/rules/skills/writing-plans.mdc`「plan/spec docs 落盘即 commit P0 协议」段绑定（同 8 段定义 / 双向引用）
- 与本规则「commit body Source 溯源 + 实测数据格式」段协同（Source + 实测数据是 8 段中的 2 段）
- 与 `memory-bank/systemPatterns.md`「跨决策协同度 100% doudec-evidence」段协同（决策矩阵元数据格式）

### 实证

- TASK-20260505-03 commit `1555cf4`：8 段范本首次完整实施 / triple-evidence 启动
- TASK-20260505-04 commit `02dd40c`（plan 阶段 dogfood）：8 段范本工作流元任务变体 / quad-evidence 候选

## 工作流产出物提交

工作流命令（`/build`、`/reflect`、`/archive`）产生的 Memory Bank 和文档变更也需要提交，确保 git 历史完整反映工作流全生命周期。

| 场景 | 提交消息格式 |
|------|------------|
| `/build` TDD 循环 | 代码 + MB 更新合并提交（使用常规 `feat`/`fix` 等类型） |
| `/build` 收尾 | `chore(build): finalize [task_id] memory bank state` |
| `/reflect` 产出物 | `docs(reflect): add reflection for [task_id]` |
| `/archive` 产出物 | `docs(archive): add archive for [task_id]` |
| `/archive` 最终重置 | `chore(workflow): complete [task_id] and reset to idle` |
| `/van` 遗留清理 | `chore(workflow): commit residual artifacts from previous task` |

**原则：** 每个工作流阶段结束时，working tree 应为干净状态。

## 完成开发分支

当所有任务完成并通过验证后：

### 1. 最终验证

- [ ] 所有测试通过
- [ ] 没有 linter 错误
- [ ] 代码审查完成
- [ ] Memory Bank 已更新

### 2. 选择完成方式

向用户展示选项：

| 选项 | 适用场景 | 操作 |
|------|---------|------|
| **合并到主分支** | 个人项目、确认可合并 | `git checkout main && git merge --no-ff feature/xxx` |
| **创建 PR** | 团队项目、需要审查 | `git push -u origin HEAD` 然后创建 PR |
| **保留分支** | 还需要更多工作 | 只 push 到远程 |
| **放弃分支** | 方案被否决 | `git checkout main && git branch -D feature/xxx` |

### 3. 执行用户选择

根据用户选择执行对应操作，每步都需要验证。

## 提交频率

- **每完成一个计划任务后提交**（一个任务可能包含多个 TDD 循环）
- 在提交前完成代码审查（`code-review.mdc`）
- 不要积攒大量变更后一次性提交
- 提交前运行完整测试套件确认通过

**Level 4 子系统粒度提交（强制）：**
- Level 4 任务**必须**至少按子系统分别提交，禁止将所有子系统合并为单次原子大提交
- 每个子系统完成并通过测试后独立提交，便于 code review 逐子系统审查和 `git bisect` 精确定位回归
- 提交消息应标注子系统编号和名称，例如 `feat(error-codes): add AEGIS-xxxx error code system (#1/5)`
- 如果子系统之间存在强依赖，可在依赖方提交中合并少量基础设施变更，但应在提交消息中说明

**提交流程：** TDD 循环（可多次）→ 代码审查 → 测试全部通过 → 提交

## Multi-subtask commit 拆分 — `git add -p` 推荐范式（多子任务连续完成时**必做**）

> 反复出现 P1（TASK-20260502-01 A.2.1/A.2.4/A.3.1 实证）：3 个连续子任务在同一文件 `tests/CMakeLists.txt` 累积变更，因 `git add tests/CMakeLists.txt` 而非 `git add -p` 导致 A.2.1 commit 隐含包含 A.2.4 + A.3.1 部分变更（功能正确，语义不洁）→ git bisect 定位回归时无法精确到子任务级。

**触发条件**：以下任一**必做**：

- 同一 plan 内 ≥ 2 个子任务修改同一文件（如 `CMakeLists.txt` / `Cargo.toml` / `package.json`）
- 子任务连续完成但中间未 commit（累积 working tree 含多任务变更）
- 大型重构 + 跨任务的同文件 hunk

**强制操作模板**：

```bash
# Step 1: working tree 状态自检
git status --short                            # 期望：仅本子任务相关文件
git diff <file>                                # 视觉确认 hunks

# Step 2: 选择性 stage（关键 — 拒绝整文件 git add）
git add -p <file>                              # 交互式选择 hunks
# 或直接路径过滤：git add path/to/specific/sub/file.cc

# Step 3: 验证 staged 内容
git diff --cached                              # 期望：仅含本子任务变更
git diff                                       # 期望：剩余 hunks 留给下一子任务

# Step 4: commit + 验证
git commit -m "..."                            # 含子任务 ID
git status --short                             # 期望：剩余 hunks 仍 unstaged

# Step 5: 下一子任务继续
# 重复 Step 2-4
```

**checklist（每次 commit 前必查）**：

- [ ] `git diff --cached` 仅含本子任务相关 hunks（无其他子任务的 stray hunk）
- [ ] commit 消息明确标注子任务 ID（`feat(scope): xxx (#N/M)` 或 `(C-#N)` 等）
- [ ] commit 后 `git status` 显示剩余 hunks unstaged 等待下一子任务
- [ ] 如必须把多子任务 hunk 合并到一个 commit → commit 消息明确说明范围（"includes A.2.1 + A.2.4 due to circular CMake dep"）

**反模式**：

- ❌ `git add <file>` 整文件 stage — 隐含累积所有子任务变更
- ❌ commit 消息只写最后一个子任务 ID — 实际 hunk 跨多任务
- ❌ 用 `git stash push --keep-index` 隔离再 commit — 引入额外操作易出错（`git add -p` 是首选）

**实证**：

- TASK-20260502-01 A.2.1 commit `feat(devtool): add inspector_panel CMake target` 实际隐含 A.2.4 + A.3.1 部分 `tests/CMakeLists.txt` 变更 → reflect 阶段 §6 P1 #4 标 P1 改进
- 反例：TASK-20260503-01 11 个子任务 11 commits 均严格 1 commit/子任务 — 因子任务文件无重叠，未触发本场景

**交叉引用**：

- `memory-bank/systemPatterns.md`「commit 颗粒度」段
- `.cursor/rules/skills/git-workflow.mdc` 本文件「Level 适配」段（Level 4 强制按子系统提交，本规则是 Level 2-4 通用前置）

## Commit 前防御断言（multi-session safety）

**适用场景**：
- Background agent 模式（用户主线 + agent 后台双轨并行）
- 用户/工具可能在另一会话切换分支
- 长任务（> 30 min）的多 commit 流程

**防御协议**（每次 `git commit` 前必跑）：

```bash
# 1. 分支身份断言（防止 commit 误落到错误分支）
[ "$(git symbolic-ref --short HEAD)" = "feature/TASK-<id>-<slug>" ] \
  || { echo "WARN: NOT on expected branch"; exit 1; }

# 2. working tree 状态自检（防止 race condition reset 改动后误以为 commit 成功）
git status --short  # 应显示预期的 modified/new 文件

# 3. 然后才执行 commit
git add <files> && git commit -m "..."
```

**冲突诊断信号**（任一触发 → 立刻跑 reflog 诊断）：
- `git status` 显示 clean 但你刚做了 StrReplace
- `git commit` 报 "nothing to commit, working tree clean"
- 当前分支与预期不符（`git symbolic-ref` 输出意外）
- 文件内容回退到改动前

**reflog 诊断标准命令**（systematic-debugging skill 联动）：
```bash
git reflog --date=iso | head -20
# 找一秒级时间戳的 checkout / branch switch — 暴露并发会话冲突源
```

**worktree 隔离方案**（确认冲突后）：
```bash
git worktree add .worktree-<task_short>/ feature/TASK-<id>-<slug>
cd .worktree-<task_short>
# 在 worktree 内继续工作，主 worktree 切换分支不影响这里
```

**实证**：TASK-20260430-03 R2 阶段首次实战暴露并发会话切分支 race condition（reflog 显示 23:41:23/30 + 23:45:08 两次双切，R2.1 commit 混入外部 working tree 状态 + R2.3 改动被静默丢失）。本协议是该任务 reflection §5 P0 改进建议落实，已纳入 systemPatterns / techContext 长期沉淀。

## 与 Memory Bank 集成

- 创建分支后 → 更新 `memory-bank/activeContext.md` 记录当前分支名
- 每次提交后 → 更新 `memory-bank/progress.md` 记录提交信息
- 完成分支操作后 → 更新 `memory-bank/tasks.md` 记录分支状态

## 合并冲突处理

如果在合并时遇到冲突：
1. 列出所有冲突文件
2. 逐文件分析冲突内容
3. 与用户讨论每个冲突的解决方案
4. 解决冲突后运行完整测试套件
5. 确认所有测试通过后完成合并

## 非 Git 项目处理

如果项目不是 Git 仓库：
- 在 `/van` 阶段询问用户是否需要 `git init`
- 如果用户选择不使用 Git → 跳过所有 Git 相关步骤，在 `activeContext.md` 中记录"无 Git"
- 跳过提交步骤，但仍执行 TDD 和代码审查

## Level 适配

| 级别 | 分支策略 |
|------|---------|
| Level 1 | 征求用户明确同意后，可以在当前分支直接修复 |
| Level 2 | 创建功能分支 |
| Level 3 | 创建功能分支，可能需要多次提交 |
| Level 4 | 创建功能分支，**按子系统分提交**（强制），每个子系统独立 commit 便于 bisect |
