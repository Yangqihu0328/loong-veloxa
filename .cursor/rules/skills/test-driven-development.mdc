---
description: 测试驱动开发 (TDD) - 实现任何功能或修复 Bug 时使用，在编写实现代码之前
globs: []
alwaysApply: false
---

# 测试驱动开发 (TDD)

## 概述

先写测试。看它失败。写最少的代码使其通过。

**核心原则：** 如果你没有看到测试失败，你就不知道它是否测试了正确的东西。

## 适用场景

**始终使用：**
- 新功能
- Bug 修复
- 重构
- 行为变更

**例外（需征求用户意见）：**
- 一次性原型
- 生成的代码
- 配置文件
- 覆盖补充模式（见下方）— 功能已实现但缺少测试时

## 覆盖补充模式

当功能已实现但缺少测试时，严格 TDD 的 RED 阶段不适用（测试会立即通过）。此时使用覆盖补充模式：

1. **编写测试** — 覆盖已实现功能的正常行为
2. **验证通过** — 测试应当立即通过（因为实现已存在）
3. **验证有效性** — 临时注入缺陷确认测试能捕获。**这一步替代 TDD 中的 RED 阶段**，证明测试确实在检测正确的行为
4. **补充边界** — 添加边界条件和异常路径测试
5. **恢复并提交** — 恢复实现代码，确认全部通过

**何时使用：**
- 回顾改进中为已有功能补充测试
- 代码审查后补充缺失的测试覆盖
- 遗留代码增加安全网

**何时不使用（仍应严格 TDD）：**
- 新功能开发
- Bug 修复（应先写复现测试）
- 行为变更（应先写期望行为测试）

计划中使用 `[覆盖补充]` 标记此模式的任务，详见 `writing-plans.mdc`。

## 铁律

```
没有失败的测试，就不能写生产代码
```

在测试之前写了代码？**删除它。** 重新开始。

- 不要保留作为"参考"
- 不要在写测试时"改编"它
- 不要看着它写
- 删除就是删除

从测试出发，全新实现。

## 红-绿-重构循环

### RED — 编写失败测试

写一个最小的测试，展示期望的行为。

**要求：**
- 一个行为
- 清晰的名称
- 真实代码（除非不可避免，否则不用 mock）

### 验证 RED — 看它失败

**必须执行，不可跳过。**

确认：
- 测试失败（不是报错）
- 失败消息符合预期
- 因为功能缺失而失败（不是拼写错误）

测试通过了？你在测试已有行为。修复测试。

### GREEN — 最少代码

写最简单的代码使测试通过。

不要添加功能、重构其他代码或超出测试范围的"改进"。

### 验证 GREEN — 看它通过

**必须执行。**

确认：
- 测试通过
- 其他测试仍然通过
- 输出干净（无错误、警告）

测试失败？修复代码，不是测试。

### REFACTOR — 清理

仅在 GREEN 之后：
- 消除重复
- 改善命名
- 提取辅助函数

保持测试绿色。不添加行为。

### 重复

下一个失败测试，下一个功能。

## 好测试的标准

| 质量 | 好 | 坏 |
|------|---|---|
| **最小化** | 一件事。名称中有"和"？拆分它 | `test('验证邮箱和域名和空格')` |
| **清晰** | 名称描述行为 | `test('test1')` |
| **展示意图** | 演示期望的 API | 遮蔽代码应该做什么 |

## 为什么顺序重要

**"我先写代码，之后加测试验证"**

之后写的测试立即通过。立即通过什么都证明不了：
- 可能测试了错误的东西
- 可能测试了实现而非行为
- 可能遗漏了你忘记的边界情况
- 你从未看到它捕获 bug

先写测试迫使你看到测试失败，证明它确实在测试某些东西。

## 常见合理化借口

| 借口 | 现实 |
|------|------|
| "太简单不需要测试" | 简单代码也会出错。测试只需 30 秒 |
| "之后再测试" | 立即通过的测试什么都证明不了 |
| "紧急情况没时间" | TDD 比猜测调试更快 |
| "先探索一下" | 可以。但丢弃探索代码，用 TDD 重新开始 |
| "测试很难写 = 设计不清楚" | 难以测试 = 难以使用。倾听测试的信号 |
| "已有代码没测试" | 你在改进它。为已有代码添加测试 |

## 红色信号 — 停下并重新开始

- 在测试之前写了代码
- 实现之后写测试
- 测试立即通过（**例外：覆盖补充模式下这是预期行为**）
- 无法解释测试为什么失败
- 合理化"就这一次"

**以上所有意味着：删除代码。用 TDD 重新开始。**
（覆盖补充模式除外——当任务在计划中明确标记为 `[覆盖补充]` 时，测试立即通过是正常的，但必须通过缺陷注入验证有效性。）

## Bug 修复示例

**Bug:** 空邮箱被接受

**RED**
```
test('拒绝空邮箱', async () => {
  const result = await submitForm({ email: '' });
  expect(result.error).toBe('邮箱必填');
});
```

**验证 RED:** 测试失败，`expected '邮箱必填', got undefined`

**GREEN**
```
function submitForm(data) {
  if (!data.email?.trim()) {
    return { error: '邮箱必填' };
  }
  // ...
}
```

**验证 GREEN:** 测试通过

**REFACTOR:** 如需要，提取多字段验证。

## 与 Memory Bank 集成

每完成一个 TDD 循环后：
- 更新 `memory-bank/progress.md` — 记录完成的功能点和测试结果
- 如果遇到意外的测试失败且无法快速理解原因 → 切换到**系统化调试技能**

## 与其他技能的衔接

- **测试意外失败、无法理解原因** → 切换到 `systematic-debugging.mdc`
- **TDD 循环完成后** → 执行代码审查（`code-review.mdc`），然后提交（`git-workflow.mdc`）
- **所有任务完成后** → 执行完成验证（`verification.mdc`）

## 编码前检查清单

在编写任何实现代码之前：

- [ ] **Grep 确认枚举/API 命名** — 引用项目中的枚举值（如 `ErrorCode::*`、`Role::*`、`Feature::*`）、
  工具函数（如 `safeColumnText`）、类型名之前，先用 Grep 确认准确名称。
  不要凭记忆使用 — 已多次因此产生编译错误（反复模式 9+ 次）。

## 完成验证清单

在标记工作完成之前：

- [ ] 每个新函数/方法都有测试
- [ ] 看到每个测试失败后再实现
- [ ] 每个测试因预期原因失败
- [ ] 写了最少代码使测试通过
- [ ] 所有测试通过
- [ ] 输出干净
- [ ] 测试使用真实代码
- [ ] 覆盖了边界情况和错误情况
