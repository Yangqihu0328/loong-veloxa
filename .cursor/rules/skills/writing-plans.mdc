---
description: 编写计划 - 当有需求规格需要实现时使用，在写代码之前
globs: []
alwaysApply: false
---

# 编写实现计划

## 概述

编写全面的实现计划，假设执行者对代码库零上下文且品味可疑。记录他们需要知道的一切：每个任务要修改哪些文件、代码、测试、可能需要查看的文档、如何测试。把整个计划作为小块任务给出。

**原则：** DRY、YAGNI、TDD、频繁提交。

**保存位置：** `docs/plans/YYYY-MM-DD-[feature-name].md`

## plan/spec docs 落盘即 commit P0 协议（plan 阶段产出物 commit 协议）

> **TASK-20260505-01 提议 → TASK-20260505-02 部分实施 → TASK-20260505-03 首次完整实施 → triple-evidence 已达固化阈值（P0 立即固化）**
> **TASK-20260505-04 自吃狗粮 → quad-evidence 已固化**
> **TASK-20260505-05 实施类 Level 2 自吃狗粮 → quint-evidence 已固化（实施类任务首次实证 / 5 数据点累计）**
> **TASK-20260505-06 实施类 Level 3 自吃狗粮 → sext-evidence 已固化（实施类 Level 3 首次实证 / 6 数据点累计 / 适用性矩阵 6 类全覆盖 ✅）**

### 触发条件

`plan` 阶段产出物**必须单 commit 落盘**：

- 涉及 spec 文档创建或修改（蓝图任务 / 实施类任务）
- 涉及 plan 文档创建（所有 Level 2+ 任务）
- 涉及 creative ×N 文档创建（V2=a 蓝图 / 蓝图 V2=完整变体）
- 涉及 Memory Bank 三件套同步更新（`activeContext.md` + `tasks.md` + `progress.md`）

### 强制操作

1. **plan + spec + creative ×N + Memory Bank 单 commit 原子落盘** — 全部 plan 阶段产出物作为单 atomic commit / 0 collateral commit
2. **build 阶段（如有）零 collateral commit** — 不得有「补 plan 段」/「补 creative 文档」/「补 spec 段」类碎片 commit
3. **commit body 含完整 8 段范本**（详见下方 §commit body 8 段范本）— 与 `git-workflow.mdc`「蓝图任务 commit body 范本」段对齐
4. **工作流元任务豁免 spec 子项** — 工作流元任务（如 [TASK-20260503-02](../../memory-bank/archive/archive-TASK-20260503-02.md) + TASK-20260505-04）无独立 spec / 仅 plan + Memory Bank 单 commit / `D7=B 仅 plan` 决策标准化

### commit body 8 段范本

| # | 段名 | 内容 |
|:-:|---|---|
| 1 | 任务定位 | 任务 ID + 复杂度 + 工作流变体（V2=a 蓝图 / V2=完整 / 工作流元任务）+ 主交付概要 + 不含 build 标注（如 `[V2=a 蓝图任务 / 跳过 /build]`）|
| 2 | 决策矩阵 | V/B/D 决策矩阵完整列表（# / 决策项 / 选择 / 理由概要）|
| 3 | 主交付清单 | spec / plan / creative 文档 + Memory Bank 文件 + 行数 |
| 4 | 后续实施 | 实施子任务清单 + plan ×0.6 估时 + ×1.3-1.5 buffer（如适用）|
| 5 | 协议元数据 | 「plan/spec docs 落盘即 commit P0 协议」执行状态（哪些文件单 commit / 0 collateral 验证）|
| 6 | plan ×0.6 实测系数 | 实测时间 vs plan ×0.6 估时 + 系数 + 子档归属（极速区 / 极致极速区 / 标准区）|
| 7 | Source 溯源 | 双源：上游 spec / archive / reflection §X 引用 + commit hash（沿用 `git-workflow.mdc` Source 溯源协议）|
| 8 | 下一步 | `/reflect`（蓝图 V2=a / 工作流元任务跳过 build）/ `/build`（蓝图 V2=完整 / 实施类任务）|

### 反模式

- ❌ 「先 commit plan，等 build 阶段补 spec」分两 commit（破坏原子性 / TASK-05-02 实测 collateral commit `feec51b` ⚠️ 自查发现）
- ❌ 「creative 文档放到 build 阶段第 1 commit」（违反「plan 阶段产出」边界 / TASK-04-30-04 历史 反模式）
- ❌ 「Memory Bank 三件套等 build 完再更新」（导致 reflect 阶段 MB drift）
- ❌ 工作流元任务强制独立 spec（沿用 TASK-03-02 范式 / 工作流元任务豁免）
- ❌ commit body 缺 Source 溯源 / 缺 plan ×0.6 实测数据（破坏跨任务可追溯性）

### triple-evidence → quad-evidence → quint-evidence → sext-evidence 实证表

| # | 任务 | 任务类型 | 实施程度 | commit | 备注 |
|:-:|---|---|---|---|---|
| 1 | TASK-20260505-01 | 实施类 Level 3 | 提议 P1 #6 改进建议 | 反思阶段提案 | 提出协议构想 |
| 2 | TASK-20260505-02 | 实施类 Level 2 | 部分实施（plan + spec + MB 1 commit / build 阶段 1 collateral commit `feec51b` ⚠️）| `4feda52` | 协议雏形落地 |
| 3 | TASK-20260505-03 | V2=a 蓝图 Level 4 | **首次完整实施 ✅**（plan + spec + creative ×3 + MB ×3 = 8 files 单 commit / 0 collateral）| `1555cf4` | triple-evidence 达固化阈值 |
| 4 | TASK-20260505-04 | 工作流元任务 Level 2-3 | **自吃狗粮 ✅ + reflect 验证**（plan + MB 单 commit / 工作流元任务豁免 spec / 9 commits 全部按 plan）| `02dd40c` + `7160c38` finalize | **quad-evidence 已固化 / P0 协议成熟期** |
| 5 | TASK-20260505-05 | 实施类 Level 2 | **自吃狗粮 ✅ + 实施类首次实证**（plan + MB ×3 单 commit / 实施类任务首次完整实施 / 4 commits 全部按 plan）| `41ef50a` + `63208ac` finalize | **quint-evidence 已固化 / 适用性矩阵全覆盖** |
| **6** | **TASK-20260505-06** ⭐ | **实施类 Level 3** | **自吃狗粮 ✅ + 实施类 Level 3 首次实证**（plan + MB ×3 单 commit / 实施类 Level 3 任务首次完整实施 / 4 commits 全部按 plan）| **`39d2981` + `5bc70a0` finalize** | **sext-evidence 已固化 / 适用性矩阵 6 类全覆盖 ✅** |

### 适用性矩阵（sext-evidence 已确立 / 6 类全覆盖 ✅）

| 任务类型 | 适用 | 实证 | 备注 |
|---|:-:|---|---|
| V2=a 蓝图任务 Level 4 | ✅ 强适用 | TASK-05-03 | 8 files 单 commit / triple-evidence base |
| 工作流元任务 Level 2-3 | ✅ 强适用 | TASK-05-04 | 4 files 单 commit / 工作流元任务豁免 spec |
| 实施类任务 Level 2 | ✅ 适用 | TASK-05-05 | 4 files 单 commit / D6=A 仅 plan / 引用上游 spec |
| **实施类任务 Level 3** | ✅ **适用（sext-evidence 新覆盖）** | **TASK-05-06** | **4 files 单 commit / D8=A 仅 plan / 引用 GLES 蓝图 plan §3.2** |
| 实施类任务 Level 1 | ⚠️ 半适用 | TASK-05-01 | plan 可能为空 / 跳过 P0 协议 |
| Level 4 实施类（多 Phase）| ⚠️ 部分适用 | TASK-05-02 | plan + MB 单 commit 可行 / build 各 Phase 单独 commit |

### 与 brainstorm/decision matrix 配合

- **brainstorm 阶段** D 决策矩阵中如涉及「commit 拆分粒度」`D1` + 「P0 协议自吃狗粮」`D8` → VAN 推荐默认指向「自吃狗粮 + 文件聚合 commit」候选（与既有 quad-evidence 协同 ✅）
- **plan 阶段** 落盘 commit 直接执行本协议 / 不再讨论 / 反模式抑制

### 交叉引用

- [`memory-bank/systemPatterns.md`](../../memory-bank/systemPatterns.md) 「跨决策协同度 100% doudec-evidence」段（决策矩阵协同范式）
- [`memory-bank/systemPatterns.md`](../../memory-bank/systemPatterns.md) 「V2=a 蓝图任务范式 triple-evidence」段（V2=a 范式协议）
- [`.cursor/rules/skills/git-workflow.mdc`](./git-workflow.mdc) 「蓝图任务 commit body 范本」段（commit body 8 段范本同源）
- [`.cursor/rules/skills/git-workflow.mdc`](./git-workflow.mdc) 「commit body Source 溯源 + 实测数据格式」段（Source 溯源 + plan ×0.6 数据协议）

## 范围检查

如果规格覆盖了多个独立子系统，应该在头脑风暴阶段就分解为子项目规格。如果没有，建议分为多个计划——每个子系统一个。每个计划应产出独立可工作、可测试的软件。

## 文件结构

在定义任务之前，列出将创建或修改的文件及每个文件的职责。

- 设计边界清晰、接口定义良好的单元
- 每个文件应有一个清晰的职责
- 偏好更小、聚焦的文件而非做太多事的大文件
- 一起变更的文件应该放在一起
- 在现有代码库中遵循已有模式

## 公开 API testability 检查清单（涉及新增公开 C ABI / public API 时**必填**）

> 反复出现升级 P1（TASK-20260503-01 C-#1 实证）：C.4.2 dogfood smoke 需要 `vx_view_hot_reload_tracked_count` 才能验证「count=1」契约，plan 阶段未识别该 testability 接口需求 → build §C.5.2 finalize 才补加 → 1 行 ABI 加 1 commit 但破坏 plan 原子性。

**触发条件**：本次 plan 涉及以下任一**必填**：

- 新增公开 C ABI 函数（`vx_*` / `VX_*` 前缀，对外可见）
- 新增公开 C++ class / namespace API（`veloxa::*` 头部 export）
- 扩展既有公开 API 行为（新增参数 / 新增返回字段 / 新增错误码）

**强制产出 testability 接口需求清单**：

| 维度 | 问题 | 接口需求 | 落实子任务 |
|---|---|---|---|
| **smoke 可观测** | 该 API 的副作用可被 `examples/<name>` smoke 测试观测吗？ | 如 hot_reload 触发计数 → `vx_view_hot_reload_tracked_count()` getter | 写 ctest smoke 时同步加 |
| **dogfood 可达** | 该 API 的状态可被 DevTool / inspector / debugger 读取吗？ | 如 hot_reload status → `vx_devtool_get_hot_reload_status()` JS binding | DevTool UI 子任务前置 |
| **observability 可断言** | 该 API 的内部状态可被 ctest / valgrind / log assert 验证吗？ | 如 last_error 字段 → 单测 `EXPECT_TRUE(mgr.last_error().has_value())` | unit test 子任务覆盖 |

**判读规则**：

- 任一维度回答「否」→ plan 必须补「testability 子任务」前置（getter / binding / log accessor 暴露）
- 三维度均「是」→ plan 标注「testability OK」并在验收要点段重复 ✅

**反模式**：

- ❌ plan 仅写「实现 API X」未列 testability 清单 → build 阶段 dogfood smoke 才发现缺 getter
- ❌ 把 internal-only `friend class XxxTest;` 混淆为公开 API testability — 内部 friend 不暴露给 example/dogfood/observability 工具
- ❌ 假设「下一个任务再加」— testability 接口与 API 同子任务设计才能保证 commit 原子性

**TASK-20260503-01 实证**：plan §C.4.1 设计 `vx_view_attach_devtool` + `hot_reload_dir` 参数，未识别 dogfood smoke 需要「reload 触发次数 getter」→ §C.4.2 才追加 `vx_view_hot_reload_tracked_count()` 1 行 ABI + 1 commit；如 plan 阶段三问 testability 清单则可前置到 C.4.1 同 commit。

**交叉引用**：`memory-bank/systemPatterns.md`「lazy-attach C ABI 容错模式」段（同源 C ABI 设计模式）；`.cursor/rules/skills/test-driven-development.mdc`（testability 是 TDD 前置条件）。

## C ABI 设计模式 — lazy-attach 默认契约（涉及新增公开 C ABI 函数时**必填**）

> **quad-evidence 已成 Veloxa 公开 C ABI 默认范式**（TASK-20260502-02 first → 03-01 dual → 03-04 triple → 05-02 quad → 已固化为「全 C ABI 默认行为」）。新增 C ABI 函数时，依赖 sub-system 的 lazy-attach 容错处理是默认契约 / 不再单独决策。

### 触发条件

新增公开 C ABI 函数（`vx_*` / `VX_*` 前缀）/ 该函数依赖以下任一 sub-system：

- `Application::update_manager_`（pipeline hooks / invalidate / 帧调度）
- `Application::script_engine_`（QuickJS context / DOM bindings）
- `Application::devtool_`（Inspector / Performance overlay / Console）
- `Application::hot_reload_manager_`（CSS/HTML 热重载）
- 任何在 `Application` 构造时**可能不被初始化**的可选子系统

### 强制 lazy-attach 契约

| 维度 | 要求 |
|---|---|
| **行为契约** | 当依赖 sub-system 未初始化时，C ABI 函数**返回 `VX_ERROR_INVALID_STATE`** 而非 crash / `assert` / `abort` |
| **副作用契约** | 返回 `VX_ERROR_INVALID_STATE` 时**不得**有任何可观测副作用（不写日志 / 不分配内存 / 不触发回调）— 调用方可安全重试 |
| **顺序契约** | C ABI 函数**不得**假设调用顺序（如「必须先调 attach 再调 set」）— 调用方可任意顺序调用 / 内部 lazy 处理 |
| **idempotency** | 重复调用必须 idempotent（如 `attach` 后再 `attach` 不报错 / `detach` 后再 `detach` 不报错）|

### plan 阶段强制清单（每新增 C ABI 函数填）

```markdown
| C ABI 函数 | 依赖 sub-system | 未初始化行为 | 重复调用行为 |
|---|---|---|---|
| vx_view_xxx | update_manager_ | VX_ERROR_INVALID_STATE | idempotent |
```

### 反模式

- ❌ `assert(update_manager_ != nullptr)` 在 C ABI 内部（导致 release 模式 abort）
- ❌ 假设「调用方保证已 attach」（违反 lazy-attach 默认契约）
- ❌ C ABI 内部 `if (!update_manager_) LOG_ERROR(...)`（破坏副作用契约）
- ❌ plan 写「`vx_view_xxx` 调用 `update_manager_->Y`」未声明依赖未初始化时行为

### quad-evidence 实证表

| # | 任务 | C ABI 函数 | 依赖 | 行为 |
|:-:|---|---|---|---|
| 1 | TASK-20260502-02 | `vx_view_set_pipeline_hooks` | `update_manager_` | INVALID_STATE / idempotent ✅ |
| 2 | TASK-20260503-01 | `vx_view_attach_devtool` | `devtool_` + `hot_reload_manager_` | INVALID_STATE + WARNING_HOT_RELOAD_FAILED 双语义 ✅ |
| 3 | TASK-20260503-04 | `vx_devtool_get_console_output` | `devtool_->console_panel_` | INVALID_STATE / 0 副作用 ✅ |
| 4 | TASK-20260505-02 | `vx_view_invalidate` | `update_manager_` | INVALID_STATE / idempotent ✅ |

### 与 veloxa_api.h 头部 doc 的协同

- `veloxa/api/veloxa_api.h` 顶部 `/* */` 项目级文档块**必须含**「lazy-attach contract」节统一引用前序 ABI（参考 TASK-20260505-04 落地的头部 doc 段）— 用户首次打开头文件即见 lazy-attach 默认契约
- C ABI 实现文件（`veloxa/api/veloxa_api.cc`）每个相关函数顶部 doc 注释必须显式标注 `// lazy-attach: VX_ERROR_INVALID_STATE if XXX uninitialized`

### 交叉引用

- [`memory-bank/systemPatterns.md`](../../memory-bank/systemPatterns.md)「lazy-attach C ABI 容错模式 quad-evidence」段（同源 quad-evidence 详细实证）
- [`veloxa/api/veloxa_api.h`](../../veloxa/api/veloxa_api.h) 顶部 doc 段（lazy-attach contract 范本）
- 本规则「公开 API testability 检查清单」段（lazy-attach 是 testability 前置 — 单测必须覆盖 INVALID_STATE 分支）

## CMake 链接方向约束分析（涉及 C/C++ 编译单元时**必填**）

> 反复出现（TASK-20260414-01、TASK-20260418-01 至少 2 次）：下游目标需要上游的"间接"依赖时，若上游是 PRIVATE 链接，下游编译/链接会失败。

在任务分解**之前**，回答以下问题：

1. **依赖图**：本次新增/修改的库目标（如 `vx_text`）和下游目标（含测试 `xxx_test`、可执行文件）各自链接了哪些上游？列出 `target_link_libraries` 方向。
2. **头文件暴露**：每个下游目标的源文件是否会 **直接** `#include <上游依赖的头>`？（例如 `font_manager_test.cc` include `<ft2build.h>` → 需要 FreeType 头）
3. **符号依赖**：每个下游目标是否会 **间接** 依赖上游依赖暴露的符号？（例如 `font_manager_test` 调用 `FontManager::GetHbFont`，其实现在 `vx_text` 内部调用了 `hb_ft_font_create_referenced` → 静态库不传递，需要 PUBLIC 或下游直接链）
4. **当前链接方向**：列出问题 2/3 中依赖在提供方的 `PUBLIC`/`PRIVATE`/`INTERFACE` 状态。
5. **结论**：是否需要把提供方的某些依赖从 `PRIVATE` 升 `PUBLIC`？还是下游自己补链（一般不推荐，除非语义上真正独立）？
6. **禁止项**：不要在多个 `CMakeLists.txt` 中重复调用同一 `pkg_check_modules(FOO ...)`——`FindPkgConfig` 对重入敏感，会报 `Unknown arguments specified`。

如果上述分析得出"改 CMakeLists.txt"结论，把 CMake 变更作为独立 Phase 前置任务（而非后补），列出具体的 `target_link_libraries(X PUBLIC/PRIVATE …)` 修改行。

## 静态库循环依赖审计（涉及"已有库 A 提供新符号给已 link 它的 B"时**必填**）

> 反复出现（TASK-20260414-01、TASK-20260419-01）：当 `A` 已 `target_link_libraries(... B)`，本次又让 `A` 暴露符号给 `B` 调用，linker 会报 `undefined reference`。CMake 对静态库循环可处理，但需要双向 link。

在任务分解**之前**回答：

1. **新符号归属**：本次新增的导出符号（函数/类）属于哪个静态库？例：`vx::css::EnumValueToCssString` 属于 `vx_core`。
2. **新符号被谁调用**：列出本次将要 `#include` 该符号头文件并调用它的 .cc 所属目标。例：`dom_bindings.cc` 属于 `vx_script`。
3. **当前 link 拓扑**：grep 现有 `target_link_libraries`，确认 #1 与 #2 之间的 link 方向。
   - 若 `B → A`（B 已链 A）：无需新增，已通。
   - 若 `A → B`（A 已链 B）且 #2 为 B：**循环！必须新增 `target_link_libraries(B PRIVATE A)`**，让 linker 双向扫描。
   - 若两者无 link 关系：决定从哪边新增 link（一般下游加上游 PRIVATE）。
4. **结论**：把 CMake 变更作为该 Phase 的步骤 1（不是 RED 之后），避免实现写完才发现链接失败。

## Web 标准 API 多重载形态清单（接入 WHATWG/DOM/Fetch/MDN 标准 API 时**必填**）

> 反复出现（TASK-20260414-01 addEventListener、TASK-20260419-01 removeEventListener）：标准 Web API 通常有多种 overload 形态（0/1/2/3 args，参数可选，意义随参数数量变化），头脑风暴只设计单一形态会在实施时返工。

在任务分解**之前**列出表格：

| 调用形态 | 参数语义 | C++ 路径 | 边界/错误 |
|---------|---------|---------|---------|
| `api()` | 无参兜底 | ... | ... |
| `api(arg1)` | 单参（按 type 批量？按 key 单查？） | ... | ... |
| `api(arg1, arg2)` | 主用法 | ... | 未匹配如何处理？ |
| `api(arg1, arg2, options)` | 含可选 options | ... | 未知 options key 是否报错？ |

每行对应 1 个测试。表格直接复制到对应 Phase 的"任务步骤 1"。

参考 spec 来源（按优先级）：
- WHATWG Living Standard（HTML/DOM/Fetch）
- W3C Recommendation
- MDN Web Docs（实现行为）
- 主流引擎源码（Gecko / WebKit / Blink）确认实际行为

## FetchContent C 子项目编译选项审计（涉及 FetchContent + 第三方源码时**必填**）

> 反复出现（TASK-20260413-01）：根 `add_compile_options` 的严格告警标志会污染上游 C 源，触发 `-Werror=pedantic` 等失败。

在任务分解**之前**回答：

1. **第三方代码语言**：本次 `FetchContent_Declare` 拉取的代码是否包含 C 源（QuickJS、libuv、zlib 等）？
2. **全局选项作用域**：根 `CMakeLists.txt` 的 `add_compile_options(...)` 是否对 C 源生效？特别检查 `-Werror`、`-Wpedantic`、`-Werror=pedantic` 等。
3. **冲突预测**：第 1+2 都为是 → 计划必须包含**前置 Phase**，将严格告警限制为 C++：
   - 推荐：`add_compile_options("$<$<COMPILE_LANGUAGE:CXX>:-Werror>")` 等
   - 备选：对自有目标用 `target_compile_options(my_target PRIVATE -Werror)`，避免全局注入
4. **验证方式**：在前置 Phase 末尾 `cmake -B build && cmake --build build --target <第三方目标>` 验证无新告警。

如本节得出「需要改根 CMakeLists」结论，把 CMake 变更作为独立 Phase 前置任务。

## FetchContent 网络代理守卫（涉及 FetchContent 且未离线 `_deps` 的任务**必填**）

> 反复出现 9+ 次（TASK-20260413-01 / TASK-20260419-02 / -04 / 反思反复识别）：Cursor 沙箱 / WSL 环境首次 `cmake -B build` 触发 FetchContent 因 git 全局代理未设而超时挂死；单次 5-10 min 恢复，累计 ≥ 1h。

在任务分解**之前**回答：

1. **是否触发 FetchContent 拉取**（命中任一即触发本段守卫）：
   - 根或子 `CMakeLists.txt` 已存在 `FetchContent_Declare`（grep 验证）
   - **且** 以下任一：
     - `build/` 或 `build-bench/` 目录不存在
     - `build*/_deps/` 未包含目标依赖源码
   - **或** 本次 plan 新增 `FetchContent_Declare`
   - **跳过条件**：已离线预置 `_deps/` 完整目录（含目标依赖）

2. **检测当前代理状态**（VAN 或 Plan Phase 0 执行）：
   ```bash
   git config --global --get http.proxy
   ```
   - 有返回 → 记录到 `progress.md` 进入下一步核对
   - 空返回 → **必须在 VAN 阶段补设**（见 3）

3. **补设代理（VAN 阶段强制）**：
   开发环境代理地址的**单一真相来源**是 `memory-bank/techContext.md`「FetchContent 与代理」段。规则文件中**严禁硬编码** IP，用占位符：
   ```bash
   # 从 techContext.md 读取实际地址，替换占位符后执行
   git config --global http.proxy <开发环境代理地址>
   git config --global https.proxy <开发环境代理地址>
   ```
   执行后再次 `git config --global --get http.proxy` 确认生效并记录到 `progress.md`。

4. **Plan Phase 0 固化**：
   plan 文档必须包含 Phase 0「代理状态核验」步骤，执行 grep 并把输出作为制品写入 `progress.md`，以便 `/reflect` 阶段回溯。

5. **Archive 阶段决策**：
   `/archive` 阶段询问用户：保留代理 / unset (`git config --global --unset http.proxy`)。默认**保留**（下一任务多半也需要）。

6. **交叉引用**：
   - 开发环境代理地址：`memory-bank/techContext.md`「FetchContent 与代理」段
   - VAN 命令守卫：`.cursor/commands/van.md` 步骤 1「FetchContent 代理状态检查」子项

## Phase 0 audit — spec vs code 一致性 + 能力假设双 audit（涉及既有 spec 数据回归类任务时**必填**）

> **反复模式 #8 spec 数据回归 triple-evidence 已固化**（TASK-20260503-04 + TASK-20260505-01 + TASK-20260505-02 三次累计）。spec 标记某功能「缺失/未实现」但实际代码已实现 / spec 推荐路径「假设可行」但底层引擎能力不支持 — 这两类回归在 plan 阶段未 audit 即写代码 → build 阶段才发现冲突 → 计划返工。

### 触发条件

plan 的 Phase 0 audit 段**必填**以下任一情形：

- 任务范围引用了既有 `docs/specs/*.md` 文件
- VAN 阶段从 spec 中抽取了「未实现功能清单」/「待补能力清单」/「推荐实施路径」
- 任务范围涉及既有 sub-system 能力扩展（DOM bindings / DevTool / Renderer / 等）
- 跨任务重复使用同一 spec 文档（≥ 2 任务引用）

### 强制双子条 audit

#### 子条 A — spec vs code 一致性 audit（spec 标记"未实现"但实际已实现）

**操作步骤：**

1. **VAN/plan 阶段** 对 spec 中标记 `❌` / `⏳` / `TODO` / `未实现` / `缺失` 的功能清单逐项 grep 实证：
   ```bash
   # 例：spec 标 "DOM bindings 缺 textContent setter"
   rg -n "textContent" veloxa/script/dom_bindings.cc          # 验证是否已实现
   rg -n "set_textContent\|js_set_textContent" veloxa/script/  # 验证 setter 形式
   ```

2. **如发现 spec 标"缺失"但代码已实现** → plan 阶段强制：
   - 在 plan §0「Phase 0 audit」段记录「spec vs code 偏差矩阵」
   - 调整子任务范围（剔除已实现项 / 缩小工作量）
   - 触发 spec 数据回归 `docs(specs)` commit（与本任务 plan commit 分离 / 沿用 reflect 阶段处理路径）

3. **如发现 spec 标"已实现"但代码未实现** → 同样记录 + 评估是否影响本任务依赖

#### 子条 B — 能力假设 audit（spec 推荐路径"假设可行"但底层引擎能力不支持）

**操作步骤：**

1. **VAN/plan 阶段** 对 spec 推荐的「实施路径」/「API 调用方式」/「数据通信路径」做能力假设 grep 实证：
   ```bash
   # 例：spec 说 "通过 QuickJS Proxy 实现自动 closure"
   rg -n "JS_NewProxy\|JS_NewProxyTarget" _deps/quickjs-src/   # 验证 QuickJS 是否暴露 Proxy API
   rg -n "new Proxy" examples/ tests/                          # 验证既有用法
   ```

2. **如发现底层引擎/库不支持** → plan 阶段强制：
   - 在 plan §0 记录「能力假设偏差矩阵」+ 备选实施路径
   - **brainstorming 阶段**主动 push-back（参考 `brainstorming.mdc`「Phase 0 grep 实证驱动的主动 push-back 模式」段）
   - 视情况升级复杂度 / 缩减 scope / 重做 spec

3. **如能力部分支持** → 评估退化路径（如 Proxy → 手工 closure / DOM walk）

### 反模式

- ❌ plan 仅写「按 spec §X 实施」未做 spec vs code grep 实证（最常见反模式）
- ❌ 假设 spec 描述准确 / VAN/plan 阶段不实证（违反「证据优于断言」核心原则）
- ❌ 发现偏差后绕过本规则继续写代码 / build 阶段才发现冲突
- ❌ 仅 grep 子条 A（一致性）跳过子条 B（能力假设）— 两子条独立必填

### triple-evidence 实证表

| # | 任务 | 类型 | 偏差情况 | 后果 |
|:-:|---|---|---|---|
| 1 | TASK-20260503-04 | 子条 A | spec 标 console_output API 缺失 / 实际已存在 `vx_devtool_console_panel_get` | plan 阶段缩减 scope ✅ |
| 2 | TASK-20260505-01 | 子条 A | spec 标多个 DOM bindings 缺失 / 实际全部已实现 | reflect 阶段触发 spec 数据回归 commit |
| 3 | TASK-20260505-02 | 子条 B | spec 推荐 QuickJS Proxy 路径 / 实际 QuickJS 不暴露 JS_NewProxy 公开 API | brainstorm 阶段 push-back / 改用 explicit closure（参考 `brainstorming.mdc` Phase 0 grep push-back 段）|

### 与既有规则协同

- 与本规则「公开 API testability 检查清单」段配套（spec 数据回归 + testability 双 audit）
- 与 `brainstorming.mdc`「Phase 0 grep 实证驱动的主动 push-back 模式」段协同（双子条 audit 是 push-back 的实证基础）
- 与 `memory-bank/systemPatterns.md`「反复模式 #8 spec 数据回归 audit 协议」段协同（同源 triple-evidence 详细实证）

## Phase 0 audit — JS context 归属 / host binding 注册 ctx 一致性 audit（涉及 QuickJS / panel JS / 用户脚本时**必填**）

> **TASK-20260503-04 反思 §5 #1 实证**（panel JS 实际跑在 console panel 自己的 QuickJS context 而非 main page context / host binding 注册到错误 ctx 导致 panel JS 调用 binding 报 `ReferenceError`）。VAN/plan 阶段假设「panel JS 与 main page 共享 ctx」未实证 → build §C.5.2 才发现需要双 ctx 注册。

### 触发条件

plan 涉及以下任一情形**必填**：

- DevTool panel JS 注入（console panel / inspector panel / performance panel）
- 用户脚本注入（hot reload / script tag / `<script src=>`）
- host binding 注册（`vx_view_register_binding` / `JS_NewCFunction` 类调用）
- 跨 ctx 共享数据（panel JS 访问 main page 数据 / 跨 panel 通信）

### 强制 audit 步骤

1. **ctx 归属 grep**：每个 JS 执行点定位其 QuickJS context 归属：
   ```bash
   # 例：console panel JS 跑在哪个 ctx？
   rg -n "JS_NewContext\|JS_FreeContext" veloxa/devtool/   # 找 panel ctx 创建点
   rg -n "console_panel.*ctx\|panel_ctx_" veloxa/devtool/  # 找 panel ctx 字段
   ```

2. **binding 注册点反向 grep**：每个 host binding 验证注册到哪些 ctx：
   ```bash
   rg -n "JS_NewCFunction\|JS_DefinePropertyValueStr" veloxa/script/  # 找所有 binding 注册点
   # 验证：注册到 main_ctx_ vs panel_ctx_ vs 两者
   ```

3. **跨 ctx 调用矩阵**：列出每个 JS 执行点能访问的 binding 集合：

   ```markdown
   | JS 执行点 | 归属 ctx | 可调用 binding | 缺口 |
   |---|---|---|---|
   | console panel JS | panel_ctx_ | console.* | ❌ document.* (main_ctx_) |
   | hot reload script | main_ctx_ | document.* / window.* | OK |
   ```

4. **缺口补救**：如发现 JS 执行点缺 binding → plan 阶段加「双 ctx 注册」前置子任务

### 反模式

- ❌ 假设「panel JS 与 main page 共享 ctx」未 grep 实证
- ❌ host binding 仅注册到 main_ctx_ / panel JS 调用报错 / build 阶段才发现
- ❌ plan 写「panel JS 调用 console.log」未声明 console binding 注册到 panel_ctx_
- ❌ 跨 ctx 共享对象（如 main_ctx_ JSValue 传到 panel_ctx_）未做 ctx 隔离审计

### 实证

- TASK-20260503-04 §C.5.2：plan 假设 console panel JS 调用 `vx_console.log()` 直接生效，build 阶段发现 panel JS ctx 缺 binding → 1 commit 补「panel_ctx_ 同步注册 console.log binding」/ 计划返工

### 交叉引用

- `memory-bank/techContext.md`「QuickJS context 隔离模式」段（同源 ctx 归属设计）
- 本规则「资源类反向探针 SOP」段（comment policy / 反向探针 false positive 防御）

## Phase 0 audit — 资源类反向探针 SOP（涉及 panel HTML/CSS/JS 资源反向探针时**必填**）

> **TASK-20260503-04 反思 §5 #2 实证**（D.3 console_panel.html 注释里的 `<input` 字面量触发 binding 反向探针 false positive / 注释中的字符串被错认为代码使用点）。资源反向探针（如「console panel 是否使用了 `<input>` 标签」类查询）应限定到非注释区域。

### 触发条件

plan 涉及以下任一情形**必填**：

- 反向探针 panel HTML / CSS / JS 资源文件（验证某 binding / class / API 是否被使用）
- 验证「panel JS 是否调用了 `vx_xxx` binding」类查询
- 验证「panel CSS 是否引用了某 class」类查询
- 反向探针结果用于决定是否保留/移除某 binding / API

### 强制 SOP

#### 步骤 1 — 限定到非注释区域 grep

```bash
# 默认 rg 会 match 注释中的字符串 → 必须使用 -g 排除 / 或 awk 过滤
# bad: rg -n "<input" veloxa/devtool/console_panel.html
# good: 排除 HTML 注释 <!-- ... -->
rg -n "<input" veloxa/devtool/console_panel.html | grep -v "<!--"

# JS 资源排除单行注释 // 和 块注释 /* */
rg -n "vx_console" veloxa/devtool/*.js | grep -v "^.*//.*vx_console"
```

#### 步骤 2 — 反向探针结果二次验证

发现 match 后必须打开文件人工确认是否真为代码使用点（vs 注释 / 文档字符串 / 测试 fixture 中 mock 数据）：

```markdown
| 探针目标 | 文件 | 行号 | 类型（代码 / 注释 / 文档 / mock） |
|---|---|---|---|
| <input | console_panel.html | 42 | 注释（<!-- TODO: add <input -->）❌ false positive |
| <input | console_panel.html | 87 | 代码 ✅ true positive |
```

#### 步骤 3 — comment policy 推荐

对**新写**的 panel 资源（HTML / CSS / JS）：

- ✅ 注释中**避免**写出未来可能被反向探针的字面量（如 `// TODO: add <input>` → 改为 `// TODO: add input element`）
- ✅ 注释中如必须含字面量 → 加显式标记（如 `<!-- DEAD-CODE: <input /> 已废弃 -->`）便于反向探针自动过滤
- ✅ panel 资源开头加 comment policy 注释引导后续维护者

### 反模式

- ❌ 直接 `rg -n "X" panel.html` 不加注释过滤 → false positive
- ❌ 反向探针发现 0 match 即声明「未使用」未做注释 vs 代码区分
- ❌ panel HTML 注释中含未来 binding 字面量（导致后续探针 false positive）

### 实证

- TASK-20260503-04 D.3：plan 反向探针 console_panel.html 是否使用 `<input>` 标签 → match 多处 → 实际全在注释 → 决定移除 `<input>` binding 错误（false positive 导致计划误判）

### 交叉引用

- 本规则「Phase 0 audit — JS context 归属」段（同源 panel JS / 资源 audit）
- `memory-bank/systemPatterns.md`「反向探针 false positive 抑制」段（同源 comment policy）

## 测试基础设施审计（任何 Phase 包含新写测试时**必填**）

> 反复出现（TASK-11 多次）：测试需要访问的内部状态没有访问路径，临时塞 friend 或 `#define private public`。

对每个测试任务列出：

1. **被测内部状态**：测试需要读哪些内部字段？（私有成员、内部容器大小、内部回调触发次数）
2. **当前访问路径**：是否已存在公开 getter / 测试专用 friend / 间接观测点？
3. **缺失补救**：若无访问路径 → 计划中加「补充访问路径」前置任务。优先级顺序：
   - **首选**：暴露公开只读 getter（`size()`、`count_xxx() const`）
   - **次选**：在被测类中显式 `friend class XxxTest;`（白名单 friend）
   - **禁止**：`#define private public`、`reinterpret_cast` 越权访问

## ctest 数量预期 config 矩阵（plan 验收段写「期望 ctest N PASS」时**必填**）

> 反复出现 P1（TASK-20260502-02 P1 #2 + TASK-20260503-01 C-#2 第二次同类反复升 P1 警戒线）：plan §验收写「期望 ctest 1228+」未明示「DEVTOOL=ON + SDL2=ON + Benchmarks=ON 矩阵」假设；实测 1247 是 SDL2/Benchmarks=OFF 的活跃测试集（DEVTOOL=ON），属正常配置差额，但 plan vs 实测 偏差产生疑似回归恐慌 → 浪费 reflect 阶段澄清时间。

**触发条件**：plan / spec / reflection 中出现 ctest 数量声明（"期望 N PASS" / "+M cases" / "baseline N/N" 等）时**必填**。

**强制产出 ctest config 矩阵**：

| Config | DEVTOOL | SDL2 | Benchmarks | 期望 ctest | 实测 ctest | diff |
|---|:-:|:-:|:-:|:-:|:-:|:-:|
| baseline | ON | OFF | OFF | 1195 | - | - |
| 本任务 ON path | ON | OFF | OFF | +N → 1195+N | - | - |
| OFF path（A14 验证）| OFF | OFF | OFF | -M（黑名单 M 测试）| - | - |
| SDL2 path（example smoke）| ON | ON | OFF | +K（含 hello_*_smoke）| - | - |

**判读规则**：

- 期望 vs 实测 diff = 0 → ✅ PASS，plan 假设正确
- diff = 配置差额（如 SDL2 套件未启用）→ ✅ 配置差额，plan 写明则非回归
- diff ≠ 0 且非配置差额 → 🔴 真实回归，触发 systematic-debugging skill

**最小标注模板**（plan 验收段必备 1 行）：

```markdown
**ctest 期望矩阵**：DEVTOOL=ON 1195+N / DEVTOOL=OFF 1082+0（A14 黑名单守门）/ SDL2=ON 1265+K（含 example smoke）
```

**反模式**：

- ❌ plan 写「期望 1228+」未明示配置矩阵 → 实测 1247 / 1265 / 1082 等数字时无法快速判定哪个是真 baseline
- ❌ 仅声明 baseline 数字未声明配置 → 跨任务跨 host 跨配置难以对照
- ❌ 把「ctest 1228 PASS」当作所有配置永真 — 配置矩阵差额可达 ±200 测试

**TASK-20260502-02 / TASK-20260503-01 双重实证**：

- TASK-20260502-02 P1 #2 首次出现：plan §B.5.2 验收写「DEVTOOL=ON 1228 / DEVTOOL=OFF 1082」未明示 SDL2/Benchmarks 配置 → reflect 阶段才补
- TASK-20260503-01 C-#2 第二次同源：plan §C.5.2 写「期望 1260+」未识别 SDL2 配置差 → 实测 1247（DEVTOOL=ON + SDL2=OFF）vs 1265（DEVTOOL=ON + SDL2=ON）反 plan 假设
- **第二次同源 = P1 警戒线** — 本规则首版即避免第三次同源

### add_test config guard 边界假设审计（**强制 audit 子条** — TASK-20260503-04 + TASK-20260503-05 双重实证后升级 P0）

> **反复模式 #1 子维度「config 矩阵 guard 边界假设漏审」第 2 次连续命中（TASK-20260503-05 plan §0.4 OFF 1082 假设错 / TASK-20260503-04 plan §0.6 OFF 1087 假设错）→ 触发 3 次=P0 升级阈值（systemPatterns「反复模式渐进式抑制」段已沉淀 3 次=P0 原则）→ 本子条强制必填**。

**触发条件**（满足任一即必填本子条）：

1. plan 中新增 `vx_add_test` / `add_test` 调用（任意子目录的 CMakeLists.txt）
2. plan §配置矩阵段写「OFF 1XXX 不变」/「OFF 测数 = baseline」类预测
3. 任务涉及新公开 C API + lazy-attach 容错 ABI（OFF 路径 stub 测路径常见）

**强制 audit 子条**（plan §0 实证段必须含此子段）：

```markdown
### Phase 0.X — add_test config guard 边界审计

对每个新增的 *_test.cc 文件，grep 验证其 add_test 调用是否被 config guard 包围：

  rg "vx_add_test\(${NEW_TEST_NAME}" tests/CMakeLists.txt -C 5 | rg "if\s*\(VX_"
  rg "add_test\(.*${NEW_TEST_NAME}" tests/**/CMakeLists.txt -C 5

判读：
- 如有 `if (VX_BUILD_DEVTOOL)` / `if (VX_PLATFORM_SDL2)` 等 guard 包围 →
  该测仅在该 config 下跑（OFF 测数预测 = baseline）
- 如无 guard 包围 → 该测在所有 config 下都跑（OFF 测数预测 = baseline + N）
- **特别注意**：OFF stub 测（lazy-attach C ABI 容错验证 / `#ifdef VX_BUILD_DEVTOOL`
  双路径测试）通常**不加** add_test guard 让 OFF 也跑（用于验证 OFF 路径返
  INVALID_STATE / NOT_ATTACHED 协议正确性）— 这是有意为之的设计模式，
  plan §配置矩阵预测 OFF 数字时**必须**计入此类测
```

**反模式**：

- ❌ 仅依据测试源文件路径（如 `tests/devtool/console/`）推断「OFF 不跑」— 路径不决定 config guard 归属
- ❌ 仅依据 source 是否含 `#ifdef VX_BUILD_DEVTOOL` 推断「OFF 跑空壳」— 需 add_test 不加 guard 才会真跑
- ❌ 假设「子目录 CMakeLists.txt 自动继承父级 if (...) guard」— 实际由各 add_test 调用点决定

**TASK-20260503-04 / TASK-20260503-05 双重实证**：

- TASK-20260503-05 plan §0.4 首次出现：写「DEVTOOL=OFF 1082 → 1082（不变）」实际 1087（+5 — quickjs_engine_test 在 vx_script 无 DEVTOOL guard，OFF 仍跑 9 测中 5 测为新增）
- TASK-20260503-04 plan §0.6 第二次同源：写「DEVTOOL=OFF 1087 不变」实际 1091（+4 — console_api_test 含 OFF stub 测路径验证 INVALID_STATE 返回，未加 add_test guard）
- **第 2 次连续命中 = P0 升级**（vs P1 警戒线一次范本即不再容忍第二次）— 本子条强制必填即避免第三次同源

**交叉引用**：`memory-bank/systemPatterns.md`「反复模式渐进式抑制」段「3 次=P0 升级原则」（已沉淀）；`memory-bank/reflection/reflection-TASK-20260503-04.md` §3.2 + §4.2 + §5.1（升级触发详细记录）；`memory-bank/reflection/reflection-TASK-20260503-05.md` §3.1 + §4.1（首次实证记录）。

**交叉引用**：`memory-bank/systemPatterns.md`「ctest baseline 配置矩阵」段（待本任务 archive 阶段补）；`memory-bank/techContext.md`「CMake basic vs full 配置矩阵」段（VX_BUILD_BENCHMARKS + VX_PLATFORM_SDL2 配置语义说明）。

## 边界输入清单（涉及解析、查找、转换、边界判断时**必填**）

> 反复出现（TASK-06）：only happy path 测试，遇到空/null/超长立刻崩。

每个相关 Phase 必须列出：

| 类别 | 示例 | 预期行为 |
|------|------|---------|
| 默认路径 | 正常字符串、合法 ID、典型尺寸 | 业务成功 |
| 空 | `""`、`nullptr`、零长度数组 | 静默忽略 / 返回错误码（明示哪个）|
| 超长 | 1MB 字符串、深度 10000 嵌套 | 拒绝 / 截断（明示）|
| Unicode | 多字节 UTF-8、emoji、ZWJ | 正确处理（不截到字节中间）|
| 边界值 | 0、-1、INT_MAX、FLT_MIN | clamp / 拒绝（明示）|
| 未注册项 | 不存在的 ID、未定义的 enum | 返回 unknown / 默认值 |

每条边界输入对应 ≥ 1 个测试。

## 调用链端到端验证（修改函数签名 / 默认值 / 返回类型时**必填**）

> 反复出现（TASK-09）：SelectorMatcher 加了参数没透传到 StyleResolver，导致 restyle 阶段伪类永远 false。

计划必须包含：

1. **完整调用链清单**：从最上游入口（`Application::Update`、`vx_view_update`、用户回调）到最下游被改函数，列出每一层的文件 + 函数名。
2. **透传决策**：每一层是否需要透传新参数？若是，标注「需透传」。
3. **测试覆盖**：现有测试是否覆盖整条链？若否 → 加 ≥ 1 个端到端集成测试。
4. **验证脚本**：在收尾 Phase 用 grep 确认所有调用点已更新：
   ```bash
   rg "OldFunc\(" --type cpp | grep -v "_test.cc"
   ```

## 中文文档 StrReplace 字符类型 audit（编辑含中文标点的 .md / .mdc / .rst / .txt 文档时**必填** — TASK-20260504-01 反思入库 / P0 沉淀）

> 反复模式新候选首次定型（TASK-20260504-01 commit 5 finalize 阶段实证）：编辑 README.md「项目状态」段时因全角 `（` vs 半角 `(` / 全角 `；` vs 半角 `;` 字符差异 StrReplace fuzzy match 失败 4 次重试，浪费 ~5-10 min。视觉上字符差异不易区分（特别是括号），易形成「凭记忆构造 old_string」陷阱。

**触发条件**：编辑以下任一文件类型时强制做本 audit：

- 中文 README / spec / archive / progress / activeContext / systemPatterns / techContext / productContext
- 中文 reflection / creative 文档
- 任何含中文标点（`（）；！？「」、`）的 .md / .mdc / .rst / .txt 文档

**强制 4 项 mitigation**：

| # | mitigation | 描述 | 反模式 |
|:-:|---|---|---|
| 1 | **强制先 Read 原文范围** | 所有 StrReplace 前先用 Read 工具读取准确目标范围（含 ≥3-5 行上下文）/ 不允许凭记忆构造 old_string | ❌ 凭直觉/记忆构造 old_string，假设字符类型 |
| 2 | **缩小 StrReplace 范围** | 单段 StrReplace 跨越全角/半角字符越少 / 错配概率越低 / 推荐每次 StrReplace ≤ 10 行 / 大段改动拆 N 次 | ❌ 一次性大段（≥ 30 行）替换 |
| 3 | **复制粘贴而非手动输入** | old_string 内容必须**完整复制**自 Read 输出 / 不允许手动输入中文标点 | ❌ 手敲 `（` `；` `、` 等全角符号 |
| 4 | **首次失败立即 Read + 缩小** | 第 1 次 StrReplace 失败（even after relaxing whitespace）→ 立即 Read 准确范围 + 缩小 old_string / 不允许「再试一次」式盲猜 | ❌ 第 1 次失败后立即用稍微变形的 old_string 再试 |

**实证数据**：

| 任务 | 文件 | 重试次数 | 浪费时间 | 根因字符 |
|---|---|:-:|:-:|---|
| TASK-20260504-01 commit 5 | `README.md` 「路线图」段 | **4** | ~5-10 min | 全角 `（` vs 半角 `(` / 全角 `；` vs 半角 `;` |
| TASK-20260504-01 commit 5 | `productContext.md` 「已实现表」 | 1 | ~30 s | 全角 `；` 中部混入 |

**与既有规则协同**：

- 与 systemPatterns 「StrReplace hunk 隔离 path-level isolation 范式」协同 — 缩小范围本质即更小 hunk
- 与本规则 「Web 标准 API 多重载形态清单」/ 「ctest 数量预期 config 矩阵」/ 「add_test config guard 边界假设审计」配套 — 都是「编辑前 audit」类强制守门

**反复模式候选定型阶段**：本次为**首次定型**（1 实证）/ 待未来 ≥ 1 次重复后正式升级反复模式 #N（沿用 systemPatterns 反复模式渐进式抑制范式）。但 mitigation 已立即沉淀生效。

**交叉引用**：

- `memory-bank/reflection/reflection-TASK-20260504-01.md` §4.2 候选 ⓪ 完整定义
- `memory-bank/systemPatterns.md` 「中文文档编辑安全 audit」段（同源沉淀）

---

## 管线注入点代码级可行性验证（设计文档提出"在 X 步骤后注入 Y"时**必填**）

> 反复出现第 4 次（TASK-13）：设计文档说"在 Layout 后插入动画覆盖层"，实际代码里 LayoutBox 是 const，覆盖层无法工作。

头脑风暴阶段必须：

1. **定位注入点**：找到 X 步骤的实际代码（文件 + 函数 + 行号）。例如「在 LayoutEngine::Layout 末尾、return 之前」。
2. **数据可访问性**：Y 所需的全部输入数据在该位置是否已可访问？是否需要新增参数透传 / 暴露内部状态？
3. **可写性**：Y 是否需要修改 X 处的输出？该输出当前是 const / 私有？是否需要 const_cast / 接口扩展？
4. **结论**：若任一项「不可」→ 设计文档**必须**显式标注「需要扩展 X 接口」并给出具体签名变更。

此段产出物：「注入点核对表」一节进入设计文档。

## 调参/调阈值/调时间常量类子任务前置 baseline smoke 实证（涉及调整现有参数/阈值/时间常量类子任务时**必填**）

> 反复模式「前置依赖/环境/API 能力未验证」第 9+ 次同源新维度（TASK-20260503-03 P1 实证）：plan §3 P1 假设「调 `VX_HELLO_DEVTOOL_AUTOQUIT_MS` 200→600ms + 收紧 PASS_REGULAR_EXPRESSION = 自然多帧」；实测 autoquit 600ms / 1500ms 均 frames=1 — `UpdateManager::Update()` 第 17 行 `if (!dirty_) return;` 短路 + `dirty_` 仅 `transition_mgr_.HasActive()` 才 rearm，静态 CSS 第 1 帧后永久 frames=1。VAN/plan 阶段 grep 看到 `set_pipeline_hooks` C ABI + `OnFrame` 调用链 + `update_manager_->Update()` 全部存在 ✅，但**「现有实现 runtime 行为」是另一个维度，grep 不能发现**。反复模式 #1 适用范围**升级**到包括「调参/调阈值/调时间类假设的 runtime 行为未验证」子维度。

**触发条件**：plan §3 子任务涉及以下任一情形 → 必填本段：

- 调整现有 ctest `set_tests_properties(... PROPERTIES TIMEOUT N)` / `ENVIRONMENT "...=N"` 时间/数值常量假设可获得某种 runtime 行为变化
- 调整现有 `PASS_REGULAR_EXPRESSION` / 断言阈值假设可收紧/放宽既有测试范围
- 调整现有 source code 中 magic number / 默认参数 / 配置常量假设可调整 runtime 行为
- 任何「调 X 即可获得 Y」的 runtime 行为假设，X 与 Y 之间的因果路径未在 plan §0 grep 之外做实证或源码 Read 确认

**强制 5min baseline smoke 实证（plan §0 必填两步择一）**：

1. **实证路径（推荐 — 5 min 投入）**：
   - 跑一次现有 baseline 命令，记录实测输出到 plan
   - 例（本任务的反例）：`SDL_VIDEODRIVER=dummy VX_HELLO_DEVTOOL_AUTOQUIT_MS=200 ./build/examples/hello_devtool 2>&1 | grep "PERF SMOKE"` → 若实测 frames=1 已经是 baseline，则「调 autoquit ms = 多帧」假设立即被否决
   - 实证记录格式：「实测命令：[命令] / 实测输出：[输出] / 假设是否成立：✅/❌」

2. **Read 路径（替代 — 适用 baseline 不可跑情形）**：
   - 若 baseline 依赖大型 reconfigure / 真实硬件 / 用户交互等无法快速跑，必须 Read 假设涉及的核心 runtime 行为源码（不只 grep API 存在性）确认机制路径
   - Read 范围：从假设的「触发源」（如 SetTimer / OnFrame）到「目标行为」（如 frame hooks 调用）的完整调用链中的关键短路 / 守卫 / 状态机分支
   - 例（本任务应该做的）：`Read veloxa/core/update_manager.cc` → 看到 `if (!dirty_) return;` 第 17 行短路 → 立即识别 dirty_ rearm 路径仅 `transition_mgr_.HasActive()` 一个源 → 静态 CSS 场景下假设否决

3. **若 1 + 2 都不可执行**：
   - plan §3 子任务必须显式标注「**未验证假设 — 实施失败概率高**」，并把该子任务降级为「探针式实施」（先做最小验证子任务，PASS 后再做主体）
   - 反例：本任务 P1 没标注「未验证」直接进入 build 阶段实施 → 12 min 失败回退代价

**判读规则**：

- 实证假设成立 ✅ → plan 子任务正常进入实施
- 实证假设否决 ❌ → 立即在 plan 阶段拆细化 / 取消该子任务（节省 build 阶段反复模式 #1 命中代价）
- Read 路径机制清晰 → plan 子任务带「机制依赖说明」段进入实施
- Read 路径发现「调 X 不能直接获得 Y」→ plan 子任务必须重新设计或拆细化

**反模式**：

- ❌ plan §0 grep 仅查 API/调用链存在性（如「`set_pipeline_hooks` 存在 ✅」），不查 runtime 行为机制（如「`UpdateManager::Update()` 是否每帧无条件触发 hooks」）
- ❌ 假设「调 autoquit ms 多大 = 自然多帧」「调 timeout 大 = 测试更稳定」「调阈值更宽松 = 更多测试 PASS」类**线性外推**，不做实证或 Read 验证
- ❌ 把 baseline 跑过 1 次（如「frames=1 hud_visible=1 旧 PASS」）当作假设可成立证据 — baseline PASS 不等于「线性外推假设」可成立

**ROI 公式**：

```
plan 5min baseline smoke 投入 → 节省 build 阶段（反复模式 #1 命中代价 ~30-60 min + 用户 AskQuestion ~5-10 min + plan 调整 ~5-10 min）= ROI 8-16×
```

**TASK-20260503-03 实证**：P1 子任务实施失败 ~12 min（手测 ~5 + 根因定位 ~2 + 回退 ~1 + AskQuestion ~5 + plan 调整 ~5）— **反复模式 #1 命中代价基线**入库。若 plan 阶段做 5min baseline smoke（`SDL_VIDEODRIVER=dummy ... ./hello_devtool` 一次），假设立即否决，0 build 阶段代价。

**正例对比（同任务 P3 task #3 audit 预跑）**：VAN 阶段对「8 处反模式」假设做 grep + Read 实证 → 修正为「2 处反模式」 → build 阶段 0 浪费，估时 ×0.20。

**交叉引用**：`memory-bank/systemPatterns.md`「调参/调阈值类子任务的 5min baseline smoke 实证规则」段（archive 阶段同步补）。

## 既有测试隐式契约 fingerprint（layout / parser / event 等富边界子系统**必填**）

> 反复模式「前置依赖/环境/API 能力未验证」第 9+ 次同源新维度（TASK-20260430-01 实证）：plan §0 grep 仅查目标功能 fingerprint（F1-F6），未对**即将 touch 的代码路径相关的所有 `*test*.cc` 做边界期望反向 fingerprint**。R3 既有 sibling collapse 测试隐含「`Layout(root, ctx)` 之后 root 自身 margin-top 不会 propagate 到 viewport」假设，build P3 GREEN 实施完后该测试退化（root 节点 propagate 到了不该 propagate 的位置）。

**触发条件**：本任务即将修改的代码路径具备以下任一特征 → 必填本段 grep 表：

- 子系统拥有大量边界 case 测试（layout / CSS parser / HTML parser / event system / animation / IFC）
- 现有测试数量 ≥ 5 个且测试 fixture 含数值化几何 / 状态断言
- 当前任务会扩展既有 API（如新增参数 / 新增分支 / 新增递归边界）

**强制产出 grep 表**：plan §0 batch grep 必须包含「**既有测试边界期望反向 fingerprint 表**」，对 ≥ 1 个相关测试文件做关键字 grep 并把命中条目记录到 plan：

| 子系统 | 必查测试文件 | 关键字模板 | 期望命中数（≥）|
|---|---|---|---|
| Layout | `tests/core/layout/*_test.cc` | `EXPECT_FLOAT_EQ.*\.y` / `EXPECT_FLOAT_EQ.*content_height` / `EXPECT_TRUE.*collapsed` / `EXPECT_FALSE.*collapsed` | 3 |
| CSS parser | `tests/core/css/*_test.cc` | `EXPECT_EQ.*Length` / `EXPECT_EQ.*kKeyword` / `EXPECT_FALSE.*ParseDeclaration` | 3 |
| HTML parser | `tests/core/html/*_test.cc` | `EXPECT_EQ.*tag_name` / `EXPECT_EQ.*attribute` / `EXPECT_NE.*nullptr` | 3 |
| Event | `tests/core/event/*_test.cc` | `EXPECT_EQ.*phase` / `EXPECT_TRUE.*propagated` / `EXPECT_FALSE.*default_prevented` | 3 |
| Animation | `tests/core/animation/*_test.cc` | `EXPECT_FLOAT_EQ.*progress` / `EXPECT_EQ.*kRunning` | 3 |

**判读规则**：

- 命中 ≥ 期望数 → plan §0 列出所有命中条目，标注每条隐含的边界假设（如「root 不向上 propagate」「auto width 等于 fit-content」）
- 命中 < 期望数 → 拓展关键字模板再 grep；仍不足 → 任务范围内**禁止改动该子系统的核心算法**（fingerprint 不足以保护回归）

**与 §9 Layout 必检项协同**：§9.1 锁 box-model 跨 LayoutType 独立解析；§9.2 锁默认值边界；§9.4 锁递归 API 传递语义；本节锁**既有测试边界假设的反向覆盖**，四者覆盖 layout 类任务 plan 阶段验证完整链路。

**反模式**：

- ❌ plan §0 grep 仅查目标功能（"first child collapse 当前是否实施"）→ 漏掉既有测试的隐含期望（"root 不向上 propagate"）
- ❌ 把「ctest baseline N/N PASS」当作既有测试覆盖完整证据 — baseline PASS 不等于 fingerprint 完整列出
- ❌ 测试文件命中条目 ≥ 5 但 plan 不列出具体行号与语义 — build 阶段无法快速反向判定退化点

**TASK-20260430-01 实证**：plan §0 grep 仅查 F1-F6（clearance stub / float 解析 / first-child 现状 / LayoutChild API / wpt-005 / FetchContent），未列「R3 sibling collapse 测试隐含 root 不 propagate」 → P3 GREEN 后既有 `MarginCollapseLayoutTest` 系列退化；P4 加 `box->parent == nullptr || box->parent->parent == nullptr` 隐式 BFC root 识别才修正。

**交叉引用**：`memory-bank/systemPatterns.md`「既有测试隐式契约 fingerprint」段（待 archive 阶段补）。

## CSS shorthand 能力 grep 表（CSS / 解析器类任务**必填**）

> 反复模式「前置依赖/环境/API 能力未验证」第 9+ 次同源新维度（TASK-20260430-01 副发现）：plan §1.2 RED 测试 `BorderBottom_BlocksLastChildCollapse` 直接用 `border-bottom: 1px solid black`，build 时发现 css parser 不解析 `border-bottom` shorthand → child 没有 border-bottom 计算值 → blocks_bottom=false → 测试反而 PASS（虚假通过）。VAN/plan 仅 grep 验证 `padding` 总体可解析，未对每个 shorthand 单独验证。

**触发条件**：plan / RED 用例使用任意 CSS shorthand 时**必填**：

- `margin` / `padding` / `border` / `font` / `background` / `transition` / `animation` / `flex` / `grid` 等总称属性
- 含方向后缀的 shorthand：`margin-top` / `border-bottom` / `border-left-color` / `padding-block` 等
- CSS variables / `calc()` / `env()` / 其他 functional notation

**强制产出 grep 表**：plan §0 batch grep 必须包含「**CSS shorthand 能力验证表**」：

| shorthand 候选 | 验证命令 | 命中预期 | 缺失兜底 |
|---|---|---|---|
| `padding` | `rg "kPadding\b\|\"padding\"" veloxa/core/css/parser.cc -n` | ≥ 1 处 longhand 展开 | — |
| `margin` | `rg "kMargin\b\|\"margin\"" veloxa/core/css/parser.cc -n` | ≥ 1 处 longhand 展开 | — |
| `border-bottom` | `rg '"border-bottom"\|kBorderBottom\b' veloxa/core/css/ -n` | ≥ 1 处 shorthand 解析 | 若未命中 → 改用 `border-bottom-width` + `border-bottom-style` + `border-bottom-color` longhand 三条；或用 `padding-bottom` 等价物理分隔符 |
| `border` | `rg "ParseBorderShorthand\|kBorder\b" veloxa/core/css/parser.cc -n` | ≥ 1 处 shorthand 解析 | 若未命中 → 改用 4 边 longhand |
| `font` | `rg "ParseFontShorthand\|kFont\b" veloxa/core/css/parser.cc -n` | ≥ 1 处 shorthand 解析 | 若未命中 → 改用 `font-size` + `font-family` + `font-weight` longhand |
| `background` | `rg "ParseBackgroundShorthand\|kBackground\b" veloxa/core/css/parser.cc -n` | ≥ 1 处 shorthand 解析 | 若未命中 → 改用 `background-color` + `background-image` longhand |
| CSS var (`--*`) | `rg "kCustomProperty\|--" veloxa/core/css/property.cc -n` | ≥ 1 处 dispatch | 若未命中 → 测试中禁用 CSS variable，改用直接值 |
| `calc(...)` | `rg "ParseCalc\|kCalc\b" veloxa/core/css/parser.cc -n` | ≥ 1 处 calc 表达式解析 | 若未命中 → 改用预计算的具体数值 |

**判读规则**：

- 命中 ≥ 1 处 → plan / RED 可直接使用该 shorthand
- 命中 0 处 → **强制改用兜底方案**或**列入独立 P3 触发型技术债**（不在当前任务范围扩展 parser）
- 任何 shorthand「假定可用但未 grep」均视为反模式

**反模式**：

- ❌ plan 写「使用 `border-bottom: 1px solid black` 隔离 last-child collapse」未单独 grep → build 阶段测试虚假通过
- ❌ 对一个 shorthand 命中即假定整组 shorthand 均可用（`padding` ✅ 不代表 `padding-block` ✅）
- ❌ 用 ctest baseline PASS 代替 shorthand 能力验证 — baseline 不一定覆盖该 shorthand

**TASK-20260430-01 实证**：plan §1.2 测试 `BorderBottom_BlocksLastChildCollapse` 直接用 `border-bottom: 1px solid black`，VAN/plan 仅 grep 验证 `padding` 总体可解析；build P4 实施时发现 css parser 不处理 `border-bottom` shorthand → child 没有 border-bottom 计算值 → blocks_bottom=false → 测试反而 PASS（虚假通过）→ 改测试名为 `PaddingBottom_BlocksLastChildCollapse` + 用 `padding-bottom: 1px` 等价物理分隔符；border-bottom shorthand 缺失列入独立 P3 候选 `tasks.md §待立项候选`。

**交叉引用**：`memory-bank/systemPatterns.md`「CSS shorthand 能力 grep 表」段（待 archive 阶段补）。

## 性能基准任务必检项（涉及 google/benchmark 新 .cc / 新 BM 时**必填**）

> 反复出现升级 P0（TASK-20260419-02 → TASK-20260419-03 → TASK-20260419-05，至少 3 次）：性能基准任务的特殊前置条件未在 plan 阶段识别，导致 build 阶段返工或假阳性。

在任务分解**之前**回答以下 5 项：

### 1. Release + 独立 build 目录

- **必须**：plan §0 全局约束段写明 `-DCMAKE_BUILD_TYPE=Release` + 独立 `build-bench/` 目录（与 Debug `build/` 隔离）
- **反例**：TASK-20260419-02 首次未指定 → Debug 编译跑出微秒级假数字（O0 不优化）

### 2. CMake helper 函数实际签名验证 + 链接可见性

- **必须 (a)**：grep 项目内 `vx_add_benchmark` / `add_benchmark` / `add_test` 等自定义 CMake 函数的真实签名（不靠印象/复制其它项目）
- **必须 (b)**：当下游 target 通过 vx_core / vx_graphics 等中间 target 链接到第三方库（PNG::PNG / JPEG::JPEG / Freetype 等）时，**grep 必须确认链接关键字**（`PUBLIC` / `PRIVATE` / `INTERFACE`）：
  - `PUBLIC` / `INTERFACE` → 传递到下游，下游可直接链接
  - `PRIVATE` → **不传递**，下游必须独立 `find_package(<Lib> REQUIRED)` + 显式链接
  - 检查命令示例：
    ```bash
    rg "target_link_libraries\(vx_core" --type cmake -A 3 | rg "PNG::PNG|JPEG::JPEG"
    ```
- **反例 1**：TASK-20260419-05 plan §1.1 写成三参 `vx_add_benchmark(name file libs)`，实际签名 `vx_add_benchmark(name [extra_libs...])`（文件名硬编码）
- **反例 2（升 P0，TASK-20260419-09 phase-1 工程意外）**：plan 阶段假设 `bench_imagecache` 链接 `vx_core` 即可拿到 `PNG::PNG`，实际 `vx_core PRIVATE PNG::PNG` 不传递 → cmake reconfigure 报 `Target "PNG::PNG" not found`；修正方式 `benchmarks/CMakeLists.txt` 顶部加 `find_package(PNG REQUIRED)`

### 3. 目标 API 的发射/触发条件 grep（**最关键**，反复模式 #3 第 9+ 次升 P0）

> 反复出现（TASK-07 候选方案根因假设未验证 + TASK-05 Phase 6 styled-corpus 假阳性 = 第 2 次同型）

性能基准的 hot path 函数（`Record` / `Replay` / `Tokenize` / `Match` 等）通常带 **emit gating**：

- `RecordBox` 仅在 `bg.a > 0` 时 emit `FillRect`
- `RecordBox` 仅在 `image_handle != 0` 时 emit `kDrawImage`
- `SelectorMatcher` 仅在 selector 含 `:hover` 时走伪类路径
- ...

**必须**：plan 阶段对每个目标 BM 的 hot path 函数执行 grep（30 秒成本）：

```bash
# 例：bench 目标是 render::Record 的 FillRect 路径
rg "PaintCommand::FillRect|emit.*FillRect|background_color" veloxa/core/render/renderer.cc
```

确认 emit 触发条件后**反推 corpus 构造要求**（如「default div 不会 emit FillRect → 必须用 styled corpus 设 inline `background-color`」）。

**反例：**
- TASK-20260419-05 Phase 1 smoke `BM_ReplaySmoke = 1.65 ns + items_per_second=0/s`，假阳性持续到 Phase 6 才被发现：default div alpha=0 → 空 list
- TASK-20260419-07 候选方案 B3 `__builtin_memcpy` 假设根因是 fortify wrapper，未先 grep `string_fortified.h` 验证 → BUILD 试验才发现 IPA 在中端先于 fortify 展开

**落实**：候选方案 ≥ 3 项时强制要求每方案附「≤30 秒根因验证操作」（grep / 读 1 个头 / 跑 1 个 build）。性能基准任务**全程强制**。

### 4. Bench Smoke 验收三件套（避免空 list 假阳性）

「数字非零」单独**不充分**。Phase 1 smoke 验收必须 ≥ 3 项：

1. **数字非零** — 基础（避免 BM 没跑）
2. **`SetItemsProcessed(N)` 或 `SetBytesProcessed(N)` 给非零 N** — 强迫 bench 作者在 BM 体内写 `state.SetItemsProcessed(state.iterations() * collection.size())` 之类，把"非空"语义入到 metric 中
3. **JSON 输出校验 items_per_second > 0**：
   ```bash
   ./bench --benchmark_format=json --benchmark_min_time=0.001s | jq '.benchmarks[] | select(.items_per_second == 0)'
   ```
   预期空输出（无 0 项）

#### smoke 工具链可用性检查（plan 使用 `jq` / `bc` / `valgrind` / `xmllint` 等前**必做**）

> 反复出现（TASK-20260419-11 P3）：smoke 自检脚本 `jq` 在 Cursor 沙箱终端 `command -v jq` 返回 127，被迫临时切换 `python3` 重写脚本；破坏 plan 原子性，~5 min 恢复，但污染 progress.md 与 Phase 提交粒度。

**规则：** plan 里任何 smoke / 自检 / CI 脚本如使用下列工具，写 plan 之时**必须**同步 grep 验证可用性：

| 工具 | 验证命令 | 缺失兜底方案 |
|------|---------|-------------|
| `jq` | `command -v jq \|\| echo MISS` | `python3 -c "import json, sys; ..."` heredoc |
| `bc` | `command -v bc \|\| echo MISS` | `awk 'BEGIN{print ...}'` |
| `valgrind` | `command -v valgrind \|\| echo MISS` | AddressSanitizer / 跳过或降级为单元测试 |
| `awk` | `command -v awk \|\| echo MISS` | `python3 -c` 替代 |
| `xmllint` | `command -v xmllint \|\| echo MISS` | `python3 -c "from xml.etree import ..."` |
| `rg` / `ripgrep` | `command -v rg \|\| echo MISS` | POSIX `grep -rn` 降级 |

**执行时机：**
- **Plan 阶段 Phase 0「基线核验」步骤**：一次性 batch grep 本 plan 所需全部工具，输出命中/缺失矩阵写入 `progress.md`
- **Build 阶段 Phase N 执行脚本前**：若 plan Phase 0 标记该工具 MISS，直接使用兜底方案，**严禁**临时切换技术栈后再补 plan

#### Toolchain 版本激进升级行为变化检查（VAN/Plan Phase 0 §0.10 子段**必做**）

> 反复出现升级 P1 + R12 风险登记（TASK-20260503-01 §0.1 实证）：binutils 2.46（2026 升级）单次扫描静态归档严格化，破坏既有 `vx_core ↔ vx_script ↔ vx_devtool` 双重循环依赖 → 271 link target 中 ~6 测试 link FAILED → baseline 阻塞 hotfix 分离协议触发（feature 分支外 hotfix 分支独立修复 fast-forward main）。

**触发条件**：每次 VAN/Plan Phase 0 **必做** — 不论任务大小（成本 30 秒，收益避免 baseline 阻塞）。

**强制产出工具链版本快照表**：

```bash
gcc --version | head -1
ld --version | head -1                # 重点关注 binutils 版本（2.40+ 是激进期）
cmake --version | head -1
ninja --version
```

输出对比 `memory-bank/techContext.md`「环境就绪」段记录的上次任务版本。

**已知行为差异速查表**（每发现 1 项激进变化 → 增 1 行）：

| 工具 | 版本起 | 行为变化 | 影响 | 兜底 |
|---|---|---|---|---|
| binutils ld | 2.46 (2026) | 单次扫描静态归档严格化 | 破坏循环依赖 link | `--start-group ... --end-group` 包围循环组 |
| gcc | 14+ | (待登记) | - | - |
| cmake | 4.0+ | (待登记) | - | - |

**判读规则**：

- 版本与上次任务一致 → ✅ 跳过差异检查，记 1 行 progress
- 版本主版本号变化（如 2.45 → 2.46） → 🟡 必须 grep 已知差异表 + 跑 baseline ctest 验证
- 已知差异速查表未覆盖 + baseline FAIL → 🔴 触发「baseline 阻塞 hotfix 分离协议」（systemPatterns 段）

**反模式**：

- ❌ Phase 0 跳过工具链版本核对 → build 阶段第一步 ctest 阻塞才发现
- ❌ 工具链升级后默认「行为不变」— 激进版本（major bump 或 binutils 跨年版本）必须 grep 行为差异
- ❌ 工具链 baseline FAIL 在 feature 分支内修复 → 污染 feature 分支语义；正确做法是 hotfix 分支分离协议

**TASK-20260503-01 实证**：

- VAN §0.1 baseline 写「期望 1228 PASS」未做工具链版本核对 → build 阶段 binutils 2.46 严格化导致 ~6 测试 link FAIL
- 修复路径：`hotfix/binutils-2.46-link-group` 分支独立修复（10 行 CMake 加 `--start-group/--end-group`）→ fast-forward merge `ddc1e3c` → feature 分支 rebase
- 如 Phase 0 §0.10 子段先行 → 提前发现 binutils 2.46 与 2.45 的差异 → 直接在 plan 阶段加 hotfix 子任务，无 build 中断

**交叉引用**：

- `memory-bank/systemPatterns.md`「baseline 阻塞 hotfix 分离协议」段（hotfix 分支 → fast-forward main → feature rebase 链路）
- `memory-bank/systemPatterns.md`「R12 工具链版本激进升级风险」段
- `memory-bank/systemPatterns.md`「CMake 静态库循环依赖处理 — binutils 2.46+ 双循环依赖叠加场景覆盖性」段

#### 测试文件 include 卫生 grep（new test files / 引入新第三方 C header 时**必做**）

> 反复出现（TASK-20260425-01 P5 实证）：`#include <SDL2/SDL.h>` 不慎写进 `tests/api/api_test.cc` 的 anonymous namespace，间接拉入 `<math.h>` 时把 `std::abs` 解析到 `{anonymous}::std`，编译错误 `'abs' has not been declared in '{anonymous}::std'`，~5 min 排查（错误信息隐晦）。
>
> 「测试隔离问题」类反复模式第 8 次新变体（namespace 维度）。

**规则：** 任何 plan 涉及「新增 test 文件」或「现有 test 引入新第三方 C header（特别是 SDL/Vulkan/X11/Win32/CUDA 等会 transitive include `<math.h>`/`<string.h>`/`<stdlib.h>` 的 system header）」时，Plan §0 batch grep **必须**包含：

```bash
# 检查所有 test 文件 #include 是否在 file scope（无 namespace 包裹）
rg "^namespace .*\{" -A 50 tests/ | rg "^[^:]+-:\s*#include " \
  && echo "BAD: include inside namespace" || echo "OK: all includes at file scope"
```

**与 verification.mdc 协同：** test build 失败时，error message 形如 `'<symbol>' has not been declared in '{anonymous}::std'` / `not a member of {anonymous}` 是该模式的标志性 fingerprint，应直接定位到 namespace 内 include。

#### 验收用例与 example 实现一致性检查（plan 验收清单引用具体 UI 行为时**必做**）

> 反复出现（TASK-20260425-01 P6.1 实证）：plan §6.1 步骤 2 期望「鼠标 hover 红块 → 颜色 `#FF6B6B`」作为 A2 鼠标输入端到端验收用例，但 `examples/hello_sdl2.cc` 实施时沿用静态盒子样式，**未注入 `:hover` CSS 规则** → P6.1 标为遗留没暴露，但 plan 与 example 永久不一致直到回顾阶段 P0 强制修复。
>
> 「计划文件清单与实际变更不一致」反复模式第 10 次新维度（UI 行为维度）。

**规则：** plan 验收清单（A1/A2/A3 / 集成测试预期 / smoke 验收等）若引用具体 UI 行为（hover 颜色变化 / click 事件 / keystroke 反馈 / focus 状态等），**必须**满足下列**至少一项**：

1. **Plan example 段直接附 CSS/HTML/JS 片段**（不是只写「会有 hover 反馈」）—
   ```css
   #box-red:hover { background-color: #FF6B6B; }  /* A2 验收依赖 */
   ```
2. **Plan §X 步骤 0 加显式同步检查项**：
   ```markdown
   - [ ] **步骤 0 同步检查**：example 文件含验收所需的 CSS 规则
     `rg ":hover" examples/hello_sdl2.cc` 必须命中
   ```
3. **example 与 plan 同 PR 提交** — 强制 reviewer 同时看到，避免 plan 漂移

**反模式：**
- ❌ plan 写「鼠标 hover 会变色」但 example 没 :hover 规则 → 验收用例不可达
- ❌ plan 假设 example 已有某行为但 example 实现时简化 → 一致性 drift

**与 `verification.mdc` 的协同：** verification 只要求「运行后才声明通过」；本子块是 verification 的**前置条件**（跑都跑不起来就谈不上验证）。

### 5. RangeMultiplier 案例数预估（避免 BM 数与 plan 数不一致）

google/benchmark `RangeMultiplier(m)->Range(lo, hi)` 的精确 case 数为：

> **`ceil(log_m(hi / lo)) + 1`**

例：
- `RangeMultiplier(2)->Range(8, 512)` → `log2(64) + 1 = 7` cases
- `RangeMultiplier(2)->Range(2, 64)` → `log2(32) + 1 = 6` cases
- `RangeMultiplier(4)->Range(64, 4096)` → `log4(64) + 1 = 4` cases

plan 的「BM 数预估表」必须用此公式，**不要肉眼数**（TASK-20260419-03 实证 13 个 Range case 0 偏差，TASK-20260419-05 14 行同样命中）。

### 6. 渲染/绘制 bench 前置清单（render 系列任务专用）

如本次 bench 涉及 `vx::render::Record` / `vx::render::Replay` / `SoftwareCanvas` / `gfx::Canvas` 等渲染 API，参见 `memory-bank/systemPatterns.md`「Render Bench 前置清单」段，逐条核对：

- FillRect / FillRoundedRect / StrokeRoundedRect / DrawText / DrawImage 各自的 emit 条件
- 两个隐式契约：`ctx.stylesheets` 非空（StyleResolver gate）+ ImageCache 三阶段同传

### 7. WSL2 / 云机 bench 稳态协议（环境敏感场景**必填**）

> 反复出现（TASK-20260424-03 P6 实证）：WSL2 CPU governor 在不活跃时下调频率，冷启动瞬态 + load average 抖动让 google/benchmark 默认 `--benchmark_min_time=0.05s` 单次测量 CV 高达 8%，首次测得 6727 ns（43% 偏离真实 4689 ns），险些得出错误的 Phase 倒退结论。

本任务运行环境为 **WSL2 / 云 VM / Docker** 时，plan 阶段的 bench 命令**必须**改用稳态协议：

1. **sleep 等待** — `sleep 10` 让 CPU 进入 idle governor 的快速爬坡区
2. **warm-up 预热** — 单 filter `--benchmark_repetitions=3 --benchmark_min_time=0.2s` 跑一次不记录结果
3. **正式测量** — `--benchmark_repetitions=10 --benchmark_report_aggregates_only=true --benchmark_min_time=0.2s`
4. **CV 门槛** — 读取 `_cv` 聚合行，若 `> 2%` 重跑；仍不稳则记为环境不可信，延后测量

**样板（stash-swap 对比场景）：**

```bash
# before
git stash push -u
sleep 10
./build-bench/benchmarks/bench_xxx --benchmark_filter='WarmMedium' \
  --benchmark_repetitions=3 --benchmark_min_time=0.2s > /dev/null  # warm-up
./build-bench/benchmarks/bench_xxx --benchmark_filter='WarmMedium' \
  --benchmark_repetitions=10 --benchmark_report_aggregates_only=true \
  --benchmark_min_time=0.2s > /tmp/before.txt

# after
git stash pop
cmake --build build-bench -j --target bench_xxx
sleep 10
./build-bench/benchmarks/bench_xxx --benchmark_filter='WarmMedium' \
  --benchmark_repetitions=3 --benchmark_min_time=0.2s > /dev/null  # warm-up
./build-bench/benchmarks/bench_xxx --benchmark_filter='WarmMedium' \
  --benchmark_repetitions=10 --benchmark_report_aggregates_only=true \
  --benchmark_min_time=0.2s > /tmp/after.txt
```

**交叉引用：** `memory-bank/systemPatterns.md`「Bench Smoke 自检模式」段包含等价协议详情与 CV 门槛 rationale。

#### 7.0.1 同窗口对照 — performance regression 验证默认范式（**P0 强制**，TASK-20260426-01 升级）

> 反复模式新升级（TASK-20260426-01 R3 实证）：跨时间窗 bench baseline 数据点不可比 — 系统抖动 ±3-7%（CV 5-8%）在 ±10% 退出门下严重放大假信号。R3 初版「+10.2%」用一周前 baseline (3709 ns) vs 新窗口测量 (4087 ns)，同窗口对照后真实增量仅 +3.2%（差异分解：~70% 时窗校准 + ~30% 真实优化）。

**规则**：performance regression 验证**必须**在同时间窗内对照（stash 改动测 baseline → 还原测改动版 → diff），**不允许**跨日比对绝对数字。`bench/baseline/*.json` 仅作长期趋势监控，单次退化判定一律同窗口。

**完整 stash-swap 协议**（含 ninja 时间戳偏差防护补丁）：

```bash
# Step 1: stash 改动测原 baseline
git stash push -u
sleep 10
cmake --build build-release --target <bench>     # 防 stash 后 .o 比源新致 ninja 跳过
./build-release/<bench> --benchmark_filter='Target' \
  --benchmark_repetitions=10 --benchmark_report_aggregates_only=true \
  --benchmark_min_time=0.2s > /tmp/baseline_in_window.txt

# Step 2: stash pop 还原 + 强制重编（关键防 ninja 时间戳跳过）
git stash pop
touch <changed_src>...           # 强制 mtime 防 ninja 跳过重编（多文件多次 touch）
cmake --build build-release --target <bench>

# Step 3: 测改动版（同窗口）
sleep 10
./build-release/<bench> --benchmark_filter='Target' \
  --benchmark_repetitions=10 --benchmark_report_aggregates_only=true \
  --benchmark_min_time=0.2s > /tmp/changed_in_window.txt

# Step 4: diff（同窗口 cv 通常 5-8%，跨窗口 8-15%）
python3 -c "..." /tmp/baseline_in_window.txt /tmp/changed_in_window.txt
```

**判定准则**：

- 同窗口 CV ≤ 8% 视为可信（vs §7 跨窗口 CV ≤ 2% 协议不再适用）
- mean / median 双指标交叉，单指标偏移落 stddev 范围内 → 接近统计不显著
- 不允许跨日 / 跨重启 / 跨编译器升级比对绝对数字

**ninja 时间戳偏差 — 关键防护**：

> 反复模式「测试隔离问题」第 9 次新变体（TASK-20260426-01 R4 实证）：git stash + 构建产物 `.o` 时间戳 > stash 后还原的源码 → ninja 跳过重编 → 测试始终用旧 binary，输出迷惑性 0 改动报告。

**强制 Step 2 包含 `touch <changed_src>` + `cmake --build` 全图重建**；多文件改动 `touch` 全部关键 src（或用 `cmake --build --target <lib> --clean-first`）。

**TASK-20260426-01 实证**：R3 V1 优化后 stash-swap 同窗口 mean +3.2% / median +3.4%（跨窗口曾误判 +10.2%）+ R4 同窗口 mean -3.6% / median +2.65%（跨窗口曾不可知）。两次实证「stash-swap 同窗口对照」是 ±10% 退出门唯一可靠手段。

**交叉引用**：`memory-bank/systemPatterns.md`「同窗口对照 bench — performance regression 验证范式」段包含完整范式与 ninja 时间戳偏差 rationale。

### 7.1 Cache-targeted BM 稳态访问模式数学推演清单（cache/memoization 优化任务**必填**）

> 反复出现（TASK-20260424-04 P4 实证）：`BM_*_RoundRobin` 原 plan 声明 "pool=256 + cap=128 + linear → 50% hit"，实测稳态是 **100% miss**（第一轮 evict 128..255 替换 0..127，第二轮反向）。Plan 阶段未做稳态推演，下游 BM 语义与命名背离 → Phase 4 临时调整 pool size，虽未影响门槛但削弱规划精度。

**规则：** Plan 阶段设计 cache-targeted BM 时，**必须**在 plan §BM 定义段附带一张「稳态访问推演表」，列出 ≥ 3 轮迭代，明示稳态 hit rate。

**推演模板：**

```markdown
#### BM_Name 稳态推演（cap=C, pool=P, policy=FIFO, access=linear）

| 轮次 | access index 区间 | cache 内容（evict 后） | hit rate |
|:-:|---|---|:-:|
| 1 | 0 .. P-1 | 末 C 个：[P-C .. P-1] | 0% |
| 2 | 0 .. P-1 | 末 C 个：[P-C .. P-1]（若 P > C 线性替换） | [推演结果] |
| 3+ | ... | ... | [稳态值] |

**稳态结论：** [XX% hit / XX% miss]，与 BM 命名 `Name` 语义一致性 ✅/❌
```

**常见稳态结论速查（FIFO cache, linear round-robin access）：**

| cap vs pool | 稳态 hit rate | 适用语义 |
|---|:-:|---|
| `pool == cap` | **100% hit**（第 2 轮起 cache 恰好包含全 pool） | 压 full-cache scan cost |
| `pool < cap` | **100% hit**（第 2 轮起） | 同上，但未压满容量 |
| `pool > cap`，linear | **0% hit**（每轮 evict 都是下一轮要用的） | 压 miss + insert overhead |
| `pool > cap`，pseudo-random | `~cap / pool`（均匀 workload 近似） | 真实混合负载模拟 |
| `pool == 2 × cap`，每 text 连续访问 2 次 | **50% hit**（第 2 次必命中） | 短局部性模拟 |

**验证：** 若 BM 实测 hit rate 与推演结果偏差 >5%，plan 阶段的访问模式假设错误 → Phase 开始前必须修正 pool/access 参数或更名 BM。

**反模式：**
- ❌ Plan 直接声明 "50% hit" / "80% hit" 不附推演 → 容易与 FIFO/LRU + linear/random 交互产生非直觉稳态
- ❌ BM 命名含 "RoundRobin" / "MixedHit" 却未定义访问模式数学模型 → 下游 reader 误读测量语义

**交叉引用：** `memory-bank/systemPatterns.md`「Env Toggle A/B 对照模式」段（env toggle 可作为稳态推演的事后验证手段：`VX_<FEATURE>_OFF=1 ./bench` 应与 0% hit 相同量级）。

### 8. 编译器已做优化识别 — 位运算/除法近似反模式（优化类任务**必填**）

> 反复出现升级 P1（TASK-20260424-03 P5 实证）：手写 `/常量 → (x * magic + round) >> shift` 位运算近似在**标量路径**几乎必然输给 GCC/Clang 原生优化。

**根因：** GCC 自 4.x / Clang 自早期版本已对所有编译期常量除法自动应用 **Granlund-Montgomery 魔数乘法**（imul + shr，2 instructions），而手写 add-shift 链通常 3 adds + 2 shifts = 5 instructions 且破坏 u8↔u32 扩展优化。

**规则：** plan 阶段任何下列候选**必须**先 godbolt 确认 GCC `-O3` 原生输出：

| 候选模式 | 原代码 | 典型手写"优化" |
|---------|------|--------------|
| 常量除法 | `x / 255` | `(x * 257 + 32768) >> 16` |
| 常量模 | `x % N`（N 常量） | `x - (x * magic >> shift) * N` |
| 常量乘除组合 | `x * C / D`（C, D 常量） | 自选魔数 |

**验证命令（plan Phase 0 或候选筛选阶段必做）：**

```bash
# godbolt CLI（如可用）
echo 'unsigned f(unsigned x) { return x / 255; }' | \
  gcc -O3 -S -x c - -o - | grep -E 'imul|shr|mul'
# 预期：imul $0x80808081, ... + shr $0x7, ...
# → Granlund-Montgomery 已应用；手写近似极可能倒退
```

**例外：SIMD 场景值得手写** —— 编译器无法 auto-vectorize 到 `pmulhuw` / `_mm_mulhi_epu16` / NEON `vrshrn_n` 等 SIMD-specific 指令时，手写 per-lane 魔数近似仍是真正加速点。仅**标量路径**手写必然输给编译器。

**结论落地：** plan §候选表增加「**`-O3` 原生输出已确认不含魔数乘法？**」列；未确认列为「高风险」并降优先级；SIMD 候选无需此检查。

**交叉引用：** `memory-bank/systemPatterns.md`「已验证的模式（来自 TASK-20260424-03）」段「编译器已做优化识别 — 位运算近似反模式」包含完整反例与 Phase 5 回退数据。

### 9. Layout 类任务必检项（涉及 `layout_engine.cc` / `LayoutBox` / `LayoutType` 任务**必填**）

> 反复模式「非默认路径遗漏验证」第 5 次升级 P0（TASK-20260426-01 R4 实证）：vertical-align 系统性 sign error / LayoutInline 默认 fill-available 反 IFC 直觉 / inline-block atomic 显式 height 失效 共 3 项 build 阶段才发现，creative 阶段未画好坐标约定 + 默认值边界。

#### 9.1 跨 LayoutType 共用样式属性必须每路径独立 box-model 解析（**P0**）

**规则**：当 `display: X` 在 BuildTree 阶段映射成 `LayoutType::Y`（如 inline-block → kInline / block → kBlock / flex → kFlex）时，Layout::Y **必须**独立完成 box-model（width/height/padding/border/margin）解析；plan §0 必须显式列举每种 LayoutType 的 box-model 解析约束表：

| `display` | `LayoutType` | 必须独立解析的 box-model 属性 |
|---|---|---|
| `block` | `kBlock` | width / height（含 auto/explicit）|
| `inline-block` | `kInline`（atomic） | **width / height（atomic 高度边界）+ ascent / descent** |
| `inline` | `kInline` | width = max line.end_x（fit-content 默认）/ ascent/descent 来自 child |
| `flex` | `kFlex` | width / height + main / cross axis |

**TASK-20260426-01 实证**：vx BuildTree 把 `display: inline-block` 映射成 `LayoutType::kInline` → R4 初版 LayoutInline 不读 style.height → atomic ascent = border_box_height = 0 → vertical-align 关键字物理位置失效（RED2 测试触发）。

#### 9.2 默认值边界必须 plan 阶段锁定（**P1**）

**规则**：plan §3 设计决策**必须**为每条 layout 算法显式锁定：

- 默认 width 语义（fit-content vs fill-available）
- 默认 height 语义（auto vs explicit vs zero-fallback）
- 隐式 strut（implicit anchor）的来源（parent ascent/descent vs context-specific）

**反模式**：plan 仅写「实现 LayoutInline」不规定默认 width 语义 → build 阶段实证发现「default fill-available 反 IFC 直觉，nested inline child 永远撑满父宽度，相邻 child 必然换行」。

**TASK-20260426-01 实证**：R4.6 LayoutInline 默认 `content_width = containing_width` 致 `A11_4_VerticalAlignLengthOffsetMovesUp` 触发：up_span->y 反超 base_span->y；改 fit-content (max line.end_x) 才修正。

#### 9.3 Mixed TDD D3 类回归测试默认含反向探针（**P1 强制条目**，反复 5 次升级）

> 累计 5 次实战零误报（TASK-11 D3 + TASK-24-01 default block + TASK-24-03 三层 SIMD + TASK-25-01 :hover + TASK-20260426-01 4 次完整循环）。

**规则**：Mixed TDD 模式下「为预防特定 bug 而新增的回归测试」（D3 类，含反向断言 / 不变量验证）**必须**配反向探针验证（临时破坏实现确认 FAIL → 恢复确认 PASS）；不可省略，不可推迟。

**判定标准**：以下场景视为 D3 类必加反向探针：

- 验证 enum 值之间区分（`EXPECT_NE(parse("sub"), parse("super"))` 类）
- 验证不变量（如 `Collapsed() = max(pos) + min(neg)` 反对单边 max/min）
- 验证黑名单 / 白名单（实现可能被自然丢弃路径 over-match）
- 验证「不应发生」（`EXPECT_LT/GT` 边界对比）

**操作步骤**（写入 plan 任务模板）：

```markdown
### 任务 N：[组件名] [TDD]
- [ ] RED 单元测试（含 D3 类反向断言 N 项）
- [ ] **反向探针**：临时改实现为 placeholder（如 `return false;` / `return max_positive;`）
- [ ] 跑测试 → 确认 D3 类测试 FAIL（其他测试可不受影响）
- [ ] 恢复实现 → 跑测试 → 确认全 PASS
- [ ] 记录反向探针结果到 progress.md（"实现已正确时也能精准 FAIL"）
- [ ] GREEN 实施
```

**交叉引用**：`memory-bank/systemPatterns.md`「Mixed TDD RED 反向探针实践」段。

#### 9.4 递归算法 API 传递语义决策必检项（**P0 强制**，TASK-20260430-01 升级）

> 反复模式「前置依赖/环境/API 能力未验证」第 9+ 次显化新维度（TASK-20260430-01 D2 实证）：plan 阶段锁定 `MarginChain` 为 by-value in / by-value out（POD 12B），build P3 实施 first-child margin propagate 三层递归（grandparent → parent → child）时立即发现：grandchild 修改 chain 后父层物理化拿不到累积值，by-value 在递归边界天然失效。根因 = plan mental walkthrough 仅推演单层「caller → callee」，未推演 ≥3 层「Level N → N+1 → N+2」+ 「caller 是否需要看到 callee 的修改」。

**触发条件**：算法在以下任一情形下涉及 API 传递语义决策（by-value / by-ref / by-pointer / in-out / 返回值），plan / creative 阶段**必填**多级递归 mental walkthrough：

- 算法本身递归（layout / parser / event bubble / clean-up cascade / DOM tree walk）
- 跨函数边界传递「累积态 / chain / cursor / accumulator / state」
- 同一参数同时承担 input + output 两种角色（in-out 候选）

**强制产出 3 项 mental trace**：

1. **≥3 层最小递归路径** — 写出 `Level N caller → Level N+1 → Level N+2` 的具体调用链（含函数名 + 参数）

   范例：`LayoutBlock(root) → LayoutBlockChild(child1) → LayoutBlockChild(grandchild)`

2. **每层共享状态修改清单** — 列出每层是否会修改共享状态（chain / accumulator / cursor / counter）：

   | 层级 | 函数 | 修改的共享态 | 修改类型（read / write / read-modify-write）|
   |---|---|---|---|
   | N | LayoutBlock | cur_chain | rmw（合并 sibling outgoing）|
   | N+1 | LayoutBlockChild(child) | incoming_chain | rmw（first-child propagate）|
   | N+2 | LayoutBlockChild(grandchild) | incoming_chain | rmw（grandchild 也是 first-child）|

3. **caller 可见性判定** — 每层调用站点回答「callee 完成后，caller 是否需要看到 callee 的修改？」

   - **答 yes** → by-value **必然失效**；必须 by-pointer / by-reference / 返回值显式回传
   - **答 no** → by-value 等价 by-ref（同函数内变量），可保留
   - **混合**（部分子调用 yes / 部分 no）→ 选 in-out by-pointer + 显式 return；by-value 不能覆盖

**判定矩阵**（plan §决策表必填一行）：

| 候选 | 单层 mental trace | 三层 mental trace | 选哪个？|
|---|---|---|---|
| 仅修改本层局部变量 | by-value ✅ | by-value ✅ | by-value（最简）|
| 调用 callee 修改后本层需读 | — | by-pointer / by-reference | **不能 by-value** |
| callee 修改 + 本层物理化时读累积 | — | in-out by-pointer + return | **不能 by-value** |

**反模式**（plan 阶段必拦截）：

- ❌ 仅画「caller → callee」单层箭头图就锁定 by-value
- ❌ 推演 callee 修改但忽略「caller 物理化阶段是否需读」
- ❌ 把 POD struct 大小（12B / 16B / 24B）作为 by-value 唯一依据 — 编译器 SROA 优化与「跨函数边界 propagate 是否成立」是两个独立问题

**TASK-20260430-01 实证**：D2 plan 锁 `MarginChain incoming`（by-value，理由：POD 12B + 编译器 SROA 等价无开销） → build P3 三层递归立即失效（grandchild 修改 chain 后 parent 物理化时 chain 已丢失）→ 改 in-out by-pointer (`MarginChain* incoming`) + return MarginChain；调用站点 `LayoutBlockChild(child, w, ctx, &cur_chain)`；零 ABI 风险（POD 12B），编译器仍可 SROA 优化。修改成本：签名 + 全部 callsite。

**与 §9.1 / §9.2 协同**：§9.1 锁定 box-model 跨 LayoutType 独立解析；§9.2 锁定默认值边界；本节 §9.4 锁定**跨函数 chain 传递语义**，三者覆盖 layout 类算法 plan 阶段 API 设计完整边界。

**交叉引用**：`memory-bank/systemPatterns.md`「递归算法 API 传递语义决策必检项」段；`.cursor/commands/creative.md` 「6.algorithm 段算法伪码必须明确赋值 vs 合并语义」段（同源 reflection §6 建议 #4，强制配套使用）。

---

## 小块任务粒度

**每一步是一个动作（2-5 分钟）：**
- "写失败测试" — 一步
- "运行确认它失败" — 一步
- "实现最少代码使测试通过" — 一步
- "运行测试确认通过" — 一步
- "提交" — 一步

## 计划文档头部

每个计划**必须**以此头部开始：

```markdown
# [功能名称] 实现计划

**目标：** [一句话描述构建什么]

**架构：** [2-3 句话描述方法]

**技术栈：** [关键技术/库]

**复杂度级别：** [Level 1-4]

---
```

## 附录：LOC 估算 — 隐性附加工作类型清单 + ×1.3-1.5 buffer 范本

> **TASK-20260503-02 反思 §6 #4 + TASK-20260503-04 P2 #1 同源合并**：plan 阶段估算 LOC（行数）经常仅计「核心代码」/ 漏掉同步产生的「隐性附加工作」LOC → build 阶段实际 LOC 比 plan 估算多 1.3-1.5 倍 / 估时同步漂移。

### 触发条件

涉及以下任一情形时**必填** ×1.3-1.5 buffer：

- plan 涉及新写测试（`xxx_test.cc` + 隐性 fixture / mock setup / TEST_F 样板）
- plan 涉及新增公开 API / C ABI（隐性 doc string + commit-friendly 注释 + Source 溯源元数据）
- plan 涉及新增子模块（隐性 CMakeLists.txt 调整 + include 路径 + 链接配置）
- plan 涉及反向探针（隐性 comment policy 注释 + DEAD-CODE 标记）
- 任意 plan 估时 ≥ 30 min 的子任务（buffer 防御）

### 隐性附加工作类型清单（7 类常见）

| # | 类型 | 隐性 LOC 比例 | 范例 |
|:-:|---|:-:|---|
| 1 | **Doxygen / API doc string** | +5-15% | 公开 C ABI 函数顶部 `/** */` 块（每函数 ~5-15 行）|
| 2 | **commit-friendly 注释扩展** | +3-8% | `// TASK-XXXXXXXX-XX: <reason>` 类元数据注释（reflect 阶段沉淀的扩展注释）|
| 3 | **test fixture / mock setup** | +20-40% | `class FontManagerTest : public ::testing::Test { ... }` setup / teardown / 共享 mock |
| 4 | **atomic counter / observer 接口** | +5-15% | `std::atomic<size_t> counter_; size_t counter() const { return counter_.load(); }` / friend test class |
| 5 | **反向探针注释 / comment policy** | +1-3% | `// DEAD-CODE: <input /> 已废弃` / panel HTML 头部 comment policy |
| 6 | **lazy-attach contract 注释** | +2-5% | C ABI 实现文件每个函数顶部 `// lazy-attach: VX_ERROR_INVALID_STATE if XXX uninitialized` |
| 7 | **CMakeLists.txt / 构建系统调整** | +3-8% | `target_link_libraries` PUBLIC/PRIVATE 调整 + `add_test(NAME ... CONFIGURATIONS DEVTOOL_ON)` 配置 |

### ×1.3-1.5 buffer 范本

```markdown
## 估时

| Phase | 核心 LOC（plan 估）| 隐性 LOC（×1.3-1.5）| 实际 LOC（buffer 后）| plan ×0.6 估时 |
|---|:-:|:-:|:-:|:-:|
| Phase A | 200 | +60-100 | 260-300 | ~30-45 min |
| Phase B | 150 | +45-75 | 195-225 | ~25-35 min |
| Phase C | 80  | +25-40 | 105-120 | ~15-20 min |
| **总计** | **430** | **+130-215** | **560-645** | **~70-100 min** |
```

**说明：**

- ×1.3 = 保守 buffer（核心代码占主导 / 隐性工作可控）
- ×1.5 = 激进 buffer（含 ≥ 3 类隐性附加工作 / 测试密集 / API 密集）
- 选择系数依据：核心 vs 隐性比例 + 反复模式预警程度

### 反模式

- ❌ plan 仅写核心 LOC 估算 / 隐性附加工作"等到 build 阶段再说"（破坏估时准确性）
- ❌ buffer 系数固定 ×1.0（忽略隐性工作 / 经验上至少 ×1.2）
- ❌ buffer 系数固定 ×2.0（过度膨胀 / 估时失真 / 不利于反复模式 #6 提交粒度抑制）
- ❌ 估时与 LOC 解耦（plan ×0.6 vs LOC 必须有明确换算关系）

### 实证

- TASK-20260503-02：plan 估 6 子项 ~250 LOC → 实际 ~340 LOC（×1.36 / 隐性 commit body 元数据 + reflect 注释）
- TASK-20260503-04：plan 估 ~400 LOC → 实际 ~560 LOC（×1.40 / 隐性 fixture + lazy-attach contract 注释）

### 交叉引用

- 本规则「公开 API testability 检查清单」段（testability 接口是隐性附加工作类型 4「atomic counter / observer」常见来源）
- 本规则「中文文档 StrReplace 字符类型 audit」段（隐性附加工作类型 5「comment policy」同源）
- `.cursor/rules/skills/git-workflow.mdc`「commit body Source 溯源 + 实测数据格式」段（commit-friendly 注释扩展同源）

## 测试模式选择

计划中的每个任务必须标注测试模式。根据代码现状选择：

| 模式 | 标记 | 适用场景 | 核心流程 |
|------|------|---------|---------|
| **TDD 模式** | `[TDD]` | 功能未实现，从零开始 | RED → GREEN → REFACTOR |
| **覆盖补充模式** | `[覆盖补充]` | 功能已实现但缺少测试 | 写测试 → 验证通过 → 验证有效性 → 补充边界 |

**判断方法：** 如果实现代码已存在（来自之前的任务或分支），测试会立即通过 → 使用覆盖补充模式。

## 蓝图任务子任务规格化深浅梯度（V2=a 蓝图任务 / 含 N ≥ 3 实施子任务时**必填**）

> **TASK-20260505-03 反思 §3.d #3 + §4 #5 实证**：18 实施子任务全做完整规格化 → plan 文档 ~770 行（其中 ~600 行是子任务规格）/ 但其中 ~14 个子任务实际后续会独立立项 + 重做 Phase 0 audit + brainstorm → 完整规格化部分浪费。

### 触发条件

蓝图任务（V2=a）含 ≥ 3 个实施子任务时**必填**：

- 每个子任务标注规格化深度（🟢 完整 / 🔵 概要）
- plan §8「实施子任务清单」段顶部必须含本规则的「深浅梯度边界明示」声明

### 🟢 完整规格化（首批 1-3 子任务 / 直接基于本规格立项）

**适用范围：** 首批 1-3 个最优先实施的子任务（直接进入下一个 Level 3 实施任务）

**规格化深度要求：**

- ✅ 完整文件结构清单（每文件职责）
- ✅ 完整代码片段（≥ 60% 实际代码 / 含函数签名 + 核心逻辑）
- ✅ TDD 测试用例完整列表（红绿循环逐项）
- ✅ ctest 期望（add_test 命令 / config 矩阵）
- ✅ commit body 完整范本（8 段全填）
- ✅ Phase 0 audit 完整结果

**LOC 占比：** 单子任务 ~100-200 行 plan 文档

### 🔵 概要规格化（其余子任务 / 仍需独立立项 + Phase 0 audit + brainstorm）

**适用范围：** 第 4 个及之后的实施子任务（独立 Level 3 立项 / 不直接进入实施）

**规格化深度要求：**

- ✅ 子任务名称 + 范围声明（一段话）
- ✅ 主要文件清单（无完整结构 / 仅文件名）
- ✅ 关键决策点（待独立立项时再 brainstorm）
- ✅ 估时（plan ×0.6）
- ❌ **不必填**完整代码片段
- ❌ **不必填**TDD 用例完整列表
- ❌ **不必填**commit body 范本
- ❌ **不必填**Phase 0 audit（独立立项时再做）

**LOC 占比：** 单子任务 ~15-30 行 plan 文档

### plan §8 边界明示模板

```markdown
## 8. 实施子任务清单（N 个 / 深浅梯度规格化）

> **规格化深度梯度：** 子任务 1-3 为 🟢 完整规格化（直接基于本规格立项）/ 子任务 4+ 为 🔵 概要规格化（独立 Level 3 立项时再 Phase 0 audit + brainstorm）

### 子任务 1 [🟢 完整]
[~100-200 行完整规格 / 含代码 + TDD + ctest + commit body]

### 子任务 2 [🟢 完整]
[~100-200 行完整规格]

### 子任务 3 [🟢 完整]
[~100-200 行完整规格]

### 子任务 4 [🔵 概要]
**范围：** [一段话声明]
**主要文件：** [文件名清单]
**关键决策点：** [待独立立项时 brainstorm]
**估时（plan ×0.6）：** [N min]

### 子任务 5 [🔵 概要]
[~15-30 行概要]
...
```

### 反模式

- ❌ 全部子任务做 🟢 完整规格化（plan 膨胀 / 浪费 / 后续独立立项时还要重做 Phase 0 + brainstorm）
- ❌ 全部子任务做 🔵 概要规格化（首批子任务无足够规格 / 实施时还要做完整 brainstorm）
- ❌ 子任务规格化深度未声明（plan 读者不知道哪些是直接实施 / 哪些是后续独立立项）
- ❌ 蓝图任务直接产出 N × Level 3 子任务 plan（应在 V2=a 蓝图阶段做深浅梯度 / 实施阶段独立立项）

### 实证

- TASK-20260505-03（first-evidence）：18 子任务（GLES 蓝图）/ 全做 🟢 完整规格化 → ~770 行 / 其中 ~14 子任务实际不会基于本规格直接立项 → 浪费 ~280 行
- 改进：首批 1-3 完整 / 4+ 概要 → 预期 ~370 行（节省 50%）+ 后续独立立项时 Phase 0 audit 仍可做（不丢失质量）

### 交叉引用

- `.cursor/rules/main.mdc`「Level 4 蓝图任务 V2=a 工作流变体」段（V2=a 蓝图任务范式）
- `memory-bank/systemPatterns.md`「V2=a 蓝图任务范式 triple-evidence」段（V2=a 范式协议）
- `memory-bank/systemPatterns.md`「V2=a 蓝图任务文档密度系数 1.0-1.4×」段（plan 文档膨胀控制 / 与本规则深浅梯度协同）

## TDD 模式任务模板 [TDD]

```markdown
### 任务 N：[组件名称] [TDD]

**文件：**
- 创建：`exact/path/to/file.py`
- 修改：`exact/path/to/existing.py`
- 测试：`tests/exact/path/to/test.py`

- [ ] **步骤 1：编写失败测试**
  [具体测试代码]

- [ ] **步骤 2：运行测试验证失败**
  运行：[具体命令]
  预期：FAIL

- [ ] **步骤 3：编写最少实现**
  [具体实现代码]

- [ ] **步骤 4：运行测试验证通过**
  运行：[具体命令]
  预期：PASS

- [ ] **步骤 5：提交**
  [具体 git 命令]
```

## 覆盖补充模式任务模板 [覆盖补充]

```markdown
### 任务 N：[组件名称] [覆盖补充]

**文件：**
- 已有：`exact/path/to/existing_impl.cpp`（已实现，缺少测试）
- 测试：`tests/exact/path/to/test.cpp`

- [ ] **步骤 1：编写测试覆盖正常路径**
  [具体测试代码]

- [ ] **步骤 2：运行测试验证通过**
  运行：[具体命令]
  预期：PASS（实现已存在）

- [ ] **步骤 3：验证测试有效性**
  临时在实现中注入缺陷（如注释掉关键逻辑），确认测试能捕获失败。
  验证后恢复实现代码。

- [ ] **步骤 4：补充边界和异常用例**
  [边界条件测试代码]

- [ ] **步骤 5：运行全部测试验证通过**
  运行：[具体命令]
  预期：PASS

- [ ] **步骤 6：提交**
  [具体 git 命令]
```

## 安全任务模板

当任务涉及安全需求（在 `/van` 中标注为 `[安全相关]`）时，在计划中包含安全任务：

```markdown
### 任务 N：安全 — [安全需求名称]

**文件：**
- 修改：`exact/path/to/handler.py`
- 测试：`tests/exact/path/to/test_security.py`

- [ ] **步骤 1：编写安全测试**
  输入验证测试（恶意输入被拒绝）、权限校验测试、边界值测试

- [ ] **步骤 2：运行测试验证失败**
  运行：[具体命令]
  预期：FAIL（缺少安全措施）

- [ ] **步骤 3：实现安全措施**
  [具体安全实现代码]

- [ ] **步骤 4：运行测试验证通过**
  运行：[具体命令]
  预期：PASS

- [ ] **步骤 5：依赖安全审计**（如果新增了依赖）
  运行：[npm audit / pip audit / 等]
  预期：无 CRITICAL 或 HIGH 漏洞

- [ ] **步骤 6：提交**
```

## 要点

- 始终给出确切的文件路径
- 计划中包含完整代码（不是"添加验证"）
- 确切的命令和预期输出
- DRY、YAGNI、TDD、频繁提交

## 执行交接

保存计划后：

**"计划完成并保存到 `docs/plans/xxx.md`。准备执行？"**

- 如果有子代理能力 → 使用子代理驱动开发
- 如果没有 → 在当前会话中使用 `/build` 命令执行

## 与 Memory Bank 集成

- 保存计划后更新 `memory-bank/tasks.md`
- 在 `memory-bank/activeContext.md` 中记录计划状态
- 在 `memory-bank/progress.md` 中跟踪进度
