# MVP-scope 文档实施计划

**文档 ID：** `2026-05-04-mvp-scope`
**任务 ID：** `TASK-20260504-01`（Level 4 蓝图 V2=a 完整变体）
**对应 spec：** `docs/specs/2026-05-04-mvp-scope.md`
**创建日期：** 2026-05-04
**状态：** Plan 阶段（待用户审查 + commit 1 落盘）

---

## 1. 任务概述

为 Veloxa 建立**统一的 MVP 范围定义** + **可量化验收清单** + **三档分级矩阵**，本任务为 Level 4 蓝图 V2=a 完整变体（含 creative ×3 不含 build）。

主交付物：
1. `docs/specs/2026-05-04-mvp-scope.md`（~480 行 / 12 段 — 已落盘 ✅）
2. `docs/plans/2026-05-04-mvp-scope.md`（本文档 / ~400 行 — 与 spec 同 commit 1）
3. `memory-bank/creative/creative-mvp-A.md`（~300-400 行 / commit 2）
4. `memory-bank/creative/creative-mvp-B.md`（~400-500 行 / commit 3）
5. `memory-bank/creative/creative-mvp-C.md`（~500-600 行 / commit 4）
6. README.md + projectbrief.md + productContext.md 同步 + MB sync（commit 5 finalize）

**预计总产出：** ~2000-2500 行文档 / 5 commits / standalone-AI 实测条件下 plan ×0.6 ~120-180 min。

---

## 2. 已锁定决策（11/11 用户 1 次 AskQuestion all_recommended）

### 2.1 VAN 阶段 V 系列 5 维度

| # | 维度 | 决策 | 锁定时间 |
|:-:|---|---|:-:|
| V1 | MVP 范围视角 | **D 三档分级 MVP-A/B/C** | VAN ✅ |
| V2 | 工作流形态 | **a V2=a 完整蓝图变体**（含 creative ×N 不含 build）| VAN ✅ |
| V3 | 文档形态 | **c 双管齐下**（spec + projectbrief 链接 + README 同步 + productContext 表）| Plan ✅ |
| V4 | 验收清单粒度 | **c 二者兼有**（粗粒度功能 + 细粒度 ctest + 配置矩阵）| Plan ✅ |
| V5 | 同步更新范围 | **c 全量同步**（4 处一致更新 + finalize commit 守门）| Plan ✅ |

### 2.2 Plan 阶段 B1-B7 决策

| # | 维度 | 决策 |
|:-:|---|---|
| B1 | 主 spec 文档结构 | 沿用 [DevTool 蓝图 spec §1-§12 范式](33afb7c)（12 段成熟模板）|
| B2 | creative ×N 拆分粒度 | **3 篇**（MVP-A/B/C 各一篇）|
| B3 | 验收清单矩阵格式 | 单矩阵 三档 × 多维度 |
| B4 | 与既有 spec 交叉引用规范 | 上游/下游分表（spec 附录 C 已完整）|
| B5 | 估时 | plan ×0.6 ~120-180 min（standalone-AI 实测条件）|
| B6 | commit 拆分粒度 | **5 commits**（plan+spec / creative-A / B / C / finalize）|
| B7 | Phase 0 grep 实证 | 7 子段（详 §3）|

### 2.3 跨决策协同度自检

**11/11 用户 1 次 AskQuestion 全 all_recommended 锁定** — 创纪录第 8 次跨决策协同度 100%（vs TASK-04 第 7 次 12/12）。

---

## 3. Phase 0 grep 实证（7 子段）

### 3.1 §0.1 ctest baseline（纯文档任务但作为 MVP-A 验收基线）

**实测：** DEVTOOL=ON 1284/1284 PASS / DEVTOOL=OFF 1091/1091 PASS（截至 [TASK-20260503-04 闭环 commit `33afb7c`](33afb7c) — 已记录）。

**判读：** ✅ 本任务**不修改代码** / 不影响 ctest baseline / spec §8.1 直接引用此 baseline 作 MVP-A 不退化标准。

### 3.2 §0.2 既有 spec / plan / archive 文件名冲突 grep

**实测：**
- `docs/specs/*MVP*.md` → 0 命中
- `docs/specs/*scope*.md` → 0 命中
- `docs/plans/2026-05-04-*.md` → 0 命中（本任务为今日首个）
- `memory-bank/archive/archive-TASK-20260504-*.md` → 0 命中
- `memory-bank/creative/creative-mvp-*.md` → 0 命中

**判读：** ✅ 全 7 个待新建文件路径**无冲突** / 可安全落盘。

### 3.3 §0.3 projectbrief.md 5 大目标完整性

**实测：** 5 大核心目标完整存在（轻量化 / 高性能渲染 OpenGL ES + Vulkan / Web 兼容子集 / 可嵌入性 / 开源开放）+ 目标平台 4 项（嵌入式 Linux 主要 / Linux Desktop / 车载 RTOS 远期 / Windows 次要）+ 目标场景 5 项（车载中控 / 仪表盘 / 工业 HMI / IoT 面板 / 医疗设备）。

**判读：** ✅ 作为 MVP 验收维度框架完整（spec §3 三档验收 + 附录 A 5 大目标映射 直接复用）。

### 3.4 §0.4 productContext.md 已实现表完整性

**实测：** 已实现核心功能表 14 项（HTML 解析器 / CSS 引擎 / Block-Inline-Flex 布局 / 软件渲染器 / 事件系统 / 脏区更新 / CSS Transitions / C API / QuickJS / border-radius / FreeType+HarfBuzz / PNG-JPEG / JS-DOM / SDL2）+ 6 大里程碑段（Layout 正确性 / DevTool A/B/C/D / QuickJS Interrupt Handler）。

**判读：** ✅ 作为 MVP-A/B 已完成基线完整（spec §3.1 + §3.2 + 附录 D 直接复用）。

### 3.5 §0.5 29 specs 历史 spec 完成度 audit

**实测：** 29 specs 全部已实施（按 archive 文件交叉对照 — 42 archives 覆盖 Foundation→DevTool 全周期）；`feature-completion-design.md §不在范围内` 8 项明确为非 MVP-A/B（per-corner border-radius / per-side 颜色 / WebP / 异步图片 / 双线性缩放 / DOM 节点 createElement-removeChild / JS modules / 中断处理器）— 其中**中断处理器已 [TASK-05 闭环 ✅](33afb7c)**（其余 7 项是 MVP-C C-G5/G6/G7 来源）。

**判读：** ✅ 29 specs 完成度清晰 / 8 项非 MVP-A/B 排除项中 1 项已闭环 7 项映射到 C-G5/G6/G7 / 无遗漏。

### 3.6 §0.6 activeContext.md P3 候选清单完整性

**实测：** P3 候选清单完整：
- DevTool 三件套主线收官 8 项后续候选（#35 阶段 2 / R9 EventManager / DomBindings R2 三连 / R3+ #1 image_decoder / TASK-30-04-D Console ✅ 已闭环 / TASK-30-04-E JS Debugger / TASK-30-04-F CDP / TASK-30-04-G UI 编辑器）
- TASK-20260503-03 P3 #0（Performance Overlay 持续 invalidate 机制）
- TASK-20260502-02 P3 #1+#2（#35 阶段 2 / R9 EventManager — 已合并到上方）
- R2 P3 候选 #1+#2+#3（DomBindings Element.children / addEventListener / innerHTML setter）

**判读：** ✅ 全部映射到 spec §3.2.1 MVP-B gap 表（4 项）+ §3.3.1 MVP-C gap 表（9 项）+ §10 扩展段（DevTool Phase E/F/G 超 MVP）/ 0 遗漏 0 重复。

### 3.7 §0.7 反复模式 #1 抑制（add_test config guard / JS ctx 归属虽不适用本任务但守门）

**实测（守门性 audit）：**
- **add_test config guard 边界假设审计**：本任务**不新增 add_test**（纯文档蓝图）→ 不适用（但 spec §8.2 已明示「未来子任务参考时强制做该 audit」延续 [P0 #1 升级落实](33afb7c)）
- **JS context 归属 audit**：本任务无 JS context 改动 → 不适用
- **前置组件能力 / 现有实现 runtime 行为假设**：本任务无新代码改动 → 不适用
- **依赖包 / 工具链是否存在**：零新依赖 → 不适用

**判读：** ✅ 反复模式 #1 全 4 子维度均不适用本任务 / 但 spec 中已交叉引用 [TASK-04 沉淀的 audit 子条](33afb7c) 让未来子任务能复用 / 守门完整。

### 3.8 Phase 0 总判读

**ROI：** Phase 0 投入 ~5 min（7 子段实证 / 大部分为已知数据复用） / 节省风险 = (1) 文档名冲突避免 / (2) 上游素材完整性确认 / (3) gap 清单 0 遗漏 0 重复（避免 reflection 阶段补 plan-fact reconcile）/ (4) 反复模式 #1 守门连续生效。

**Phase 0 投入定律 sept-evidence 升级候选：** A 5.3× / B 5.2× / C 7.6× / 02 6.7× / 03 8.0× / 05 16× / 04 ~6-10× / **本任务 ~3-5×（蓝图任务范围较小所以 ROI 偏低）** = 7 次实证 sept-evidence 维持。

---

## 4. 子任务分解（5 commits）

### 4.1 子任务 1：Plan + Spec 同步落盘（commit 1）

**已就绪文件（待 commit）：**
- ✅ `docs/specs/2026-05-04-mvp-scope.md`（~480 行 / 12 段 / 已落盘）
- ✅ `docs/plans/2026-05-04-mvp-scope.md`（本文档 / 待写完）
- 🟡 MB 三件套（tasks/activeContext/progress）状态更新阶段「初始化」→「规划中」

**commit message 范本：**

```
chore(plan): TASK-20260504-01 plan + spec + Phase 0 7-segment grep audit done

Source: TASK-20260504-01 plan §3 Phase 0 7-segment grep audit +
        spec §1-§12 V1=D 三档分级 MVP-A/B/C blueprint.

11/11 user decisions all_recommended locked (V1+V3+V4+V5+B1-B7) —
8th consecutive cross-decision协同度 100% record (vs TASK-04 7th 12/12).

Spec (docs/specs/2026-05-04-mvp-scope.md, ~480 lines):
- 12-section structure (沿用 DevTool 蓝图 spec §1-§12 范式)
- §3 三档分级验收标准 A.1-A.6 / B.1-B.10 / C.1-C.10
- §3.2.1 MVP-B 4 known gaps + §3.3.1 MVP-C 9 known gaps
  (full sync from activeContext P3 候选清单 — 0 omission 0 duplicate)
- §6 三档完成度对照矩阵 + 推荐立项顺序 (10 candidates)
- §10 DevTool Phase E/F/G 超 MVP 标识 (是 plus 非 MVP)
- §11 与未来任务关系 (10 候选立项预排)
- §12 附录 A-D (5 目标映射 / 配置矩阵 / 既有 spec 交叉引用 /
  productContext sync 表)

Plan (docs/plans/2026-05-04-mvp-scope.md, ~400 lines):
- §3 Phase 0 7-segment grep audit (文件名冲突 / 5 目标完整性 /
  productContext / 29 specs / activeContext P3 / 反复模式 #1 守门)
- §4 5 commits 子任务分解 + 5 文件落盘范围
- §6 风险登记 R1-R4
- §7 反复模式预防清单
- §8 ≥ 11 systemPatterns 协同度自我对照
- §9 2 Checkpoint (CP1 spec+plan 落盘 / CP2 creative×3 落盘)

Memory Bank 三件套阶段「初始化」→「规划中」.

DEVTOOL=ON 1284 / DEVTOOL=OFF 1091 baseline 不退化（纯文档任务 /
零代码改动）.
```

**预计耗时：** ~2-5 min（standalone-AI / Edit + Write 瞬时）

---

### 4.2 子任务 2：creative-mvp-A.md 详细设计（commit 2）

**新建文件：** `memory-bank/creative/creative-mvp-A.md`（~300-400 行）

**内容结构：**

```markdown
# Creative — MVP-A 最小可运行 demo 详细设计

## 1. 设计目标
- baseline 状态保持机制（防退化）
- 嵌入式 boot 屏 / 简单状态显示场景的最小 spec

## 2. 验收标准 A.1-A.6 详细落地
（每条验收标准的 ctest / smoke 标识符 + 维护协议）

## 3. baseline 不退化守门机制
- ctest 1284 ON / 1091 OFF 红线
- A14 链接闭包零字节增长 nm verify
- 任何后续任务 commit body 必含 "MVP-A baseline X/X PASS" Source 段

## 4. 推荐 example 范例（hello_mvp_a.cc 占位 / 非本任务实施）
- 极简静态 HTML/CSS 渲染示意

## 5. 与既有 example 关系
- hello.cc → MVP-A baseline 范例
- hello_sdl2.cc → MVP-A.5 SDL2 后端范例

## 6. 安全考量
- MVP-A 是基线档 / 无新外部输入 / 无新威胁面
- 沿用既有 5 项 mitigation（T2 / T3 / T5 / T6 / T7）
```

**commit message 范本：**

```
docs(creative): TASK-20260504-01 creative MVP-A blueprint

Source: TASK-20260504-01 spec §3.1 MVP-A baseline 档 detailed design.

MVP-A = baseline 状态保持档（已 100% / A.1-A.6 全 ✅ / ctest 1284 ON
+ 1091 OFF / A14 0 字节增长 ✅）—— 后续任何任务 commit body 必含
"MVP-A baseline X/X PASS" Source 段守门，防退化。
```

**预计耗时：** ~3-8 min

---

### 4.3 子任务 3：creative-mvp-B.md 详细设计（commit 3）

**新建文件：** `memory-bank/creative/creative-mvp-B.md`（~400-500 行）

**内容结构：**

```markdown
# Creative — MVP-B 桌面 dogfood 完整 详细设计

## 1. 设计目标
- HMI 原型开发 / Demo 演示 / 第三方桌面 evaluation
- 完整 dogfood 工作流（编辑 → DevTool inspect → perf overlay → hot reload → console eval → SDL2 验证）

## 2. 验收标准 B.1-B.10 详细落地
（每条验收标准的 ctest / smoke / dogfood 路径）

## 3. 4 项 gap 详细补全方案

### B-G1+G2+G3 DomBindings R2 三连补全（合并 Level 3 任务）
- API 设计：Element.children / addEventListener / innerHTML setter
- 复用范式：Phase A/B/C dom_bindings.cc 三次扩展经验
- 估时校准：~2-4 h plan ×0.6（archive §6 -30%）

### B-G4 Performance Overlay 持续 invalidate 机制
- 路径 (b)：hello_devtool 注入 CSS animation（推荐先做 / ~30-60 min）
- 路径 (a)：vx_view_invalidate() 公开 C ABI（更通用 / ~1-2 h）

## 4. dogfood 工作流完整链路
- 编辑 → Hot Reload → Inspector → Performance Overlay → Console eval → SDL2 验证

## 5. 推荐 example 范例（hello_mvp_b.cc 占位 / 非本任务实施）
- 完整 HMI 原型示意（按钮 / 表单 / 图片 / hover 效果 / DevTool toggle）

## 6. 安全考量
- 完整 5+T 威胁面 mitigation（T1+T2+T3+T5+T6+T7）已就位
- isolated JSRuntime 协议 + capability allowlist + DomBindings host data channel 范式可复用
```

**commit message 范本：**

```
docs(creative): TASK-20260504-01 creative MVP-B blueprint

Source: TASK-20260504-01 spec §3.2 MVP-B 桌面 dogfood 完整 档 detailed
        design + activeContext.md P3 候选清单 4 项 gap 详细补全方案.

MVP-B = 桌面 dogfood 完整（~90%）—— 4 gap (DomBindings R2 三连
补全 + perf overlay 持续 invalidate) 总估时 ~2.5-6 h plan ×0.6.
```

**预计耗时：** ~5-10 min

---

### 4.4 子任务 4：creative-mvp-C.md 详细设计（commit 4）

**新建文件：** `memory-bank/creative/creative-mvp-C.md`（~500-600 行）

**内容结构：**

```markdown
# Creative — MVP-C 真嵌入式部署 详细设计

## 1. 设计目标
- 车载中控 / 仪表盘 / 工业 HMI / IoT 面板 / 医疗设备完整覆盖
- 核心目标 #2 (硬件加速) + #1 主要 (嵌入式 Linux) 硬指标闭环

## 2. 验收标准 C.1-C.10 详细落地

## 3. 9 项 gap 详细补全方案

### C-G1 OpenGL ES / Vulkan 硬件渲染后端（核心 P0 / Level 4 多 Phase）
- 架构占位：Surface 抽象扩展 (GL Surface)
- Renderer 抽象扩展（vs SoftwareCanvas）
- DisplayList → GL command translation
- shader 设计（基础 quad + glyph + image）
- 估时：plan ×0.6 ~30-60+ h

### C-G2 DRM/KMS 嵌入式后端（核心 P0 / Level 3-4）
- 复用 spec 2026-04-25-sdl2-window-backend §未来工作 接口（Surface::Present() 抽象 + 双轨 CMake 模板）
- 平台 factory 扩展 #if defined(__linux__) && VX_PLATFORM_DRM
- 估时：plan ×0.6 ~10-20 h

### C-G3 资源加载策略（P1 / Level 3 蓝图 + 实施）
- 三档：打包 / 文件系统 / 混合
- 推荐混合（编译期内嵌资源 + 运行时文件系统覆盖）

### C-G4 嵌入式 Linux 平台构建支持
- 工具链交叉编译 + sysroot 配置
- yocto / buildroot 集成范例

### C-G5 DomBindings 节点动态创建删除（feature-completion 排除项）

### C-G6 CSS 高级特性 5 项（5 个独立 Level 2-3 任务）

### C-G7 图像扩展 3 项（3 个独立 Level 2 任务）

### C-G8 R9 EventManager HitTest 改造

### C-G9 性能优化收口（多任务集合）

## 4. 推荐 example 范例（hello_mvp_c.cc / hello_drm.cc 占位）
- 真车载 HMI 完整示意 + 60fps 验证

## 5. 安全考量
- 完整保留 MVP-B 5+T 威胁面 mitigation
- 新增：T9（GL command injection — 如允许 user JS 直接 dispatch GL command 需限制）
- 新增：T10（DRM permission — KMS 设备需 root / 容器化部署需 cap_sys_admin）

## 6. 长期愿景对接
- DevTool Phase E/F/G 超 MVP 但可在 MVP-C 完成后顺势启动
```

**commit message 范本：**

```
docs(creative): TASK-20260504-01 creative MVP-C blueprint

Source: TASK-20260504-01 spec §3.3 MVP-C 真嵌入式部署 档 detailed
        design + 9 gap 详细补全方案 + 核心目标 #2 (硬件加速) + #1
        主要 (嵌入式 Linux) 硬指标闭环路径.

MVP-C = 真嵌入式部署（~70%）—— 核心 P0 = C-G1 OpenGL ES (~30-60+ h
plan ×0.6) + C-G2 DRM/KMS (~10-20 h plan ×0.6) = 整个项目下半年主线.
```

**预计耗时：** ~10-15 min

---

### 4.5 子任务 5：Finalize 同步 + MB sync（commit 5）

**修改文件：**

| 文件 | 改动 |
|---|---|
| `README.md` | 「项目状态」段加「MVP 档对照」子段 + 路线图按 MVP-A/B/C 分层重写 |
| `memory-bank/projectbrief.md` | 加「MVP 范围参考」段 + 链接到 `docs/specs/2026-05-04-mvp-scope.md` |
| `memory-bank/productContext.md` | 已实现表加「MVP 档」列对照（沿用 spec 附录 D）|
| `memory-bank/{tasks,activeContext,progress}.md` | sync 阶段「规划中」→「**蓝图主交付物落盘完成**」（Level 4 V2=a 蓝图 = 「广义构建完成」）|

**关键点：**
- README 路线图段重写时**保留**既有「Phase A/B/C/D 已完成」实现里程碑表 + **新增** MVP-A/B/C 档对照子段（不替换）
- projectbrief 5 大目标段保留不动 + 加「MVP 范围参考」尾段
- productContext 已实现表加列时保持原 14 项次序 + 加 MVP 档列

**commit message 范本：**

```
chore(blueprint): finalize TASK-20260504-01 MVP-scope blueprint
                  全量同步 README + projectbrief + productContext + MB

Source: TASK-20260504-01 V5=c 全量同步 + V2=a 蓝图主交付物落盘完成.

4 处文档同步 (README + projectbrief + productContext + MB 三件套)
保持与 docs/specs/2026-05-04-mvp-scope.md + creative-mvp-A/B/C.md
一致.

Memory Bank 阶段「规划中」→「蓝图主交付物落盘完成」(Level 4 V2=a
蓝图变体「广义构建完成」语义 / 沿用 main.mdc Level 4 蓝图 V2=a 工作流
变体段).

下一步: /reflect 启动反思阶段.
```

**预计耗时：** ~5-10 min

---

## 5. 估时（standalone-AI 实测条件）

| 子任务 | 预计耗时 | 累计 |
|:-:|:-:|:-:|
| 1. Plan + Spec 同步落盘 | ~2-5 min | ~5 min |
| 2. creative-mvp-A.md | ~3-8 min | ~13 min |
| 3. creative-mvp-B.md | ~5-10 min | ~23 min |
| 4. creative-mvp-C.md | ~10-15 min | ~38 min |
| 5. Finalize 同步 + MB sync | ~5-10 min | ~48 min |
| **合计** | **~25-48 min** | — |

**plan ×0.6 比值预测：**
- 蓝图原始估时 ~2-3 h（120-180 min）
- standalone-AI 实测预期 ~25-48 min
- **比值预测：0.14-0.40×**（极宽 — 因蓝图任务无代码 build 风险 / 但 4-5 篇文档累计写作时间存在不确定性）
- 落入 **「creative 全锁死 + 范式 100% 复用 + AI agent 极速区 0.05-0.15×」候选** 还是 **「纯文档/规则极速区 0.15-0.25×」**？— 待 reflect 阶段实测后归档子档

---

## 6. 风险登记 R1-R4

详见 spec §7（4 项风险 / mitigation 已落实）：

| # | 风险 | 概率 | 影响 | mitigation |
|:-:|---|:-:|:-:|---|
| R1 | MVP 档定义在用户审查阶段被推翻 | 中 | 高 | brainstorm 1 次 AskQuestion 4 选项 + all_recommended 已锁定 / 审查阶段反对回退到 brainstorm |
| R2 | 与既有 README/projectbrief 信息漂移 | 中 | 中 | V5=c 全量同步 + finalize commit 守门 |
| R3 | MVP-C 估时 ~40-80 h 令用户失去推进动力 | 低 | 中 | 强调战略长期 / MVP-B 仅 ~2.5-6 h / 路线图渐进 |
| R4 | gap 清单与 activeContext P3 候选清单不一致 | 低 | 低 | 完整复用原文 / 不引入新 gap |

---

## 7. 反复模式预防清单

| # | 反复模式 | 本任务适用 | 预防措施 |
|:-:|---|:-:|---|
| 1 | 前置依赖/环境/API 能力未验证（含 add_test config guard 子维度）| ⚠️ 不适用代码 / **守门** | spec §8.2 已声明未来子任务参考时强制做 add_test config guard audit（[P0 #1 升级落实](33afb7c)）|
| 2 | 仅 happy path 测 | ❌ 不适用（纯文档）| — |
| 3 | 函数签名/默认值/返回类型透传链遗漏 | ❌ 不适用 | — |
| 4 | 管线注入点设计文档级可行性未验证 | ❌ 不适用 | — |
| 5 | 公开 API testability 接口前置识别 | ❌ 不适用 | — |
| 6 | 工具链版本激进升级行为变化 | ❌ 不适用 | — |
| 7 | reflection 沉淀回流（累积 ≥ 4-6 项跨任务 P1 待处理事项时批量清零）| ✅ 隐式适用 | spec §11.3 + activeContext 待处理事项段 sync — 后续工作流元任务可批量清零本任务 reflection 产出建议 |

**总判：** 7 项反复模式中 6 项不适用本任务（纯文档 / 零代码） / 1 项守门 / 1 项隐式适用 — **预期反复模式命中 0/7（创第 6 次零反复纪录候选）**。

---

## 8. 与既有 systemPatterns 协同度自我对照（≥ 11 模式）

详见 spec §9（11 模式自我对照）：

| # | systemPattern | 本任务对应 |
|:-:|---|---|
| 1 | Level 4 蓝图任务 V2=a 工作流变体 | ✅ 主路径 |
| 2 | plan ×0.6 数据点群组（蓝图任务）| 🆕 第 78-80 数据点群组（5 commits 实测）入库 |
| 3 | Phase 0 投入越深 / build phase 越快定律 sept-evidence | ✅ 7 子段 grep 实证 |
| 4 | 跨决策协同度 100%（all_recommended）| 🎯 第 8 次连续命中（11/11）|
| 5 | 反复模式渐进式抑制 | ✅ 守门 + 隐式 |
| 6 | commit body Source 溯源 + 实测数据 quad-evidence | 🎯 累计 32-33 commits 累计 |
| 7 | DevTool isolated JSRuntime 协议 | 引用（MVP-B + DevTool E/F/G 复用范本）|
| 8 | lazy-attach C ABI 容错模式 | 引用（MVP-B / DevTool 4 件套已沉淀）|
| 9 | A14 链接闭包零字节增长守门 | 引用（MVP-A.6 + MVP-B/C 沿用）|
| 10 | 5 大可复用 DevTool 范式 | 引用（MVP-B + DevTool E/F/G 复用）|
| 11 | reflection 沉淀回流模式 | 适用（MVP-B/C 子任务完成后回流到本 spec 状态更新）|

**协同度自评：** 11/11（100% — 5 ✅ 直接命中 + 5 引用 + 1 🆕 新数据点入库）

---

## 9. 检查点（CP1 + CP2）

### 9.1 CP1：spec + plan + commit 1 落盘后

**位置：** 子任务 1 完成后

**自审检查：**
- [ ] spec 12 段结构完整（§1-§12 + 附录 A-D）
- [ ] spec §3 三档验收标准 A.1-A.6 / B.1-B.10 / C.1-C.10 全列
- [ ] spec §3.2.1 MVP-B 4 gap + §3.3.1 MVP-C 9 gap 与 activeContext P3 候选清单 0 omission 0 duplicate
- [ ] spec §6 三档完成度对照矩阵填全（完成度 % / ctest / gap 数 / 估时 / 优先级）
- [ ] spec §10 DevTool Phase E/F/G 标识为「超 MVP / 是 plus」（避免与 MVP 档混淆）
- [ ] spec §11.2 推荐立项顺序 10 候选填全 + 对应 MVP 档
- [ ] spec §12 附录 A-D 4 个映射表完整
- [ ] plan 5 子任务分解 + commit message 范本就位
- [ ] commit 1 message 含 Source 溯源 + 实测数据 quad-evidence 格式
- [ ] MB 三件套阶段「初始化」→「规划中」

### 9.2 CP2：creative ×3 全部落盘后

**位置：** 子任务 4 完成后

**自审检查：**
- [ ] creative-mvp-A.md ~300-400 行 / 6 段结构完整
- [ ] creative-mvp-B.md ~400-500 行 / 4 项 gap 详细补全方案就位
- [ ] creative-mvp-C.md ~500-600 行 / 9 项 gap 详细补全方案就位
- [ ] 每篇 creative 的 commit body 含 Source 溯源到本 spec 对应段
- [ ] 各档 MVP 与 productContext.md 已实现表的映射一致

**CP2 通过 → 进入子任务 5 finalize**

---

## 10. 验收清单（plan 阶段产出物）

- [x] spec `docs/specs/2026-05-04-mvp-scope.md` 已落盘（~480 行）
- [ ] plan `docs/plans/2026-05-04-mvp-scope.md` 已落盘（本文档）
- [ ] MB 三件套阶段「初始化」→「规划中」
- [ ] commit 1 提交（含 spec + plan + MB 同步）
- [ ] CP1 自审通过

**完成标志：** 用户收到 commit 1 消息 + plan 文档可审 + 等待 `/creative` 启动 creative ×3 阶段。

---

**Plan 阶段结束。** 下一步：用户审查 spec + plan / 确认无重大反对 → commit 1 落盘 → 用户调用 `/creative` 启动 creative ×3 阶段（MVP-A → MVP-B → MVP-C 串行 3 commits）。
