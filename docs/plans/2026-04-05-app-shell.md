# 实现计划：事件循环与应用壳

**日期：** 2026-04-05
**任务 ID：** TASK-20260405-10
**设计文档：** `docs/specs/2026-04-05-app-shell-design.md`

## 文件结构

### 新建文件

| 文件 | 职责 |
|------|------|
| `veloxa/core/application.h` | Application 类定义 |
| `veloxa/core/application.cc` | Application 实现 |
| `tests/core/application_test.cc` | 单元测试 |
| `tests/core/application_integration_test.cc` | 全管线集成测试 |

### 修改文件

| 文件 | 变更 | 标注 |
|------|------|------|
| `veloxa/core/CMakeLists.txt` | 添加 `application.cc` | [共享文件] |
| `tests/CMakeLists.txt` | 添加 2 个测试目标 | [共享文件] |

## Phase 1：Application 类核心（单 Phase，无子代理）

各 Phase 间有强依赖（Phase 2 测试依赖 Phase 1 的 Application 类），不适合并行。
总代码量适中（~150 行实现 + ~300 行测试），不需要子代理。

### Phase 1.1：Application 类头文件 + 实现

#### 1.1.1 创建 `veloxa/core/application.h`

```cpp
#ifndef VELOXA_CORE_APPLICATION_H_
#define VELOXA_CORE_APPLICATION_H_

#include <memory>

#include "veloxa/core/css/parser.h"
#include "veloxa/core/dom/document.h"
#include "veloxa/core/event/event_manager.h"
#include "veloxa/core/layout/text_shaper.h"
#include "veloxa/core/update_manager.h"
#include "veloxa/foundation/strings/string_view.h"
#include "veloxa/graphics/canvas.h"
#include "veloxa/graphics/types.h"
#include "veloxa/platform/event_loop.h"
#include "veloxa/platform/surface.h"

namespace vx {

class Application {
 public:
  struct Config {
    platform::EventLoop* event_loop = nullptr;
    platform::Surface* surface = nullptr;
    u32 target_fps = 60;
    gfx::Color background_color = gfx::Color::White();
  };

  explicit Application(const Config& config);
  ~Application();

  Application(const Application&) = delete;
  Application& operator=(const Application&) = delete;

  void LoadHTML(StringView html);
  void LoadCSS(StringView css);
  void InjectInput(const event::InputEvent& input);

  void Run();
  void Quit();
  void Update();

  dom::Document* document() const { return document_; }
  event::EventManager& event_manager() { return event_manager_; }
  const UpdateManager* update_manager() const {
    return update_manager_.get();
  }

 private:
  void OnFrame();
  void EnsureUpdateManager();

  Config config_;
  dom::Document* document_ = nullptr;
  Vector<css::Stylesheet> stylesheets_;
  event::EventManager event_manager_;
  layout::SimpleTextShaper text_shaper_;

  u32* surface_pixels_ = nullptr;
  std::unique_ptr<gfx::Canvas> canvas_;
  std::unique_ptr<UpdateManager> update_manager_;
  platform::EventLoop::TimerId frame_timer_id_ = 0;
};

}  // namespace vx

#endif  // VELOXA_CORE_APPLICATION_H_
```

#### 1.1.2 创建 `veloxa/core/application.cc`

```cpp
#include "veloxa/core/application.h"

#include "veloxa/core/html/parser.h"
#include "veloxa/graphics/software/software_canvas.h"

namespace vx {

Application::Application(const Config& config) : config_(config) {
  if (config_.surface) {
    surface_pixels_ = config_.surface->Lock();
    canvas_ = std::make_unique<gfx::sw::SoftwareCanvas>(
        surface_pixels_, config_.surface->width(),
        config_.surface->height(), config_.surface->stride());
    canvas_->Begin();
    canvas_->Clear(config_.background_color);
  }
}

Application::~Application() {
  if (config_.surface && surface_pixels_) {
    config_.surface->Unlock();
  }
  delete document_;
}

void Application::LoadHTML(StringView html) {
  delete document_;
  document_ = html::Parser::Parse(html);
  update_manager_.reset();
}

void Application::LoadCSS(StringView css) {
  stylesheets_.push_back(css::CssParser::Parse(css));
  update_manager_.reset();
}

void Application::InjectInput(const event::InputEvent& input) {
  EnsureUpdateManager();
  if (update_manager_) {
    event_manager_.HandleInput(input, update_manager_->layout_root());
  }
}

void Application::Run() {
  EnsureUpdateManager();
  Update();

  if (config_.event_loop) {
    u32 interval = config_.target_fps > 0 ? 1000 / config_.target_fps : 16;
    frame_timer_id_ =
        config_.event_loop->SetTimer(interval, [this]() { OnFrame(); }, true);
    config_.event_loop->Run();
  }
}

void Application::Quit() {
  if (config_.event_loop) {
    config_.event_loop->CancelTimer(frame_timer_id_);
    config_.event_loop->Quit();
  }
}

void Application::Update() {
  EnsureUpdateManager();
  if (update_manager_) {
    update_manager_->Update();
  }
}

void Application::OnFrame() { Update(); }

void Application::EnsureUpdateManager() {
  if (update_manager_ || !document_ || !canvas_) return;
  UpdateManager::Config cfg;
  cfg.document = document_;
  cfg.stylesheets = &stylesheets_;
  cfg.layout_context.text_shaper = &text_shaper_;
  cfg.layout_context.viewport_width =
      config_.surface ? static_cast<f32>(config_.surface->width()) : 0;
  cfg.layout_context.viewport_height =
      config_.surface ? static_cast<f32>(config_.surface->height()) : 0;
  cfg.canvas = canvas_.get();
  cfg.event_manager = &event_manager_;
  update_manager_ = std::make_unique<UpdateManager>(cfg);
}

}  // namespace vx
```

#### 1.1.3 更新 `veloxa/core/CMakeLists.txt`

添加 `application.cc` 到 `vx_core` 源文件列表。

### Phase 1.2：单元测试

#### 1.2.1 创建 `tests/core/application_test.cc`

测试用例：

| # | 测试名 | 验证内容 |
|---|--------|---------|
| 1 | `ConstructWithSurface` | 构造成功，document 为 nullptr |
| 2 | `LoadHTMLCreatesDocument` | LoadHTML 后 document() 非空 |
| 3 | `LoadHTMLReplacesDocument` | 二次 LoadHTML 替换旧文档 |
| 4 | `LoadCSSAddsStylesheet` | LoadCSS 后 Update 可正常渲染 |
| 5 | `UpdateWithoutDocument` | 无 Document 时 Update 不崩溃 |
| 6 | `UpdateRendersContent` | LoadHTML+LoadCSS+Update 后 DisplayList 非空 |
| 7 | `InjectInputUpdatesState` | InjectInput(PointerMove) 更新 hovered |
| 8 | `QuitStopsEventLoop` | Run 后 PostTask(Quit) 退出循环 |
| 9 | `FrameTimerDrivesUpdate` | Run 启动定时器，PollOnce 触发 OnFrame |
| 10 | `MultipleCSSSheets` | 多次 LoadCSS 合并样式 |

#### 边界输入清单
- LoadHTML("") — 空 HTML
- InjectInput 在 LoadHTML 之前
- Run 在 LoadHTML 之前
- Update 在 LoadCSS 之前（无样式）
- Quit 在 Run 之前

### Phase 1.3：更新 `tests/CMakeLists.txt`

添加 `application_test` 目标。

## Phase 2：集成测试

### Phase 2.1 创建 `tests/core/application_integration_test.cc`

全管线集成测试，使用真实 HTML/CSS 解析器。

| # | 测试名 | 验证内容 |
|---|--------|---------|
| 1 | `RenderSimplePage` | LoadHTML+LoadCSS → Update → DisplayList 含 FillRect |
| 2 | `HoverChangesStyle` | LoadHTML+LoadCSS → InjectInput(PointerMove) → Update → 颜色变化 |
| 3 | `ActiveChangesStyle` | PointerDown → Update → :active 样式生效 |
| 4 | `RunAndQuit` | Run + PostTask(Quit) → 正常退出 |
| 5 | `MultipleInputsCoalesce` | 多次 InjectInput → 一次 Update → 只有最终状态的颜色 |
| 6 | `FullInteractionSequence` | hover → active → release → 完整交互序列 |

#### 关键约束（来自 P1 待处理事项）
- 使用真实 HTML/CSS 解析器（html::Parser::Parse + css::CssParser::Parse）
- 不使用 inline style
- 颜色比较通过 CssColorToGfx
- DisplayList 结构验证优先

### Phase 2.2 更新 `tests/CMakeLists.txt`

添加 `application_integration_test` 目标。

## 调用链端到端验证（来源 TASK-09 P1）

Application 的调用链：
```
Application::InjectInput(input)
  → EventManager::HandleInput(input, layout_root)
    → HitTest(layout_root, x, y)
    → 更新 hovered_/active_/focused_
    → InvalidationCallback() → UpdateManager::Invalidate()

Application::Update() / OnFrame()
  → UpdateManager::Update()
    → LayoutEngine::Layout(doc, ctx, arena)
      → BuildTree → StyleResolver::Resolve(el, ..., em)
        → CollectMatchingRules → SelectorMatcher::Matches(sel, el, em)
    → render::Record(root)
    → render::ComputeDirtyRect(old, new, vp)
    → Canvas::PushClipRect + FillRect + Replay + PopClip
```

所有中间链路已在 TASK-08/09 中验证通过。Application 仅做顶层编排，无新的参数透传需求。

## 预估

- **新增文件**：4 个
- **修改文件**：2 个
- **新增测试**：~16 个
- **实现代码**：~80 行
- **测试代码**：~350 行
