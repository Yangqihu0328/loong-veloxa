# Veloxa MVP 范围定义（Spec / 蓝图）

**文档 ID：** `2026-05-04-mvp-scope`
**任务 ID：** `TASK-20260504-01`（Level 4 蓝图 V2=a 完整变体）
**创建日期：** 2026-05-04
**状态：** 蓝图（设计规格 / 待用户审查后落盘）
**文档形态：** V3=c 双管齐下 — 本 spec 详细定义 + `memory-bank/projectbrief.md` 加章节链接 + `README.md` 路线图段同步 + `memory-bank/productContext.md` 已实现表加 MVP 档对照

---

## 1. 目的（What）

为 Veloxa（车载 HMI / 嵌入式高性能 UI 渲染引擎）建立**统一的 MVP 范围定义** + **可量化验收清单** + **三档分级矩阵**，解决项目缺独立 MVP scope 文档导致 "MVP 还差什么" 类问题难以客观回答的现状。

### 1.1 解决的具体问题

1. **MVP 边界模糊** — `projectbrief.md` 列了 5 大核心目标 / `productContext.md` 列了「已实现的核心功能」表 / `feature-completion-design.md §不在范围内` 列了局部 MVP 排除项 / 29 specs 散落各 milestone — **缺统一基准**
2. **优先级排期争议** — activeContext.md 的 P3 候选清单（DomBindings R2 / OpenGL ES / DRM/KMS / DevTool Phase E/F/G）之间无优先级，难以决定下一步立项哪个
3. **资源投入决策无基准** — "嵌入式硬件加速"（核心目标 #2）等长期目标的实施时机难以决断
4. **第三方集成者无 MVP 锚点** — README 路线图段是"按 Phase 列举"而非"按 MVP 档分层"，集成者难以判断"现在能拿来做什么"

### 1.2 本 spec 的最终交付物（V5=c 全量同步）

| 路径 | 角色 | 改动 |
|---|---|---|
| `docs/specs/2026-05-04-mvp-scope.md`（本文档）| 主 spec / 蓝图 | 🆕 新建 / 12 段 ~700-900 行 |
| `memory-bank/creative/creative-mvp-A.md` | MVP-A 详细设计 | 🆕 新建 ~300-400 行 |
| `memory-bank/creative/creative-mvp-B.md` | MVP-B 详细设计 | 🆕 新建 ~400-500 行 |
| `memory-bank/creative/creative-mvp-C.md` | MVP-C 详细设计 | 🆕 新建 ~500-600 行 |
| `README.md` | 公开门面路线图段 | 🟡 「项目状态」段加「MVP 档对照」子段 + 路线图按 MVP-A/B/C 分层 |
| `memory-bank/projectbrief.md` | 项目简介 5 大目标 | 🟡 加「MVP 范围参考」段 + 链接到本 spec |
| `memory-bank/productContext.md` | 产品上下文已实现表 | 🟡 已实现表加「MVP 档」列对照 |
| `memory-bank/{tasks,activeContext,progress}.md` | MB 三件套 | 🟡 sync 到 MVP-A/B/C 视角的待处理事项 / P3 候选优先级 |

---

## 2. 不做（Out of Scope，YAGNI）

本 spec **不做**以下事项，避免范围蔓延：

1. **不做 build 实施** — Level 4 V2=a 蓝图变体 / 主交付 = spec + plan + creative ×3 文档 + 同步候选；MVP-B/C 的 gap 修复（如 DomBindings R2 三连补全 / G1 OpenGL ES / G2 DRM/KMS）由用户后续基于本 spec 独立立项 N 个 Level 3-4 子任务
2. **不做新核心目标定义** — 沿用 `projectbrief.md` 5 大核心目标作为 MVP 验收维度框架（轻量化 / 高性能渲染 / Web 兼容子集 / 可嵌入性 / 开源开放）— 不引入第 6 个目标
3. **不做新威胁面识别** — 沿用 `2026-04-30-devtool-design.md §7` T1-T8 + 既有 spec 威胁面定义；本 spec 仅做 MVP 档与威胁面的对照映射
4. **不做新 systemPatterns 沉淀** — 本任务是文档蓝图 / 无新代码 / 无新设计模式；仅在 §9 做与既有 systemPatterns 兼容性对照
5. **不做实施估时精确化到子任务** — 本 spec 仅给档级粗粒度估时（plan ×0.6 范围）；后续独立立项 Level 3-4 子任务时按各自 plan 精化

---

## 3. 三档 MVP 分级验收标准（D 三档分级 / V1=D）

### 3.1 MVP-A：最小可运行 demo（基线档 — 已 100% 达成 ✅）

**目标场景：** 嵌入式 boot 屏 / 简单状态显示 / 静态 HMI 占位

**验收标准（A.1-A.6）：**

| # | 标准 | 当前状态 | 验证方式 |
|:-:|---|:-:|---|
| **A.1** | Veloxa 引擎能 `LoadHTML(html_str)` + `LoadCSS(css_str)` + 执行 1 帧渲染输出（headless `MemorySurface` 或 SDL2 真窗口）| ✅ | `examples/hello.cc` + `application_test` |
| **A.2** | 支持 ~45 CSS 属性 + Block / Inline / Flex 布局（含 margin collapsing + line box + vertical-align）| ✅ | `css_engine_test` + `layout_engine_test`（CSS 2.1 §8.3.1 + §10.8 实测）|
| **A.3** | 软件渲染 + DisplayList + 脏区增量重绘 + border-radius | ✅ | `software_canvas_test` + `display_list_test` |
| **A.4** | C ABI 公共头（`vx_view_create` / `vx_view_load_html` / `vx_view_load_css` / `vx_view_update` / `vx_event_loop_create` / `vx_surface_create_*`）| ✅ | `veloxa_api.h` + `c_api_test` |
| **A.5** | SDL2 实时窗口后端（桌面 / WSLg）+ 输入事件桥接（鼠标 / 键盘）| ✅ | `examples/hello_sdl2.cc` + `hello_sdl2_smoke` ctest |
| **A.6** | A14 链接闭包零字节增长（`VX_BUILD_DEVTOOL=OFF` 时二进制零增长）| ✅ | `tests/smoke/devtool_a14_link_closure.cmake` |

**ctest baseline：** DEVTOOL=ON 1284/1284 PASS / DEVTOOL=OFF 1091/1091 PASS（截至 [TASK-20260503-04 闭环 commit `33afb7c`](33afb7c)）

**MVP-A 完成度：** ✅ **100%**（baseline 状态保持机制 — 后续任何任务不得退化）

**已知 gap：** 0

---

### 3.2 MVP-B：桌面 dogfood 完整（~90% — 4 项 gap 待补）

**目标场景：** HMI 原型开发 / Demo 演示 / 第三方集成者桌面 evaluation / 设计师 + 开发者完整 dogfood 工作流

**验收标准（B.1-B.10）：**

| # | 标准 | 当前状态 | gap / 验证方式 |
|:-:|---|:-:|---|
| **B.1** | 含 MVP-A 全部能力（A.1-A.6）| ✅ | 继承 MVP-A |
| **B.2** | DOM 事件 W3C 三阶段 + `:hover` / `:active` / `:focus` + CSS Transitions 单属性过渡 | ✅ | `event_test` + `transition_manager_test` |
| **B.3** | QuickJS 脚本引擎 + Interrupt Handler T1 5 维度 mitigation + 32 MiB 内存限制 | ✅ | `quickjs_engine_test`（[TASK-05 闭环](33afb7c)）|
| **B.4** | JS-DOM 绑定基础（`getElementById` / 属性 / `style` proxy / 事件监听器）| ⚠️ **部分** | **gap #1-#3**（详 §3.2.1）|
| **B.5** | FreeType + HarfBuzz 字体渲染 + GlyphCache + `hb_shape` cache | ✅ | `text_test` + `glyph_cache_test` |
| **B.6** | PNG / JPEG 图片解码 + ImageCache + `<img>` 元素 | ✅ | `image_decoder_test` |
| **B.7** | DevTool 4 件套全闭环（Inspector + Performance Overlay + Hot Reload + Console JS REPL）| ✅ | [TASK-20260502-01/02 + TASK-20260503-01/04 闭环](33afb7c)）|
| **B.8** | F12 / F11 hotkey toggle + dogfood 完整链路（DevTool 用 Veloxa 自身渲染调试 UI）| ✅ | `examples/hello_devtool.cc` + smoke tests |
| **B.9** | Performance Overlay 多帧持续验证（dirty rect 边框可视化跨多帧 / 60 帧 ring buffer 滑动平均）| ⚠️ **部分** | **gap #4**（详 §3.2.1 — Performance Overlay 持续 invalidate 机制）|
| **B.10** | 完整安全威胁面 mitigation（T1 任意 eval + T2 路径穿越 + T3 redaction + T5 overlay 隔离 + T6 callback budget + T7 buffer overflow）| ✅ | DevTool 蓝图 §7 全 mitigation |

#### 3.2.1 MVP-B 已知 gap（4 项）

| # | gap | 来源 | 优先级 | 估时 plan ×0.6 | 立项形态 |
|:-:|---|---|:-:|:-:|---|
| **B-G1** | DomBindings.Element.children 集合 getter（HTMLCollection 风格）| TASK-20260502-01 R2 P3 #1 / dogfood 暴露 | P3 高 | 部分（详下） | Level 3 任务 `DomBindings R2 三连补全` |
| **B-G2** | DomBindings.element.addEventListener | 同上 R2 P3 #2 | P3 高 | 部分（详下） | 同上 |
| **B-G3** | DomBindings.element.innerHTML setter | 同上 R2 P3 #3 | P3 高 | 部分（详下） | 同上 |
| **B-G4** | Performance Overlay 持续 invalidate 机制 | TASK-20260503-03 P3 #0 / dirty_ 机制硬约束 | P3 中 | ~30-60 min（路径 b） / ~1-2 h（路径 a） | Level 1-3 任务 |

**B-G1+G2+G3 合并立项估时：** ~2-4 h plan ×0.6（archive §6 已校准 -30%）— 受益于 Phase A/B/C dom_bindings.cc 三次扩展经验 + JS native binding 范式成熟

**MVP-B 完成度：** ⚠️ **~90%**（gap 总估时 ~2.5-6 h plan ×0.6 / 1 个 Level 3 任务 + 1 个 Level 1-3 任务 = 2 个独立子任务）

---

### 3.3 MVP-C：真嵌入式部署（~70% — 2 项关键 gap + 多项次要 gap）

**目标场景：** 车载中控 / 仪表盘 / 工业 HMI / IoT 面板 / 医疗设备 — `projectbrief.md` "目标场景" 全覆盖

**验收标准（C.1-C.10）：**

| # | 标准 | 当前状态 | gap / 验证方式 |
|:-:|---|:-:|---|
| **C.1** | 含 MVP-B 全部能力（B.1-B.10）| ⚠️ | 继承 MVP-B + 补 4 项 B-gap |
| **C.2** | **OpenGL ES / Vulkan 硬件加速渲染后端**（60fps 流畅复杂界面）| ❌ **缺** | **C-G1**（强阻塞核心目标 #2）|
| **C.3** | **DRM/KMS 嵌入式 Linux 后端**（无 X11 / Wayland 裸 Linux 部署）| ❌ **缺** | **C-G2**（强阻塞核心目标 #1 "嵌入式 Linux 主要目标"）|
| **C.4** | 资源加载策略明确（打包 vs 文件系统 vs 混合）| ❌ **缺** | **C-G3**（systemPatterns §待定架构决策）|
| **C.5** | 多平台构建支持（嵌入式 Linux / Linux Desktop ✅ / 车载 RTOS 远期）| ⚠️ **部分** | **C-G4**（仅 Desktop ✅；嵌入式 Linux 需 G2；RTOS 远期）|
| **C.6** | DomBindings 高级能力（`createElement` / `removeChild` / DOM 节点动态创建删除）| ❌ **缺** | **C-G5**（feature-completion §不在范围内）|
| **C.7** | CSS 高级特性（`@keyframes` Animations / 完整 z-index stacking context / `position: fixed` 真支持 / `pointer-events: none` 真支持 / per-corner border-radius）| ❌ **缺** | **C-G6**（5 项 CSS gap）|
| **C.8** | 图像扩展（WebP 解码 / 双线性图片缩放 / 异步图片加载）| ❌ **缺** | **C-G7**（feature-completion §不在范围内）|
| **C.9** | EventManager HitTest 改造（HUD `pointer-events` 真支持 — 不依赖 `data-passthrough` 占位）| ❌ **缺** | **C-G8** R9 P3 候选 |
| **C.10** | 性能优化收口（#35 阶段 2 拆 LayoutEngine / R3+ #1 image_decoder 安全三件套 / 其他 codebase review R3+ 13 项 P1 候选）| ❌ **缺** | **C-G9** 一组 |

#### 3.3.1 MVP-C 已知 gap（9 项）

| # | gap | 来源 | 优先级 | 估时 plan ×0.6 | 立项形态 |
|:-:|---|---|:-:|:-:|---|
| **C-G1** | OpenGL ES / Vulkan 硬件渲染后端 | `projectbrief.md` 核心目标 #2 / 当前仅 SoftwareCanvas | **P0**（强阻塞）| ~30-60+ h | Level 4 多 Phase 独立立项 |
| **C-G2** | DRM/KMS 嵌入式 Linux 后端 | `projectbrief.md` 核心目标 #1 主要目标 / spec `2026-04-25-sdl2-window-backend-design.md §未来工作` 已留接口（`Surface::Present()` 抽象 + 双轨 CMake 模板可复用）| **P0**（强阻塞）| ~10-20 h | Level 3-4 独立立项 |
| **C-G3** | 资源加载策略 | systemPatterns §待定架构决策 | P1 | ~5-10 h | Level 3 蓝图 + 实施 |
| **C-G4** | 嵌入式 Linux 平台构建支持 | techContext §平台后端 / 含 G2 + 工具链交叉编译 | P1 | ~5-10 h（含 G2 完成后）| Level 3 |
| **C-G5** | DomBindings 节点创建删除 | feature-completion §不在范围内 | P2 | ~3-5 h | Level 3 |
| **C-G6** | CSS 高级特性 5 项（Animations / z-index / fixed / pointer-events / per-corner border-radius）| 散落多 spec | P2 | ~10-20 h（5 项分立）| 5 个独立 Level 2-3 任务 |
| **C-G7** | 图像扩展 3 项（WebP / 双线性 / 异步）| feature-completion §不在范围内 | P3 | ~6-12 h（3 项分立）| 3 个独立 Level 2 任务 |
| **C-G8** | R9 EventManager HitTest 改造 | TASK-20260502-02 P3 #2 | P3 | ~1.5-2 h | Level 2-3 |
| **C-G9** | 性能优化收口（#35 阶段 2 / R3+ #1 / 其他 R3+ 13 项）| TASK-20260502-02 P3 #1 + TASK-30-03 R3+ | P3 | ~10-30 h（多任务集合）| 多个独立 Level 2-3 |

**MVP-C 完成度：** 🟡 **~70%**（核心 P0 gap = C-G1 + C-G2 = ~40-80 h plan ×0.6 / 估为整个项目下半年主线）

---

## 4. 设计决策矩阵 D1-D4（VAN + brainstorm 产出）

### 4.1 VAN 阶段决策（5 维度 — 用户已锁定 2 项）

| # | 维度 | 决策 | 理由 |
|:-:|---|---|---|
| **D1**（V1）| MVP 范围视角 | **D 三档分级 MVP-A/B/C** ✅ | 兼顾历史已交付「桌面 dogfood」+ 核心目标 #2「嵌入式硬件加速」长期目标 / 避免单一档定义争议 |
| **D2**（V2）| 工作流形态 | **a V2=a 完整蓝图变体**（含 creative ×N 不含 build）✅ | 沿用 [TASK-20260430-04 DevTool 三件套蓝图范式](33afb7c) / Level 4 蓝图（systemPatterns「Level 4 蓝图任务 V2=a 工作流变体」段）|
| **D3**（V3）| 文档形态 | **c 双管齐下** ✅ | 主 spec 详细定义 + projectbrief 章节链接 + README 路线图同步 + productContext 已实现表 MVP 档对照 — 4 处对齐避免漂移 |
| **D4**（V4）| 验收清单粒度 | **c 二者兼有** ✅ | 粗粒度功能列表（产品视角）+ 细粒度 ctest / smoke + 配置矩阵 — 沿用 [writing-plans.mdc «ctest 数量预期 config 矩阵»](33afb7c) audit |
| **D5**（V5）| 同步更新范围 | **c 全量同步** ✅ | spec + README + projectbrief + productContext 4 处一致更新 + finalize commit 一并落盘 |

### 4.2 Plan 阶段 B1-B7 决策（用户已锁定 all_recommended 7/7）

| # | 维度 | 决策 | 备注 |
|:-:|---|---|---|
| **B1** | 主 spec 文档结构 | 沿用 [DevTool 蓝图 spec §1-§12 范式](33afb7c) | 12 段：目的/不做/验收/决策矩阵/架构/优先级/风险/测试策略/systemPatterns 兼容/扩展段/与未来任务关系/附录 |
| **B2** | creative ×N 拆分粒度 | **3 篇**（MVP-A/B/C 各一篇）| 视 V1=D 拆分；每篇 ~300-600 行 |
| **B3** | 验收清单矩阵格式 | 单矩阵 三档 × 多维度 | 行=MVP-A/B/C；列=完成度 % / ctest 数量 / gap 数 / 估时 / 优先级 |
| **B4** | 与既有 spec 交叉引用规范 | 上游/下游分表 | 上游 6 类素材；下游 = 后续所有任务排期参照 MVP-A/B/C 优先级 |
| **B5** | 估时 | plan ×0.6 ~120-180 min（2-3 h）| Level 4 V2=a 但规模约 1/3 of TASK-30-04（4 篇→3 篇）/ 总约 2000-3500 行 |
| **B6** | commit 拆分粒度 | **5 commits**：plan+spec / creative-A / creative-B / creative-C / finalize 同步 | 沿用 [TASK-30-04 蓝图范式](33afb7c) |
| **B7** | Phase 0 grep 实证 | 7 子段 | §0.1 ctest baseline / §0.2 spec 文件名冲突 / §0.3 projectbrief 5 大目标 / §0.4 productContext 已实现表 / §0.5 29 specs 完成度 audit / §0.6 activeContext P3 候选 / §0.7 反复模式 #1 抑制 |

### 4.3 跨决策协同度自检

V1+V3+V4+V5+B1-B7 = **11/11 用户 1 次 AskQuestion 全推荐锁定** — 创纪录第 8 次跨决策协同度 100%（vs TASK-04 第 7 次 12/12）。Brainstorm + plan 阶段决策耗时 ~5 min（standalone-AI 实测条件）。

---

## 5. 架构

### 5.1 无新架构

本 spec **不引入新架构** — 完全引用既有：

| 既有架构 | 来源 | MVP 档对应 |
|---|---|:-:|
| 8 大子系统（foundation / platform / graphics / core / text / script / api / devtool）| README §架构概览 + projectbrief.md | A/B/C 全 |
| Surface + EventLoop + UpdateManager + Application 主循环 | techContext §平台后端 | A/B/C 全 |
| 5 大 DevTool 范式（双 UpdateManager / 双层 API / #ifdef + CMake conditional / canvas Translate / 资源嵌入）| TASK-20260502-01 沉淀 | B + C |
| isolated JSRuntime 协议 + DomBindings host data channel | [TASK-20260503-04 archive §6 沉淀](33afb7c) | B + C（DevTool Phase E/F/G 复用）|
| lazy-attach C ABI 容错模式 | TASK-20260502-02 沉淀 | B + C |
| Phase 0 投入越深 / build phase 越快定律 sept-evidence | systemPatterns §Phase 0 投入定律 | 适用所有 MVP-B/C gap 实施 |

### 5.2 与 V1=D 三档分级的架构层级映射

```
┌──────────────────────────────────────────────────────────────┐
│  MVP-C（真嵌入式部署 — ~70%）                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  + OpenGL ES / Vulkan 后端（C-G1）                      │  │
│  │  + DRM/KMS 嵌入式后端（C-G2）                           │  │
│  │  + 资源加载策略 / 多平台 / DOM 高级 / CSS 高级 / 图像扩展 │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  MVP-B（桌面 dogfood 完整 — ~90%）                      │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │  + DomBindings R2 三连补全（B-G1/G2/G3）           │  │  │
│  │  │  + Performance Overlay 持续 invalidate（B-G4）     │  │  │
│  │  │  + DevTool 4 件套全闭环 ✅                        │  │  │
│  │  │  + QuickJS T1 5 维度 mitigation ✅              │  │  │
│  │  └──────────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │  MVP-A（最小可运行 demo — ✅ 100%）               │  │  │
│  │  │  HTML/CSS/Layout/SoftwareCanvas/SDL2/C ABI/A14   │  │  │
│  │  └──────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

**关键不变量：** MVP-C ⊃ MVP-B ⊃ MVP-A（严格包含关系）— 高档完成必含低档全部能力。

---

## 6. 优先级 + 三档完成度对照矩阵

### 6.1 三档总览矩阵

| 维度 | MVP-A | MVP-B | MVP-C |
|---|:-:|:-:|:-:|
| **完成度 %** | ✅ **100%** | ⚠️ **~90%** | 🟡 **~70%** |
| **ctest 数量预期（DEVTOOL=ON）** | 1284 baseline 不退化 | 1284 + ~30-50 测（DomBindings R2）+ ~5-10（perf invalidate）≈ 1325-1345 | MVP-B + ~200-500 测（OpenGL ES + DRM/KMS + CSS 高级 + 图像扩展等多任务集合）|
| **ctest 数量预期（DEVTOOL=OFF）** | 1091 baseline 不退化 | 1091 + ~10（OFF stub）≈ 1101 | 1101 + ~50-100 测 |
| **已知 gap 数** | **0** | **4** | **9** |
| **总估时 plan ×0.6（达成下一档）** | — | ~2.5-6 h | ~40-80 h（C-G1+C-G2 主线）+ ~30-80 h（其他 7 项） |
| **优先级排期建议** | baseline 状态保持 | **优先**（小成本 / 大收益 / 解锁第三方集成）| **战略长期**（C-G1+C-G2 是核心目标 #2 硬指标）|
| **代表性场景** | 嵌入式 boot 屏 / 简单状态显示 | HMI 原型开发 / Demo 演示 / 第三方桌面 evaluation | 车载中控 / 仪表盘 / 工业 HMI / IoT 面板 |
| **核心目标对应** | #4 可嵌入性 + #5 开源 | + #3 Web 兼容子集 | + #1 嵌入式 Linux + #2 高性能渲染（硬件加速）|

### 6.2 推荐立项顺序（基于优先级 + ROI）

**短期（MVP-B 收口 / ~2.5-6 h 总投入）：**
1. ⭐ `TASK-2026MMDD-NN: DomBindings R2 三连补全`（B-G1+G2+G3 / Level 3 / plan ×0.6 ~2-4 h）— **第一推荐**（小成本 / 大收益 / dogfood 视觉自动恢复）
2. `TASK-2026MMDD-NN: Performance Overlay 持续 invalidate 机制`（B-G4 / Level 1-3 / plan ×0.6 ~30 min-2 h）— 路径 (b) hello_devtool 注入 CSS animation 推荐先做

**中期（MVP-B → MVP-C 过渡 / ~5-10 h 总投入）：**
3. `TASK-2026MMDD-NN: 资源加载策略蓝图`（C-G3 / Level 3 蓝图 + 实施 / plan ×0.6 ~5-10 h）
4. `TASK-2026MMDD-NN: R9 EventManager HitTest 改造`（C-G8 / Level 2-3 / plan ×0.6 ~1.5-2 h）

**长期主线（MVP-C 核心 P0 gap / ~40-80 h 总投入）：**
5. **`TASK-2026MMDD-NN: G1 OpenGL ES 硬件渲染后端蓝图`**（C-G1 / Level 4 多 Phase / plan ×0.6 ~30-60+ h）— **核心目标 #2 硬指标**
6. **`TASK-2026MMDD-NN: G2 DRM/KMS 嵌入式后端`**（C-G2 / Level 3-4 / plan ×0.6 ~10-20 h）— **核心目标 #1 主要目标硬指标**

**长期次线（MVP-C 其他 gap）：**
7. CSS 高级 5 项（C-G6 / 5 个独立任务）
8. DomBindings 节点动态创建删除（C-G5 / Level 3 / plan ×0.6 ~3-5 h）
9. 图像扩展 3 项（C-G7 / 3 个独立任务）
10. 性能优化收口（C-G9 / 多任务集合）

### 6.3 DevTool Phase E/F/G 与 MVP 关系

DevTool 三件套 + Console JS REPL = MVP-B 的一部分（已 ✅）。其余 DevTool 扩展：

| 子系统 | 任务 | 与 MVP 关系 | 优先级 |
|---|---|---|:-:|
| JS Debugger backend | TASK-30-04-E | **超 MVP / 是 plus** | 用户决策驱动 |
| CDP 远程调试 port | TASK-30-04-F | **超 MVP / 是 plus** | 用户决策驱动 |
| 完整 UI 编辑器 | TASK-30-04-G | **超 MVP / 长期愿景** | 长期 |

---

## 7. 风险登记 R1-R4

| # | 风险 | 概率 | 影响 | mitigation |
|:-:|---|:-:|:-:|---|
| **R1** | MVP 档定义在用户审查阶段被推翻（如认为「应单一档」/「应分四档」）| 中 | 高（spec + creative ×3 重写）| Plan 阶段 brainstorm 已 1 次 AskQuestion 抛 V1=D 决策 + 提供 A/B/C/D 4 选项 / 用户已 all_recommended 锁定 / **审查阶段如有反对应回退到 brainstorm 而非直接撕毁** |
| **R2** | 与既有 README 路线图 / projectbrief 5 大目标段产生信息漂移 | 中 | 中 | V5=c 全量同步 / finalize commit 一并落盘 / 单 commit 守门一致性 |
| **R3** | MVP-C 估时 ~40-80 h 看似巨大令用户失去推进动力 | 低 | 中 | 强调 MVP-C 是**战略长期目标** / MVP-B 收口仅 ~2.5-6 h 即可解锁 dogfood 完整 / 路线图按 MVP-A → B → C 渐进 |
| **R4** | gap 清单与既有 activeContext P3 候选清单不一致 | 低 | 低 | 本 spec §3.2.1 + §3.3.1 完整复用 activeContext P3 候选清单原文 / 不引入新 gap 项 / 仅做 MVP 档归类 |

---

## 8. 测试策略

### 8.1 ctest 数量预期 config 矩阵（强制 — writing-plans.mdc «ctest 数量预期 config 矩阵» audit）

| MVP 档 | DEVTOOL=ON | DEVTOOL=OFF | SDL2=ON | Benchmarks=ON | 备注 |
|---|:-:|:-:|:-:|:-:|---|
| **MVP-A baseline** | **1284** | **1091** | +1 hello_sdl2_smoke | — | 截至 [TASK-04 闭环](33afb7c) |
| **MVP-B 期望** | 1284 + ~30-50 | 1091 + ~10 | 同 A | — | DomBindings R2 三连补全 + perf invalidate 估值 |
| **MVP-C 期望** | MVP-B + ~200-500 | MVP-B + ~50-100 | + DRM/KMS smoke | — | 含 G1 + G2 + 多任务集合估值（粗粒度）|

### 8.2 add_test config guard 边界假设审计（强制 — writing-plans.mdc 新增 audit 子条 / [P0 #1 升级落实](33afb7c)）

本任务**不新增 add_test**（纯文档蓝图）→ 不适用本 audit；但本 spec 用作未来子任务参考时，应在子任务 plan 阶段强制做该 audit（沿用 [TASK-04 升级 P0 落实](33afb7c) 的 add_test config guard 强制 audit 子条）。

### 8.3 三档 MVP 验收 smoke 测建议

为每档 MVP 设立单一 smoke test 集合标识符（建议未来实施）：

| MVP 档 | 建议 ctest label | 含义 |
|---|---|---|
| MVP-A | `mvp-a-baseline` | 严守 baseline 不退化（A.1-A.6 全 PASS）|
| MVP-B | `mvp-b-dogfood` | DevTool 4 件套 + DomBindings R2 + perf invalidate 全 PASS |
| MVP-C | `mvp-c-embedded` | OpenGL ES + DRM/KMS + 完整 CSS / DOM 高级全 PASS |

> **注：** 本 spec 仅建议 label / 不强制本任务实施 — 由后续 MVP-B / MVP-C 收口子任务实现。

---

## 9. 与既有 systemPatterns 兼容性（≥ 11 模式自我对照）

| # | systemPattern | 本 spec 对应 |
|:-:|---|---|
| 1 | Level 4 蓝图任务 V2=a 工作流变体 | ✅ 主路径 — `/van → /plan → /creative ×3 → /reflect → /archive` 跳过 build |
| 2 | plan ×0.6 数据点群组（蓝图任务）| 🆕 第 78-80 数据点群组（5 commits 实测）入库 |
| 3 | Phase 0 投入越深 / build phase 越快定律 sept-evidence | ✅ 7 子段 grep 实证延续 TASK-04 范式 |
| 4 | 跨决策协同度 100%（all_recommended）| 🎯 第 8 次连续命中（V1+V3+V4+V5+B1-B7 = 11/11）|
| 5 | 反复模式渐进式抑制 | ✅ 1 次抑制（add_test config guard 不适用本任务但守门 + 文档名冲突 grep）|
| 6 | commit body Source 溯源 + 实测数据 quad-evidence | 🎯 累计 27-28 + 5 = 32-33 commits 累计 quad-evidence |
| 7 | DevTool isolated JSRuntime 协议 | 引用（MVP-B / DevTool Phase E 复用范本）|
| 8 | lazy-attach C ABI 容错模式 | 引用（MVP-B DevTool 4 件套 / 已沉淀）|
| 9 | A14 链接闭包零字节增长守门 | 引用（MVP-A.6 验收标准 + MVP-B/C 沿用）|
| 10 | 5 大可复用 DevTool 范式 | 引用（MVP-B / DevTool Phase E/F/G 复用） |
| 11 | reflection 沉淀回流模式 | 适用（MVP-B/C 子任务完成后回流到本 spec 状态更新）|

---

## 10. 扩展段（V1=D 三档外的候选 / 超 MVP）

以下子系统**超 MVP-C 范围**，是 plus 而非 MVP：

| # | 子系统 | 来源 | 量级 |
|:-:|---|---|:-:|
| 1 | DevTool Phase E · JS Debugger backend | TASK-30-04 蓝图 / spec §11.2 | Level 4 |
| 2 | DevTool Phase F · CDP 远程调试 port | TASK-30-04 蓝图 / spec §11.4 | Level 3-4 |
| 3 | DevTool Phase G · 完整 UI 编辑器 | TASK-30-04 蓝图 / spec §11.3 | Level 4+ |
| 4 | Vulkan 渲染后端（vs OpenGL ES）| 长期愿景 | Level 4 |
| 5 | 完整 Web Animations API | 长期愿景 / 非车载 HMI 必须 | Level 4 |
| 6 | SVG 支持 | systemPatterns §待定架构决策 | Level 3-4 |

---

## 11. 与未来任务的关系

### 11.1 本 TASK-20260504-01 主交付（V2=a 完整变体）

| 交付物 | 类型 | 状态 |
|---|---|:-:|
| `docs/specs/2026-05-04-mvp-scope.md`（本 spec）| 主蓝图文档 | 待落盘 |
| `memory-bank/creative/creative-mvp-A.md` | MVP-A 详细设计 | 待落盘 |
| `memory-bank/creative/creative-mvp-B.md` | MVP-B 详细设计 | 待落盘 |
| `memory-bank/creative/creative-mvp-C.md` | MVP-C 详细设计 | 待落盘 |
| `README.md` 路线图段 | 公开门面同步 | 待落盘 |
| `memory-bank/projectbrief.md` MVP 范围参考段 | 项目简介同步 | 待落盘 |
| `memory-bank/productContext.md` 已实现表 MVP 档列 | 产品上下文同步 | 待落盘 |

### 11.2 用户后续独立立项 TASK 候选（基于本 spec §6.2 推荐立项顺序拆出）

| 优先 | 候选任务 | MVP 档 | Level | plan ×0.6 |
|:-:|---|:-:|:-:|:-:|
| 1 | DomBindings R2 三连补全 | MVP-B 收口 | L3 | ~2-4 h |
| 2 | Performance Overlay 持续 invalidate 机制 | MVP-B 收口 | L1-3 | ~30 min-2 h |
| 3 | 资源加载策略蓝图 | MVP-C 过渡 | L3 蓝图 + 实施 | ~5-10 h |
| 4 | R9 EventManager HitTest 改造 | MVP-C | L2-3 | ~1.5-2 h |
| 5 | **G1 OpenGL ES 硬件渲染后端蓝图** | MVP-C 核心 | L4 多 Phase | ~30-60+ h |
| 6 | **G2 DRM/KMS 嵌入式后端** | MVP-C 核心 | L3-4 | ~10-20 h |
| 7 | DomBindings 节点动态创建删除 | MVP-C | L3 | ~3-5 h |
| 8 | CSS 高级特性 5 项 | MVP-C | 5 × L2-3 | ~10-20 h |
| 9 | 图像扩展 3 项 | MVP-C | 3 × L2 | ~6-12 h |
| 10 | 性能优化收口 | MVP-C | 多 L2-3 | ~10-30 h |

### 11.3 与 TASK-30-03（codebase review）R3+ 13 项 P1 候选交叉关系

R3+ 13 项 P1 候选大多落入 MVP-C 范围内（C-G9 性能优化收口 + 部分 CSS 高级 / 图像扩展子项）— 详见 `docs/reports/2026-04-30-codebase-review.md`。

### 11.4 已识别但本 spec 不展开的细节

- 各档 MVP 的 example 范例设计（hello_mvp_a.cc / hello_mvp_b.cc / hello_mvp_c.cc）— 由各档子任务实施时设计
- 各档 MVP 的 dogfood smoke test 设计 — 同上
- 各档 MVP 的性能基准（如 MVP-A frame time / MVP-C 60fps 验证标准）— 由各档子任务设计

---

## 12. 附录

### 附录 A：5 大核心目标 → MVP 档映射

| 核心目标（projectbrief.md §核心目标）| MVP-A | MVP-B | MVP-C | 备注 |
|---|:-:|:-:|:-:|---|
| 1. 轻量化（嵌入式 Linux / RTOS 资源约束）| ✅ | ✅ | ✅ | A14 + 8 大子系统设计天然轻量 |
| 2. **高性能渲染（OpenGL ES / Vulkan，60fps）** | ⚠️ | ⚠️ | ✅ | A/B 仅 SoftwareCanvas / **C 必须 G1** |
| 3. Web 兼容子集 | ⚠️ | ⚠️ | ✅ | A/B 子集 / C 完整 |
| 4. 可嵌入性（C API）| ✅ | ✅ | ✅ | C ABI 已就位 |
| 5. 开源开放（MIT/Apache 2.0）| ✅ | ✅ | ✅ | License 已确定 |

### 附录 B：构建配置矩阵 → MVP 档对应

| Config | MVP-A | MVP-B | MVP-C |
|---|:-:|:-:|:-:|
| `VX_BUILD_TESTS=ON` | ✅ | ✅ | ✅ |
| `VX_BUILD_BENCHMARKS=ON` | 可选 | 可选 | ✅ 推荐（性能基线）|
| `VX_PLATFORM_SDL2=ON` | ✅（A.5）| ✅ | ⚠️ 桌面开发用 / 嵌入式部署用 G2 |
| `VX_BUILD_DEVTOOL=ON` | ❌ 不必（A 不依赖 DevTool）| ✅ | 可选（嵌入式部署可关闭节省体积）|
| **新增**：`VX_RENDERER=software\|gles\|vulkan` | software | software | **gles 推荐**（C-G1）|
| **新增**：`VX_PLATFORM_DRM=ON` | ❌ | ❌ | ✅（C-G2）|

### 附录 C：与既有 spec 交叉引用

| 既有 spec | 段 | 作为本 spec 的角色 |
|---|---|---|
| `2026-04-05-foundation-design.md` | 全 | 上游 — Foundation 是 A.1-A.6 基础 |
| `2026-04-05-c-api-design.md` | 全 | 上游 — A.4 C ABI 设计源 |
| `2026-04-05-graphics-platform-hal-design.md` | 全 | 上游 — A.3 SoftwareCanvas + C-G1 GLES Surface 抽象基础 |
| `2026-04-05-css-engine-design.md` | 全 | 上游 — A.2 CSS 子集 |
| `2026-04-05-layout-engine-design.md` | 全 | 上游 — A.2 Block/Inline/Flex |
| `2026-04-05-css-transitions-design.md` | 全 | 上游 — B.2 transitions |
| `2026-04-13-quickjs-integration-design.md` | 全 | 上游 — B.3 QuickJS |
| `2026-04-14-feature-completion-design.md` | §不在范围内 | 上游 — C-G5/G6/G7 多项 gap 来源（per-corner border-radius / WebP / 双线性 / DOM 节点动态 / JS modules）|
| `2026-04-25-sdl2-window-backend-design.md` | §未来工作 | 上游 — A.5 SDL2 + C-G2 DRM/KMS 接口 |
| `2026-04-30-devtool-design.md` | §11.1-11.4 + §12.2 | 上游 — B.7 DevTool 4 件套 + 扩展段 E/F/G |
| `2026-04-26-layout-correctness-design.md` | 全 | 上游 — A.2 margin collapsing + line box |
| `2026-04-30-margin-collapse-with-parent-design.md` | 全 | 上游 — A.2 margin collapsing 子集 |
| 其他 16 specs | — | 中性 — 各自子系统设计源（Foundation / DOM / Render / Event / Update / etc）|

### 附录 D：已实现里程碑 → MVP 档对照（productContext.md sync 用）

| 里程碑 | TASK | 对应 MVP 档 |
|---|---|:-:|
| Foundation 实现（2026-04-05）| TASK-20260405-01 等 | A |
| Graphics/Platform HAL 实现 | 同期 | A |
| DOM/HTML Parser 实现 | 同期 | A |
| CSS Engine 实现 | 同期 | A |
| Layout Engine 实现 | 同期 | A |
| Render Pipeline 实现 | 同期 | A |
| Event System 实现 | 同期 | B |
| Update Manager 实现 | 同期 | A |
| Application Shell 实现 | 同期 | A |
| C API 层实现 | 同期 | A.4 |
| CSS Transitions | 2026-04-13 | B.2 |
| 功能补全（border-radius / FreeType / 图片 / JS-DOM 绑定）| TASK-20260414-01 | A.3 + B.4 + B.5 + B.6 |
| Layout 正确性（margin collapsing / line box / vertical-align）| TASK-20260426-01 | A.2 |
| SDL2 实时窗口后端 | TASK-20260425-01 | A.5 |
| DevTool 三件套蓝图 | TASK-20260430-04 | B.7 蓝图 |
| DevTool Phase A · Inspector | TASK-20260502-01 | B.7 |
| DevTool Phase B · Performance Overlay | TASK-20260502-02 | B.7 + B.9（部分 — 缺 invalidate）|
| DevTool Phase C · Hot Reload | TASK-20260503-01 | B.7 |
| DevTool Phase D · Console JS REPL | TASK-20260503-04 | B.7 |
| QuickJS Interrupt Handler | TASK-20260503-05 | B.3 + B.10 T1 |

---

**文档结束。** 待用户审查后落盘 + plan 阶段 commit 1（plan + spec 同步落盘）。
